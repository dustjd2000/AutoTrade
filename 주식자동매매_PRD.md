# PRD: 개인용 주식 자동매매 프로그램 (키움증권 API 기반)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.6 |
| 작성일 | 2026-07-26 (최초), 2026-07-27 (1호 전략·이메일 반영, 키움 API 연동 완료, 모의투자 복원) |
| 대상 | 개인 투자자 (본인 단독 사용) |
| 매매 대상 | 국내 주식 현물 (KOSPI/KOSDAQ) |
| 1호 전략 | LLM 기반 급등 예상 대형주 매수 (08:45 추천·메일 → 09:00 매수·매도조건 설정 → 15:30 성과 리포트 메일) |
| 상태 | Draft |

---

## 1. 개요

### 1.1 배경
반복적인 매매 판단(진입/청산 타이밍, 손절 실행)을 사람이 직접 하면 감정 개입과 실행 지연이 발생하기 쉽다. 키움증권이 제공하는 API를 통해 시세 조회·주문 실행을 프로그램으로 처리하고, 매매 전략은 별도 모듈로 자유롭게 교체·추가할 수 있는 개인용 자동매매 시스템을 구축한다.

### 1.2 문제 정의
- 사람이 장중 내내 화면을 보며 조건을 판단하기 어렵다 (기회 손실, 피로도).
- 손절/익절 규칙을 세워도 실행 시점에 감정적으로 어기는 경우가 많다.
- 특정 전략 하나에 종속되지 않고, 전략을 바꿔가며 검증하고 싶다.

### 1.3 제품 비전
"전략 로직을 갈아 끼울 수 있는 개인용 매매 엔진" — 인증/시세/주문/리스크관리/로깅 같은 공통 기반(프레임워크)을 먼저 견고하게 만들고, 그 위에 첫 번째 실전략으로 **LLM 기반 급등 예상 대형주 매수 전략**을 얹는다. 이후에도 전략은 계속 교체·추가 가능해야 한다.

---

## 2. 목표 및 성공 지표

### 2.1 목표
1. 키움증권 API로 로그인, 시세 조회, 주문(매수/매도/정정/취소), 잔고 조회를 안정적으로 자동화한다.
2. 매매 전략을 코드 변경 최소화로 교체할 수 있는 구조(전략 인터페이스)를 만든다.
3. 손실을 제한하는 리스크 관리 규칙(손절, 일일 손실 한도, 포지션 한도)을 시스템 레벨에서 강제한다.
4. 모의투자 환경에서 충분히 검증한 뒤에만 실전 계좌로 전환할 수 있는 안전장치를 둔다.
5. 매매 내역과 시스템 상태를 로그로 남기고, 이상 상황(주문 실패, 연결 끊김 등)을 알림으로 받는다.

### 2.2 성공 지표 (개인 프로젝트 기준)
- 모의투자 환경에서 1개월 이상 무중단(수동 개입 없이) 운영 성공
- 주문 성공률 99% 이상 (네트워크/API 오류로 인한 누락 제외 목표)
- 일일 손실 한도 초과 시 자동 매매 중단 100% 동작
- 장애 발생 시 알림 수신까지 1분 이내

### 2.3 비목표 (이번 범위에서 제외)
- 해외주식, 선물/옵션, 코인 등 타 자산군 지원
- 여러 명이 함께 쓰는 멀티유저/상용 서비스화
- 고빈도매매(HFT) 수준의 초저지연 최적화
- 1호 전략 외 추가 전략의 이번 범위 내 확정 (프레임워크만 확장 가능하게 열어두고, 2호 이후 전략은 추후 별도 결정)

---

## 3. 사용자 및 사용 시나리오

- **사용자**: 본인 1인 (개발자 = 사용자)
- **핵심 시나리오 (1호 전략 기준 하루 흐름)**
  1. 08:40 이전: 시스템이 자동으로 로그인 및 계좌 연동을 완료하고, 당일 예수금을 조회해둔다.
  2. **08:45**: 당일 데이터(전일 종가 대비 등락, 거래량, 뉴스/공시 등)를 수집해 LLM에 전달하고, "급등이 예상되는 대형주 3개"를 추천받아 로그로 남긴다.
  3. **09:00 (장 시작)**: 예수금 전체의 1/2을 매수가능금액으로 산정하고, 이를 다시 3등분하여 추천받은 3개 종목에 각각 매수 주문을 실행한 뒤, 곧바로 각 종목에 대한 매도 조건(손절가/익절가 등)을 설정한다. 이때도 리스크 관리 규칙(종목당 한도, 일일 손실 한도 등)을 통과해야 실제 주문이 나간다.
  4. 장중 특이사항(주문 거부, API 연결 끊김, 손실 한도 도달, LLM 추천 실패)은 즉시 알림으로 받는다.
  5. **15:30 (장 마감 무렵)**: 당일 매수/매도 결과와 이번 달 누적 수익률·수익금을 정리한 리포트 이메일을 자동으로 받는다.
  6. 전략을 바꾸고 싶을 때는 전략 모듈 파일만 교체하면 나머지 시스템(주문/리스크/로깅)은 그대로 재사용된다.

---

## 4. 키움증권 API 연동 전략

키움증권은 현재 두 계열의 API를 제공한다. 세부 스펙(도메인, 엔드포인트, 인증 방식 등)은 버전에 따라 바뀔 수 있으므로 **구현 직전 반드시 키움증권 공식 개발자 문서로 최종 확인**해야 한다.

| 구분 | 키움 Open API+ (구, OCX/COM) | 키움 REST API (신규) |
|---|---|---|
| 통신 방식 | OCX 컨트롤 (COM 기반) | HTTP REST + WebSocket |
| 운영체제 | Windows 전용 (32bit 프로세스) | OS 무관 (서버/리눅스/맥 가능) |
| 인증 | 영웅문 로그인 창 | 앱키 + 시크릿 → 접근토큰 발급 |
| 강점 | 실시간 조건검색식 등 기능이 풍부, 자료·예제 많음 | 서버 상시 배포 용이, 24시간 무인 운영에 적합 |
| 약점 | 로그인 창이 뜬 PC를 계속 켜둬야 함, 서버 배포 어려움 | 일부 기능(조건검색식 등)이 구버전 대비 아직 제한적일 수 있음 |

**권고안**: 개인 PC를 상시 켜두고 운영할 계획이면 Open API+ 로 시작해도 무방하지만, 이 프로젝트는 "무인 자동화"가 핵심 목표이므로 **REST API + WebSocket 조합을 기본 방향으로 채택**한다. 단, 필요한 조건검색식 기능이 REST 쪽에서 아직 지원되지 않는다면 해당 기능만 Open API+로 보완하는 하이브리드 구성도 검토한다.

### 4.1 인증/계좌 연동 요구사항
- 앱키/시크릿, 접근토큰 등 민감정보는 코드에 하드코딩하지 않고 환경변수 또는 별도 설정파일(.gitignore 처리)로 관리
- 실전투자 계좌와 모의투자 계좌를 설정으로 명확히 분리 (실수로 실전 계좌에 테스트 주문이 나가는 사고 방지)
- 토큰 만료 시 자동 재발급 처리

---

## 5. 기능 요구사항

### 5.1 계좌/인증 관리
- 앱키/시크릿 기반 토큰 발급 및 자동 갱신
- **매매 모드: 모의(paper) / 실전(live) 전환** — 키움이 모의투자 서버(`mockapi.kiwoom.com`)를 제공하므로 두 모드를 모두 지원한다. 기본값은 모의투자이며, 저장된 값이 실전이어도 UI는 항상 모의로 시작한다
- **실전 전환 및 실전 엔진 시작 시 확인 절차 필수** — 확인 문구(`YES_I_UNDERSTAND`) 입력을 요구한다
- 연결 상태 헬스체크 (주기적 ping, 끊김 감지 시 재연결 시도)

### 5.2 시세 조회
- 현재가, 호가, 체결 정보 조회
- 관심종목 등록 및 실시간 시세 수신(WebSocket)
- 과거 일봉/분봉 데이터 조회 (전략 개발 및 추후 백테스팅용)

### 5.3 주문 관리
- 매수/매도 주문 실행 (지정가/시장가)
- 주문 정정/취소
- 주문 체결 여부 실시간 확인
- 주문 실패 시 사유 로깅 및 알림

### 5.4 잔고/보유종목 조회
- 예수금, 보유종목, 평가손익 조회
- 장 시작 시 잔고 스냅샷 저장 (일일 리포트 기준값)

### 5.5 전략 프레임워크 및 1호 전략

**(A) 프레임워크 공통 요구사항**
- 공통 전략 인터페이스 정의: 시세/데이터를 입력받아 "매수/매도/보유" 신호(대상 종목, 수량 또는 금액 포함)를 반환하는 표준 함수 형태
- 전략 모듈은 독립 파일/클래스로 작성하고, 설정에서 어떤 전략을 쓸지만 지정하면 나머지 시스템(주문 실행, 리스크 관리)은 그대로 동작
- 시간 기반(예: 매일 08:45, 09:00)으로 전략을 실행하는 스케줄러 필요 — 실시간 시세 이벤트 기반 전략뿐 아니라 정해진 시각에 실행되는 전략도 지원해야 함
- 추후 전략 추가 시 시스템 코드 수정 없이 전략 파일 추가만으로 확장 가능해야 함

**(B) 1호 전략: LLM 기반 급등 예상 대형주 매수**

| 단계 | 시각 | 내용 |
|---|---|---|
| 1. 데이터 수집 | 08:45 이전 | 당일 활용 가능한 데이터(전일 종가 대비 등락률, 거래량, 시가 갭, 뉴스/공시 헤드라인 등) 수집 |
| 2. LLM 추천 요청 | 08:45 | 수집한 데이터를 프롬프트로 구성하여 LLM에 전달, "대형주 중 급등 예상 종목 3개"를 요청 |
| 3. 추천 결과 알림 | 08:45~09:00 | LLM 응답을 구조화된 형식(예: JSON — 종목코드, 종목명, 추천 사유)으로 파싱한 뒤, **자동 유효성 검증 없이** 해당 내용을 이메일로 사용자에게 발송 |
| 4. 자금 산정 | 09:00 | 매수가능금액 = 예수금 전체 × 1/2 → 종목당 매수금액 = 매수가능금액 × 1/3 (= 예수금의 약 1/6) |
| 5. 매수 실행 | 09:00 (장 시작 직후) | 3개 종목에 대해 각각 산정된 금액만큼 매수 주문 실행 |
| 6. 매도 설정 | 09:00 (매수 직후) | 매수 체결된 3개 종목 각각에 대해 매도 조건(손절가/익절가 등)을 설정 |

- **예시**: 예수금 1,000만 원 → 매수가능금액 500만 원 → 종목당 약 166.7만 원씩 3종목 매수
- **주문 방식**: 장 시작 직후 즉시 체결이 목표이므로 기본은 시장가 매수를 원칙으로 하되, 지정가 대안 여부는 구현 단계에서 결정
- **LLM 연동 요구사항**
  - 사용할 LLM 및 API는 구현 단계에서 확정 (예: Anthropic Claude API 등)
  - LLM에 입력하는 데이터 출처와 프롬프트 템플릿을 코드/설정으로 버전 관리하여, 추천 근거를 나중에 추적할 수 있어야 함
  - LLM 응답이 형식을 벗어나거나(파싱 실패), 응답 자체가 지연되어 09:00까지 도착하지 않으면 **해당일 매수는 스킵**하고 알림 발송 (무리하게 대체 로직으로 임의 매수하지 않음)
  - **추천 결과 이메일 발송 (필수)**: 파싱된 추천 종목 3개에 대해 아래 항목을 포함한 이메일을 08:45~09:00 사이 발송
    - 종목코드
    - 종목명
    - 추천 근거 (LLM이 제시한 사유)
  - 종목별 매매 가능 여부(대형주 유니버스 소속, 거래정지 여부 등)에 대한 **사전 자동 검증은 수행하지 않음** — 매매 불가 종목이 추천된 경우 09:00 매수 시도 시 키움 API 주문 거부로 드러나며, 이 경우 주문 실패 로그/알림(5.7, 5.8절)으로 사용자가 인지
  - 추천받은 종목이 3개 미만일 경우의 처리 방식(예: 남은 자금 배분 방식)은 Open Questions에서 별도 확정 필요
- **매도(청산) 조건 (확정)**: 매수 체결가 대비 **+2% 익절 / -2% 손절** (UI에서 %로 조정 가능, 기본값 각 2%)
  - **매도 방식 (조사 결과 반영, 2026-07-27)**: 커뮤니티 REST API 래퍼(207개 엔드포인트 기준) 조사 결과 매수/매도/정정/취소 기본 주문만 확인되고 조건부 예약주문(스탑오더) 엔드포인트는 확인되지 않음 — **공식 문서로 최종 확인 전까지는 미지원으로 가정**하고, 시스템이 실시간 시세를 모니터링하다 +2%/-2% 도달 시 시장가 매도하는 방식(5.6절 `check_exit`)을 **1차이자 사실상 유일한 방식**으로 운영. `OrderClient.send_stop_order`는 추후 키움이 네이티브 스탑오더를 지원하는 것으로 확인될 경우를 대비한 스텁으로 유지
  - **당일 매도 원칙 (데이트레이딩)**: 장 마감 전까지 익절/손절 조건이 충족되지 않은 포지션은 **장 마감 직전 강제 매도**로 당일 내 정리 (다음날로 이월하지 않음)

**(C) 향후 전략 확장**
- 2호 이후 전략은 이번 범위에서 확정하지 않으며, 위 프레임워크 인터페이스를 그대로 따르는 새 모듈로 추가

### 5.6 리스크 관리 (필수, 시스템 레벨 강제)
- 종목당 최대 투입 금액/비중 한도 — 1호 전략은 구조적으로 예수금의 약 1/6씩 투입되므로, 이 값을 초과하는 주문은 리스크 레이어에서 거부 (전략 로직 버그로 잘못된 금액이 계산되는 경우를 대비한 이중 안전장치)
- 예수금의 나머지 1/2은 항상 미투입 상태로 유지(1호 전략 자체 규칙) — 시스템은 이 여유 자금이 임의로 소진되지 않도록 보장
- 일일 최대 손실 한도 도달 시 자동으로 신규 매수 중단
- 익절/손절 라인(+2%/-2%) 도달 시 자동 매도 — 키움 스탑오더 미지원(조사 결과) 가정 하에 시스템 모니터링이 사실상 1차 방식(전략과 무관하게 항상 동작하는 안전장치)
- 장 마감 전 당일 매도 원칙에 따른 미청산 포지션 강제 정리
- 전체 계좌 대비 최대 노출 비중 제한
- 3종목 동시 매수로 인한 종목 집중 리스크는 대형주 한정으로 일부 완화되지만, 3종목이 서로 다른 업종/테마인지까지는 전략이 보장하지 않으므로 필요 시 업종 중복 체크 로직 추가 검토

### 5.7 로깅 및 기록
- 모든 매수/매도 주문 요청·체결·실패 내역을 타임스탬프와 함께 저장 (파일 또는 로컬 DB)
- 일별 손익 요약 자동 생성
- 월별 누적 수익률·누적 수익금을 계산할 수 있도록 일별 손익을 월 단위로 집계·누적 저장
- 시스템 오류/예외 상황 로그 별도 기록

### 5.8 알림
- 주문 체결, 주문 실패, 연결 끊김, 손실 한도 도달 시 알림 발송
- **채널은 이메일 단독으로 확정 (2026-07-27)** — 텔레그램은 사용하지 않기로 하여 구현에서 제거했다. 운영 알림은 `AlertNotifier`가 이메일로 발송한다(`src/notification/alert.py`)
- 1호 전략의 LLM 추천 결과 알림(종목코드·종목명·추천근거)과 15:30 성과 리포트도 동일하게 이메일로 발송된다 (5.11절)

### 5.9 (Phase 2 이후) 백테스팅
- 과거 데이터로 전략을 미리 검증할 수 있는 별도 모듈
- 실거래 전략 인터페이스와 동일한 구조를 재사용하여 "백테스트 → 모의투자 → 실전" 순서로 검증 단계를 밟도록 설계

### 5.10 (Phase 2 이후, 선택) 모니터링 대시보드
- 현재 보유종목, 당일 손익, 시스템 상태를 볼 수 있는 간단한 웹 대시보드
- 우선순위는 낮음 (로그 파일 확인으로 초기 버전 대체 가능)

### 5.11 일일/월간 성과 리포트 이메일 (15:30)
- 매 거래일 **15:30**에 아래 내용을 담은 이메일 리포트를 자동 발송
  - 당일 매수/매도 결과: 종목별 체결가·수량·당일 손익
  - 이번 달 누적 수익률(%) 및 누적 수익금(원) — 월초부터 당일까지 합산
- 국내 정규장 마감 시각이 15:30이므로, 당일 마지막 체결 데이터가 확정되기 전에 리포트가 생성될 수 있다는 타이밍 이슈가 있음 — 정산 지연을 감안해 마감 직후 몇 분의 버퍼를 둘지, 아니면 요청대로 정확히 15:30에 발송하고 필요 시 다음날 정정 안내를 할지는 Open Questions에서 결정
- 이 리포트도 5.8절과 마찬가지로 이메일로 발송

---

## 6. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 안정성 | API 연결 끊김/오류 발생 시 프로세스가 죽지 않고 재시도 또는 안전 정지 |
| 보안 | 인증정보는 코드/저장소에 노출 금지, 로컬 암호화 또는 환경변수 사용 |
| 성능 | 실시간 시세 수신부터 주문 실행까지 수 초 이내 (초단타 수준의 초저지연은 목표 아님) |
| 운영 환경 | 기본은 REST API 기반으로 OS 독립적 구성, 필요 시 개인 PC/소형 서버(NAS, 클라우드 VM 등)에서 상시 구동 |
| 유지보수성 | 전략 모듈과 핵심 엔진(주문/리스크/로깅)의 결합도를 낮게 유지 |
| 장애 대응 | 장중 예외 발생 시 "포지션을 정리하고 정지"할지 "정지만 하고 포지션 유지"할지 설정 가능해야 함 |

---

## 7. 시스템 구성 (개략)

```
[스케줄러: 08:45 / 09:00 / 15:30 트리거]
        |
        v
[데이터 수집 모듈] --당일 데이터--> [LLM 추천 모듈] --추천 3종목(코드·종목명·근거)--> [이메일 발송 모듈] --> (사용자 메일함)
        |                                    |
   (시세/뉴스 등)                             v
        |                              [전략 모듈] --신호(매수+매도조건)--> [리스크 관리 레이어]
        v                                                                       |
[시세 수신 모듈] -------------------------------------------------------> (승인된 주문)
                                                                                 v
                                                                       [주문 실행 모듈] --> [키움 API]
                                                                                 |
                                                                                 v
                                                                       [로깅/알림 모듈] --15:30 집계--> [리포트 생성 모듈] --> [이메일 발송 모듈] --> (사용자 메일함)
```

- **스케줄러**: 08:45 데이터수집·LLM추천, 09:00 매수 및 매도조건 설정, 15:30 성과 리포트 발송을 시각 기준으로 트리거 (1호 전략 전용, 실시간 이벤트 기반 전략도 병행 지원 가능해야 함)
- **데이터 수집 모듈**: LLM 프롬프트에 쓸 당일 데이터(등락률, 거래량, 뉴스 등)를 모음
- **LLM 추천 모듈**: 데이터를 프롬프트로 구성해 LLM 호출, 응답을 구조화된 추천 종목 리스트(코드·종목명·근거)로 변환
- **이메일 발송 모듈**: LLM 추천 결과(08:45~09:00)와 성과 리포트(15:30)를 자동 검증 없이 그대로 사용자에게 이메일로 전달
- **시세 수신 모듈**: WebSocket으로 실시간 시세를 받아 전략/리스크 판단에 사용
- **전략 모듈**: 표준 인터페이스를 따르는 교체 가능한 로직 (현재는 1호 전략 탑재, 매수 신호와 매도 조건을 함께 산출)
- **리스크 관리 레이어**: 전략의 신호를 그대로 실행하지 않고, 종목당 한도/손실 한도를 먼저 검사
- **주문 실행 모듈**: 실제 키움 API 호출 담당, 재시도/오류 처리 포함
- **로깅/알림 모듈**: 전 구간(LLM 추천 근거, 매수·매도 결과 포함)의 이벤트를 기록하고 필요 시 알림 발송
- **리포트 생성 모듈 (신설)**: 로그에 쌓인 당일 매수·매도 결과와 월간 누적 데이터를 집계해 15:30 리포트 내용을 구성

---

## 8. 리스크 및 제약사항

- **규제/약관 리스크**: 증권사 Open API 이용약관상 금지 행위(비인가 재판매, 과도한 요청 등)를 반드시 준수해야 하며, 자동매매라 해도 투자 결과에 대한 책임은 본인에게 있음
- **API 변경 리스크**: 신규 REST API는 상대적으로 이력이 짧아 스펙 변경 가능성이 있으므로, 외부 API 호출부는 별도 모듈로 감싸 변경에 대응하기 쉽게 설계
- **요청 제한(Rate Limit)**: TR/REST 호출 건수 제한이 있으므로 과도한 폴링 대신 WebSocket 실시간 구독을 우선 활용
- **재무적 리스크**: 전략 오류나 시스템 버그로 인한 실계좌 손실 가능성 — 반드시 모의투자에서 충분히 검증 후 실전 전환, 실전 초기에는 소액으로 시작
- **단일 장애점**: 개인 PC/서버 1대에 의존하므로 정전, 인터넷 장애 시 대응 방안(예: 장애 시 자동 전량 정지) 필요
- **LLM 예측 불확실성**: LLM의 "급등 예상" 추천은 예측일 뿐 정확성이 보장되지 않으며, 시장 상황에 따라 손실로 이어질 수 있음 — 시스템 설계로 이를 없앨 수는 없고, 종목당 한도·손절 규칙으로 손실 폭을 제한하는 것이 현실적 목표
- **LLM 응답 지연/비용 리스크**: 08:45~09:00 사이 15분 안에 응답을 받아야 하므로 API 응답 지연 시나리오에 대한 타임아웃/스킵 처리가 필수이며, LLM API 호출 비용도 지속 발생
- **사전 검증 부재 리스크**: 추천 종목에 대한 자동 유효성 검증을 이메일 알림으로 대체했으므로, 거래정지·상장폐지 등 매매 불가 종목이 추천되면 09:00 매수 시도 시점에야 주문 실패로 드러남 — 사용자가 08:45 이메일을 실제로 확인하지 않으면 이상 상황을 사전에 인지하기 어려움
- **15:30 리포트 타이밍 리스크**: 국내 정규장 마감 시각이 15:30으로 리포트 발송 시각과 겹치므로, 마감 직전 체결 건이 리포트에 누락되거나 잠정치로 표시될 수 있음

---

## 9. 개발 로드맵 (제안)

| Phase | 내용 | 산출물 |
|---|---|---|
| Phase 0 | **(사용자 직접 진행)** 키움증권 계좌 개설 및 Open API 사용신청, 앱키/시크릿 발급 | 발급된 인증키 |
| Phase 1 | API 인증/연동, 시세 조회, 주문 실행 기본 기능 구현 (모의투자 계좌 대상) | 동작하는 API 래퍼 |
| Phase 2 | 전략 인터페이스 + 시간 기반 스케줄러 설계, 더미 전략(HOLD)으로 전체 파이프라인 연결 검증 | 최소 동작 시스템 |
| Phase 3 | 리스크 관리 레이어, 로깅(매수/매도 결과 포함), 알림 기능 추가 | 안전장치 포함 시스템 |
| Phase 4 | LLM 연동 모듈(데이터 수집 → 프롬프트 → 추천 파싱) 및 08:45 이메일 발송, 09:00 매수·매도조건 설정 로직 구현 | 1호 전략 매매 동작 버전 |
| Phase 5 | 로그 기반 15:30 일일/월간 성과 리포트 이메일 기능 구현 | 리포트 이메일 동작 버전 |
| Phase 6 | 모의투자 환경에서 1호 전략 장기 안정성 검증 (최소 2~4주, LLM 추천 정확도·리포트 정확성 관찰 포함) | 검증 로그/리포트 |
| Phase 7 | 소액 실계좌로 전환 후 투입 금액 단계적 상향, 모니터링 강화 | 실전 운영 |

---

## 10. 추후 결정 필요 사항 (Open Questions)

- 실행 환경: 상시 켜두는 개인 PC vs 클라우드 VM
- **"당일 데이터"의 구체적 범위**: 등락률·거래량·시가갭 필드는 정의했으나(`DailyStockData`), 뉴스/공시를 실제로 어느 소스(DART, 네이버 뉴스 등)에서 가져올지 미확정
- **15:30 리포트의 정산 타이밍**: 마감 직후 버퍼(예: 15:35)를 둘지, 정확히 15:30에 발송 후 다음날 정정치를 별도 안내할지
- **월간 누적 수익률 산정 기준일**: 매월 1일부터 누적할지, 실전 전환일부터 누적할지 (우선 매월 1일 기준으로 구현됨, 변경 가능)

**확정된 항목 (구현 완료, 2026-07-27):**
- 개발 언어/기술 스택: Python + PyQt6(UI) + requests/websockets
- 데이터 저장 방식: SQLite (`src/logger/trade_store.py`) — 개인 프로젝트 규모에 맞춰 별도 서버 없이 파일 하나로 운영
- 알림 채널: **이메일 단독** — 텔레그램은 사용하지 않기로 하여 제거했다. 운영 알림·LLM 추천·성과 리포트가 모두 이메일로 나간다 (5.8절)
- **키움 REST API 스탑오더 지원 여부 (2026-07-28 확인 완료, 미지원 확정)**: 공식 API 가이드의
  국내주식 주문 카테고리는 `kt10000`(매수)/`kt10001`(매도)/`kt10002`(정정)/`kt10003`(취소) 4개뿐이며,
  커뮤니티 래퍼(국내주식 207개 엔드포인트) 역시 주문 관련은 위 4개 + 금현물(`kt50000~3`) +
  신용(`kt10006~9`) 12개가 전부다. **조건부·스탑·예약 주문 엔드포인트는 존재하지 않는다.**
  → 모니터링 방식(`RiskManager.check_exit`)이 API 경로에서 유일한 청산 수단으로 확정.
  → 단, **영웅문4 `[0624]` 자동감시주문은 서버 측 감시**이며("HTS, MTS의 ON/OFF 상태에 상관없이
  저장된 감시조건이 만족될 경우 자동으로 주문이 실행됩니다", 최대 90일 지속, 보유종목 자동매도 50개
  한도) 이익실현·손실제한·트레일링스탑을 지원한다. **API로는 등록할 수 없어 HTS 수동 설정만 가능**하다.
  참고로 `[0621]` 주식StopLoss는 HTS가 켜져 있어야 동작하므로 본 프로그램과 한계가 같다.
- **"대형주" 기준 (구현 반영)**: 키움이 코스피200 구성종목을 직접 제공하지 않아, `ka10099`의 시가총액 규모별 분류에서 **`대형주`(시총 상위 100종목)**를 유니버스로 사용한다. 거래정지·관리종목·투자경고 종목은 수집 단계에서 제외한다
- 이메일 발송 방식: SMTP (UI에서 서버/계정/앱비밀번호 설정)
- **LLM 제공처/모델**: Anthropic Claude API (`ANTHROPIC_API_KEY`, 기본 모델 `claude-sonnet-5`)
- **"대형주" 기준**: 코스피200 구성종목 (`KOSPI200Universe` — 실제 종목 리스트 조회 API 연동은 남음)
- **추천이 3개 미만일 때의 자금 배분**: 종목당 투입금액을 예수금의 1/6로 고정하고, 모자란 몫은 매수하지 않고 현금으로 유지 (`LLMMomentumStrategy.build_buy_plans`)

---

## 11. 구현 필요 목록 (2026-07-27 기준 코드 스캔 결과)

프레임워크(스케줄러·리스크·로깅·알림)와 1호 전략 로직(자금배분·LLM 파싱·이메일 본문)은 구현 및 단위테스트(32개 통과)를 마쳤다. **남은 미구현 항목은 전부 외부 시스템 연동부**이며, 아래 항목이 채워지기 전까지는 프로그램을 실행해도 인증 단계에서 중단되어 08:45 추천 이메일을 받을 수 없다.

### 11.1 현재 실행 시 중단 지점

| 순서 | 지점 | 증상 |
|---|---|---|
| 1차 | `engine.start()` → `AuthClient.ensure_token()` | `NotImplementedError: 키움 API 토큰 발급 구현 필요` — 스케줄러 등록 전에 프로세스 종료 |
| 2차 | (인증 통과 가정 시) 08:45 `DataCollector.collect()` | 코스피200 종목 조회 미구현 → 수집 0건 → LLM 호출 스킵 → **추천 이메일 대신 "데이터 수집 실패" 경고만 발송** |

### 11.2 필수 구현 항목 (우선순위 순)

| # | 항목 | 파일 | 비고 |
|---|---|---|---|
| 1 | **API base URL 수정** | `config/settings.py:18-19` | 현재 값이 **한국투자증권**(`koreainvestment.com`) 주소 — 키움 REST API(`openapi.kiwoom.com` 계열)로 교체 필요. 잘못된 채로 두면 인증부터 전부 실패 |
| 2 | 접근토큰 발급/갱신 | `src/api/auth.py:44` | 앱키·시크릿 → 토큰, 만료 5분 전 자동 재발급 로직은 이미 있음(`_is_token_valid`), HTTP 호출부만 채우면 됨 |
| 3 | 잔고/예수금 조회 | `src/api/account.py:42,45,48` | `get_balance_snapshot`, `get_positions`, `get_cash` — 09:00 자금 산정과 15:30 리포트가 이 값에 의존 |
| 4 | 현재가 조회 | `src/api/market_data.py:18` | `get_current_price` — 09:00 매수 수량 계산에 필수 |
| 5 | 주문 전송 | `src/api/order.py:52` | `_call_api` — 재시도(3회)·실패 시 REJECTED 반환 래핑은 구현됨 |
| 6 | 코스피200 구성종목 조회 | `src/data/collector.py:30` | KRX 정보데이터시스템 등 데이터 소스 확정 필요 |
| 7 | 실시간 시세 수신 | `src/api/websocket_client.py:19,25,28` | `subscribe`/`connect`/`disconnect` — **익절/손절 감시가 이 스트림에 의존**하므로 실매매 전 필수 |
| 8 | 일봉/분봉 조회 | `src/api/market_data.py:22` | 등락률·시가갭 계산(항목 9)의 전제 |
| 9 | 등락률·시가갭 계산 | `src/data/collector.py:69` | 현재 `0.0` 하드코딩 — LLM 프롬프트 품질에 직결 |
| 10 | 뉴스/공시 헤드라인 수집 | `src/data/collector.py:37` | 데이터 소스 미확정(10절 Open Question). 우선 빈 리스트로 운영 가능 |

### 11.3 선택/후순위 항목

| # | 항목 | 파일 | 비고 |
|---|---|---|---|
| 11 | 주문 정정/취소 | `src/api/order.py:35,38` | 현재 전략은 시장가 매수/매도만 사용해 당장은 미사용 |
| 12 | 스탑오더 등록 | `src/api/order.py:49` | 키움 미지원 추정(5.5-B) — 지원 확인 시에만 구현 |
| 13 | UI에서 엔진 실행/정지 | `src/ui/main_window.py:446,451` | 현재 UI는 설정 저장 전용, 실제 구동은 `scripts/run_trading.py` |
| 14 | 호가 조회 | `src/api/market_data.py:26` | 지정가 주문 도입 시 필요 |

### 11.4 모의투자 미지원에 따른 변경 (2026-07-27)

키움 모의투자를 사용할 수 없는 것으로 확인되어, "모의투자에서 검증 후 실전 전환" 전제가 성립하지 않는다. 아래와 같이 조정했다.

| 구분 | 변경 전 | 변경 후 |
|---|---|---|
| UI 매매 모드 | 모의/실전 선택, 기본 모의 | **모의 선택지 비활성화, 실전 고정** + 경고 문구 표시 |
| 실전 확인 절차 | 실전으로 전환할 때 1회 | **엔진을 시작할 때마다** `YES_I_UNDERSTAND` 입력 |
| `TRADE_MODE` 기본값 | `paper` | `live` (그 외 값은 `validate()`가 거부) |
| Phase 6 검증 | 모의투자 2~4주 | **소액 실계좌** 2~4주 |

> ⚠️ 이 변경으로 **첫 실행부터 실제 자금이 사용된다**. 반드시 소액으로 시작할 것. (8절 재무적 리스크 참고)

### 11.5 사용자 준비 사항 (코드 외)

- 키움증권 계좌 개설 및 Open API 사용신청 → 앱키/시크릿 발급 (Phase 0)
- Anthropic API 키 발급 → `.env`의 `ANTHROPIC_API_KEY`
- SMTP 발송 계정 준비 (Gmail 앱 비밀번호 등) → UI에서 입력

---

*본 문서는 초안이며, 키움증권 공식 API 문서 확인 및 개발 진행 과정에서 세부 요구사항이 조정될 수 있습니다.*
