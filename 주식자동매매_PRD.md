# PRD: 개인용 주식 자동매매 프로그램 (키움증권 API 기반)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.12 |
| 작성일 | 2026-07-26 (최초), 2026-07-27 (1호 전략·이메일 반영, 키움 API 연동 완료, 모의투자 복원), 2026-07-30 (전량 매도 완료 시 즉시 리포트, 매도 불가 종목 표시 반영, 11절 구현 상태 재스캔), 2026-08-03 (예수금 투입 비율·추천 종목 수 설정 가변화 설계 반영), 2026-08-04 (LLM 추천 프롬프트 v3 — 확신 미달 시 0개 허용을 폐지하고 상대 비교로 항상 설정 개수를 채우도록 변경 / LLM 추천 시각 UI 설정 가변화), 2026-08-05 (유니버스 중형주 확대·규모별 배분, 거래량 급증 배수 도입, 방향성 무신호 종목 제외, 추천 입력 로깅, 프롬프트 v4), 2026-08-12 (강제청산 15:20 → 15:15, 리포트 15:30 → 15:35, 미체결 매도가 남으면 청산 리포트 보류), 2026-08-20 (추천 유형을 낙폭 되돌림으로 제한, 프롬프트 v10; 매수 09:10 → 09:08, 미체결 취소 09:40 → 10:10; 추천·매수 시각을 `.env` 전용으로), 2026-08-21 (운영값 조정 — 추천 09:05 → 09:02, 매수 09:08 → 09:05; 익절·손절 폭을 UI 한 칸으로 통합), 2026-09-01 (매수 결과 메일 조기 발송을 도입했다가 실매매 오작동으로 철회, 단일 인스턴스 판정을 세션 너머로 확대, 리포트에 올해 누적과 달별 그래프 추가), 2026-09-02 (즉시 실행 버튼과 스케줄 실행을 하나의 실행 큐로 통합, 매수 결과 메일 조기 발송을 체결내역 조회 주문번호 대조 방식으로 재도입) |
| 대상 | 개인 투자자 (본인 단독 사용) |
| 매매 대상 | 국내 주식 현물 (KOSPI/KOSDAQ) |
| 1호 전략 | LLM 기반 급등 예상 대형주 매수 (당일+전일 지표 기준 추천·메일(UI 설정값, 기본 09:05) → 09:08 목표가 지정가 매수 → 10:10 미체결 취소·결과 메일 → 15:15 강제청산 → 15:35 성과 리포트 메일) |
| 상태 | Draft |

---

## 1. 개요

### 1.1 배경
반복적인 매매 판단(진입/청산 타이밍, 손절 실행)을 사람이 직접 하면 감정 개입과 실행 지연이 발생하기 쉽다. 키움증권이 제공하는 API를 통해 시세 조회·주문 실행을 프로그램으로 처리하고, 매매 전략은 별도 모듈로 자유롭게 교체·추가할 수 있는 개인용 자동매매 시스템을 구축한다.

### 1.2 문제 정의
- 사람이 장중 내내 화면을 보며 조건을 판단하기 어렵다 (기회 손실, 피로도).
- 손절/익절 규칙을 세워도 실행 시점에 감정적으로 어기는 경우가 많다.
- 특정 전략 하나에 종속되지 않고, 전략을 바꿔가며 검증하고 싶다.

### 1.3 제품 비전
"전략 로직을 갈아 끼울 수 있는 개인용 매매 엔진" — 인증/시세/주문/리스크관리/로깅 같은 공통 기반(프레임워크)을 먼저 견고하게 만들고, 그 위에 첫 번째 실전략으로 **LLM 기반 급등 예상 대형주 매수 전략**을 얹는다. 이후에도 전략은 계속 교체·추가 가능해야 한다.

---

## 2. 목표 및 성공 지표

### 2.1 목표
1. 키움증권 API로 로그인, 시세 조회, 주문(매수/매도/정정/취소), 잔고 조회를 안정적으로 자동화한다.
2. 매매 전략을 코드 변경 최소화로 교체할 수 있는 구조(전략 인터페이스)를 만든다.
3. 손실을 제한하는 리스크 관리 규칙(손절, 일일 손실 한도, 포지션 한도)을 시스템 레벨에서 강제한다.
4. 모의투자 환경에서 충분히 검증한 뒤에만 실전 계좌로 전환할 수 있는 안전장치를 둔다.
5. 매매 내역과 시스템 상태를 로그로 남기고, 이상 상황(주문 실패, 연결 끊김 등)을 알림으로 받는다.

### 2.2 성공 지표 (개인 프로젝트 기준)
- 모의투자 환경에서 1개월 이상 무중단(수동 개입 없이) 운영 성공
- 주문 성공률 99% 이상 (네트워크/API 오류로 인한 누락 제외 목표)
- 일일 손실 한도 초과 시 자동 매매 중단 100% 동작
- 장애 발생 시 알림 수신까지 1분 이내

### 2.3 비목표 (이번 범위에서 제외)
- 해외주식, 선물/옵션, 코인 등 타 자산군 지원
- 여러 명이 함께 쓰는 멀티유저/상용 서비스화
- 고빈도매매(HFT) 수준의 초저지연 최적화
- 1호 전략 외 추가 전략의 이번 범위 내 확정 (프레임워크만 확장 가능하게 열어두고, 2호 이후 전략은 추후 별도 결정)

---

## 3. 사용자 및 사용 시나리오

- **사용자**: 본인 1인 (개발자 = 사용자)
- **핵심 시나리오 (1호 전략 기준 하루 흐름)**
  1. 08:40 이전: 시스템이 자동으로 로그인 및 계좌 연동을 완료하고, 당일 예수금을 조회해둔다.
  2. **추천 시각 (`.env` 설정값, 기본 09:05)**: 전일 지표와 당일 지표(전일 종가 대비 등락, 거래량, 뉴스/공시 등)를 수집해 LLM에 전달하고, "급등이 예상되는 대형주 최대 N개"(추천 종목 수, UI 설정값, 기본 3개)를 추천받아 로그로 남긴다.
  3. **09:08 (개장 8분 후)**: 예수금 전체 중 설정된 투입 비율(UI 설정값, 기본 50%)을 매수가능금액으로 산정하고, 이를 추천 종목 수(UI 설정값, 기본 3개)로 나누어 각 종목을 **추천에 함께 실린 목표 매수가로 지정가 매수**한다. 이때도 리스크 관리 규칙(종목당 한도, 일일 손실 한도 등)을 통과해야 실제 주문이 나간다. 10:10까지 체결되지 않은 주문은 취소되고, 그 시점에 매수 결과 메일이 온다.
  4. 장중 특이사항(주문 거부, API 연결 끊김, 손실 한도 도달, LLM 추천 실패)은 즉시 알림으로 받는다.
  5. **15:35 (장 마감 후) 또는 그 전 전량 매도 완료 시**: 당일 매수/매도 결과와 이번 달 누적 수익률·수익금을 정리한 리포트 이메일을 자동으로 받는다. 보유 종목이 그 전에 모두 팔리고 **체결까지 확인되면**(익절/손절 또는 15:15 강제청산) 15:35를 기다리지 않고 그 즉시 리포트가 오고, 정기 15:35 리포트는 생략된다 — 하루에 두 번 오지 않는다. 매도하지 못한 종목이 있으면 사유와 함께 리포트에 실린다.
  6. 전략을 바꾸고 싶을 때는 전략 모듈 파일만 교체하면 나머지 시스템(주문/리스크/로깅)은 그대로 재사용된다.

---

## 4. 키움증권 API 연동 전략

키움증권은 현재 두 계열의 API를 제공한다. 세부 스펙(도메인, 엔드포인트, 인증 방식 등)은 버전에 따라 바뀔 수 있으므로 **구현 직전 반드시 키움증권 공식 개발자 문서로 최종 확인**해야 한다.

| 구분 | 키움 Open API+ (구, OCX/COM) | 키움 REST API (신규) |
|---|---|---|
| 통신 방식 | OCX 컨트롤 (COM 기반) | HTTP REST + WebSocket |
| 운영체제 | Windows 전용 (32bit 프로세스) | OS 무관 (서버/리눅스/맥 가능) |
| 인증 | 영웅문 로그인 창 | 앱키 + 시크릿 → 접근토큰 발급 |
| 강점 | 실시간 조건검색식 등 기능이 풍부, 자료·예제 많음 | 서버 상시 배포 용이, 24시간 무인 운영에 적합 |
| 약점 | 로그인 창이 뜬 PC를 계속 켜둬야 함, 서버 배포 어려움 | 일부 기능(조건검색식 등)이 구버전 대비 아직 제한적일 수 있음 |

**권고안**: 개인 PC를 상시 켜두고 운영할 계획이면 Open API+ 로 시작해도 무방하지만, 이 프로젝트는 "무인 자동화"가 핵심 목표이므로 **REST API + WebSocket 조합을 기본 방향으로 채택**한다. 단, 필요한 조건검색식 기능이 REST 쪽에서 아직 지원되지 않는다면 해당 기능만 Open API+로 보완하는 하이브리드 구성도 검토한다.

### 4.1 인증/계좌 연동 요구사항
- 앱키/시크릿, 접근토큰 등 민감정보는 코드에 하드코딩하지 않고 환경변수 또는 별도 설정파일(.gitignore 처리)로 관리
- 실전투자 계좌와 모의투자 계좌를 설정으로 명확히 분리 (실수로 실전 계좌에 테스트 주문이 나가는 사고 방지)
- 토큰 만료 시 자동 재발급 처리

---

## 5. 기능 요구사항

### 5.1 계좌/인증 관리
- 앱키/시크릿 기반 토큰 발급 및 자동 갱신
- **매매 모드: 모의(paper) / 실전(live) 전환** — 키움이 모의투자 서버(`mockapi.kiwoom.com`)를 제공하므로 두 모드를 모두 지원한다. 기본값은 모의투자이며, 저장된 값이 실전이어도 UI는 항상 모의로 시작한다
- **실전 전환 및 실전 엔진 시작 시 확인 절차 필수** — 확인 문구(`YES_I_UNDERSTAND`) 입력을 요구한다
- 연결 상태 헬스체크 (주기적 ping, 끊김 감지 시 재연결 시도)

### 5.2 시세 조회
- 현재가, 호가, 체결 정보 조회
- 관심종목 등록 및 실시간 시세 수신(WebSocket)
- 과거 일봉/분봉 데이터 조회 (전략 개발 및 추후 백테스팅용)

### 5.3 주문 관리
- 매수/매도 주문 실행 (지정가/시장가)
- 주문 정정/취소
- 주문 체결 여부 실시간 확인
- 주문 실패 시 사유 로깅 및 알림

### 5.4 잔고/보유종목 조회
- 예수금, 보유종목, 평가손익 조회
- 장 시작 시 잔고 스냅샷 저장 (일일 리포트 기준값)

### 5.5 전략 프레임워크 및 1호 전략

**(A) 프레임워크 공통 요구사항**
- 공통 전략 인터페이스 정의: 시세/데이터를 입력받아 "매수/매도/보유" 신호(대상 종목, 수량 또는 금액 포함)를 반환하는 표준 함수 형태
- 전략 모듈은 독립 파일/클래스로 작성하고, 설정에서 어떤 전략을 쓸지만 지정하면 나머지 시스템(주문 실행, 리스크 관리)은 그대로 동작
- 시간 기반(예: 매일 09:05, 09:08)으로 전략을 실행하는 스케줄러 필요 — 실시간 시세 이벤트 기반 전략뿐 아니라 정해진 시각에 실행되는 전략도 지원해야 함
- 추후 전략 추가 시 시스템 코드 수정 없이 전략 파일 추가만으로 확장 가능해야 함

**(B) 1호 전략: LLM 기반 급등 예상 대형주 매수**

| 단계 | 시각 | 내용 |
|---|---|---|
| 1. 데이터 수집 | 추천 시각 (`.env` 설정값, 기본 09:05) | 대형주 유니버스 전 종목의 **일봉(`ka10086`)**을 조회해 전일 지표(전일 종가·고가·저가·등락률·거래량, 직전 평균 대비 거래량 급증 배수, 전일 변동폭)를 산출하고, 급증 배수 상위 40종목에 **현재가(`ka10001`)**를 조회해 당일 등락률을 계산한다. **DART 공시**로 악재 공시 종목을, **당일 하락 출발한 종목**을 후보에서 제외 — 아래 "전일 데이터 기준 수집"·"당일 지표 병행 수집"·"공시 수집과 악재 배제" 참고 |
| 2. LLM 추천 요청 | 추천 시각 (수집 직후) | 추려진 후보를 프롬프트로 구성하여 LLM에 전달, **낙폭을 되돌리는 구간의 종목** N개(추천 종목 수, UI 설정값, 기본 3개)와 **종목별 목표 매수가·목표 매도가**를 요청 — 아래 "추천 유형 제한" 참고 |
| 3. 추천 결과 알림 | 추천 시각~09:08 | LLM 응답을 구조화된 형식(JSON — 종목코드, 종목명, 목표 매수가, 목표 매도가, 추천 사유)으로 파싱하고 **후보 목록에 없는 종목을 제외**한 뒤, 그 밖의 **자동 유효성 검증(거래정지·상장폐지 등) 없이** 해당 내용을 이메일로 사용자에게 발송 |
| 4. 자금 산정 | 09:08 | 매수가능금액 = 예수금 전체 × 투입 비율(UI 설정값, 기본 50%) → 종목당 매수금액 = 매수가능금액 ÷ 추천 종목 수(UI 설정값, 기본 3개) |
| 5. 매수 실행 | 09:08 (개장 8분 후) | 현재가가 **허용 밴드** 안이면 그 밴드 상단(= 목표 매수가 × (1 + 상승 허용치))을 **지정가**로 매수 주문. 수량 = 종목당 매수금액 ÷ 지정가. 현재가가 목표가의 허용 상한(기본 +2%)을 넘으면 갭 상승으로, **추천 시점(09:05) 가격**의 하한(기본 -1%)을 밑돌면 갭 하락으로 보고 건너뛴다 |
| 6. 미체결 취소 · 결과 알림 | 10:10 (+ 15:15 재확인) | 목표가에 닿지 않아 미체결로 남은 매수 주문을 취소하고, 체결이 확정된 상태로 매수 실행 결과 이메일을 발송. 엔진을 10:10 이후에 켠 날을 대비해 15:15 강제청산 직전에 한 번 더 거둔다. **접수한 매수 주문이 전부 전량 체결되면 10:10을 기다리지 않고 그 시점에 결과 메일이 나간다** — 판정은 당일 체결내역 조회의 주문번호 대조이며, 한 건이라도 확인되지 않으면 종전대로 10:10이 마무리한다 (아래 "매수 결과 메일 조기 발송" 참고) |
| 7. 매도 감시 | 매수 체결 시점부터 | 보유 종목 전체를 실시간 시세로 감시하며 **합산 순손익률**이 손절선(설정값)에 닿으면 **보유 종목 전량**을 시장가 매도 (5.6절 `check_portfolio_exit`). **기본은 손절 + 종목별 단순익절이며**(확정 2026-08-13, 10절 "단순익절 기본 적용") 합산 퍼센트 익절만 기본 해제다 — 익절 두 방식은 배타적이라 하나만 켜진다. 세 체크박스는 `.env`에 저장하지 않아 **프로그램을 다시 시작해야 기본값으로 돌아간다** (5.5-B "익절/손절 적용 여부") |
| 8. 강제청산 | 15:15 | 익절/손절로 정리되지 않고 남은 보유 종목을 시장가로 전량 매도 (당일 매도 원칙). **15:20에 시작되는 장마감 동시호가를 피해 15:15에 낸다 (확정 2026-08-12)** — 동시호가에 들어간 시장가 주문은 15:30 종가에야 체결되어, 그날 리포트가 미정산 상태로 나갔다 (10절 "청산 시각과 리포트 정산") |

- **예시 (기본값 기준)**: 예수금 1,000만 원, 투입 비율 50%, 추천 종목 수 3개 → 매수가능금액 500만 원 → 종목당 약 166.7만 원씩 3종목 매수
- **자금 배분 설정 (UI에서 조정 가능, 확정 2026-08-03)**: 예수금 투입 비율(10~100%, 10% 단위, 기본 50%)과 추천 종목 수(1~10개, 1개 단위, 기본 3개)를 UI 콤보박스로 조정할 수 있다. 두 값 모두 `.env`(`INVESTABLE_RATIO_PERCENT`, `TARGET_STOCK_COUNT`)에 저장되며, LLM 프롬프트의 추천 개수 지시문도 이 값을 그대로 따른다
- **유니버스: 대형주만 (확정 2026-08-06)**: 유니버스는 `ka10099`의 **대형주(약 98종목)**뿐이다.
  2026-08-05에 중형주(192종목)까지 넓히고 추천 N종목 중 1종목을 중형주에 배분했으나, 하루 만에
  되돌린다 — 중형주는 대형주보다 얇아 지정가 매수(아래)의 체결·슬리피지 조건이 나빠진다.
  `recommender.cap_quota`와 `DailyStockData.cap_tier`는 함께 폐지한다.
  - **소형주(512종목)는 제외한다** — 유동성이 얇아 매수 슬리피지가 크다. 이 때문에 소형주로 분류되는 종목(예: 대원전선)은 추천되지 않는다
  - **코스닥은 대상이 아니다** — `mrkt_tp="0"`(코스피)만 조회하므로 코스닥 종목(예: 대한광통신)은 유니버스에 없다
- **전일 데이터 기준 수집 (확정 2026-08-06, 2026-08-14에 당일 지표가 추가됨)**: 후보 선정 지표의 **뼈대는
  전일 일봉**이다. 2026-08-06~08-13에는 추천이 장 전(08:45)이라 당일 지표가 존재조차 하지 않아 이것이
  유일한 근거였고(10절 "장 전 당일 지표 부재"), 2026-08-14에 추천을 09:05로 옮기면서 당일 지표가
  여기에 **더해졌다**(아래 "당일 지표 병행 수집"). 전일 지표의 산출 방식 자체는 그대로다.
  - 유니버스 전 종목에 일봉(`ka10086`)을 **한 번씩만** 조회한다. 한 호출이 최근 20거래일을 돌려주므로
    전일 종가·고가·저가·등락률(`flu_rt`)·거래량과 직전 평균 거래량이 모두 여기서 나온다
  - **등락률은 `flu_rt`가 비면 직전 거래일 종가 대비로 직접 계산한다 (확정 2026-08-07)**.
    이 필드가 실리지 않으면 전 종목 등락률이 0이 되어 아래 후보 정렬의 '전일 상승' 구분이
    사라지고, 프롬프트에도 "전일 등락률 +0.00%"만 나열돼 LLM이 쓸 근거가 없어진다 —
    직전 버전이 `has_direction`으로 막던 실패 모드와 같다. 비교할 직전 봉이 없으면 0으로 둔다
  - 응답에는 **당일 봉도 포함**되므로(`date` 필드가 당일인 행) 이를 제외하고 계산한다 — 어느 시각에
    돌려도 전일 지표가 같은 값이 나오게 하기 위함이다. **추천이 장중(09:05)으로 옮겨진 뒤로는 이
    제외가 특히 중요하다** (2026-08-14): 진행 중인 당일 봉이 섞이면 전일 종가·고가·저가·급증 배수가
    모두 흔들리고, 당일 지표는 아래 "당일 지표 병행 수집"이 현재가로 따로 계산하므로 여기서 섞을
    이유도 없다
  - **거래량 급증 배수 = 전일 거래량 ÷ 그 이전 거래일들의 평균 거래량**. 전일 자신을 평균에 넣으면
    배수가 희석되므로 뺀다
  - **최근 가격대도 같은 응답에서 뽑는다 (확정 2026-08-07)**: 당일을 제외한 최근 거래일들의
    **최고가·최저가·종가 이동평균** 셋. 전일 하루만 보면 그 가격이 최근 범위에서 비싼 쪽인지 싼 쪽인지
    알 수 없어, 목표 매수가가 전일 종가 ±5% 안의 감으로만 정해진다. 이미 받아 둔 일봉에서 계산하므로
    **API 호출은 늘지 않는다**. 0 이하인 값(조회가 비어 온 봉)은 계산에서 빼고, 셋 중 하나라도 산출하지
    못하면 프롬프트에서 그 항목을 통째로 생략한다 — "최근 저가 0원"을 적으면 LLM이 그 0을 근거로 삼는다
  - **전일 변동폭 = (전일 고가 − 전일 저가) ÷ 전일 종가 (확정 2026-08-14)**. 후보를 거르는 데는
    **쓰지 않고** 프롬프트에만 싣는다 — 이유는 아래 "당일 지표 병행 수집"의 변동폭 항목 참고
  - 후보는 **상위 25종목**: 전일 상승 종목을 급증 배수 내림차순으로 채우고, 25개에 못 미치면 나머지를
    하락 종목 중 급증 배수 순으로 채운다 — 전 종목이 하락한 날에도 후보가 비어 추천이 스킵되지 않게 한다.
    2026-08-14부터 이 정렬 앞에 당일 지표 필터가 한 단계 붙는다 (아래 "당일 지표 병행 수집")
  - 호출 예산: 98회 × 0.2초 ≈ **30초** (종전 331회/69초). 당일 지표 조회 40회가 더해져 총 **약 38초**다
  - **LLM에 넘긴 후보 전체와 프롬프트 원문을 로그에 남긴다** — 추천이 타당했는지 사후에 되짚을 유일한 근거다 (확정 2026-08-05)
- **당일 지표 병행 수집 (확정 2026-08-14)**: 추천이 09:05로 옮겨지면서 **당일 지표를 다시 쓴다**.
  2026-08-06에 당일 지표 경로를 걷어낸 것은 지표 자체가 무용해서가 아니라 **장 전에는 값이 오지
  않아서**였고(10절 "장 전 당일 지표 부재"), 개장 후에는 그 전제가 성립하지 않는다.
  - **키움이 주는 등락률·시가갭 필드를 받지 않고 현재가에서 직접 계산한다**:
    `당일 등락률 = (현재가 − 전일 종가) ÷ 전일 종가`. 2026-08-06에 무너진 것은 `flu_rt`·시가갭
    **필드**였지 가격 자체가 아니었다. 전일 종가는 일봉에서 이미 확보했고 현재가는 실시간 청산
    감시가 매초 쓰는 값이라, 이 계산식은 특정 필드가 비어 오는 것에 무너지지 않는다 — `flu_rt`가
    비면 직접 계산하기로 한 2026-08-07 결정(위 "전일 데이터 기준 수집")과 같은 패턴이다
  - **유니버스 98종목 전부가 아니라 전일 급증 배수 상위 40종목(`PRESCREEN_SIZE`)에만** 현재가
    (`ka10001`)를 조회한다. 최종 후보가 25종목이므로 40이면 당일 필터로 탈락한 자리를 채우고도
    남는다. 98종목 전부에 돌리면 20초가 더 드는데 그중 3분의 2는 어차피 후보에 못 든다
  - **당일 하락 출발한 종목을 후보에서 제외한다**: `당일 등락률 < −GAP_DOWN_TOLERANCE_PERCENT`
    (기본 1% → −1% 미만이면 제외)이면 뺀다. **−1% 이상이면 통과**이므로 보합·소폭 하락은 남는다.
    새 설정을 만들지 않고 갭 하락 허용치를 재사용하는 것은 판정 내용이 같기 때문이다 — "전일
    강세가 오늘 이어진다"는 전략 전제가 깨졌는지를 본다. 값이 0이면 이 필터도 함께 꺼진다.
    **배제는 공시 배제와 마찬가지로 상위 25종목을 추리기 전에 적용한다** — 빈자리를 다음 후보가 채운다
  - **필터 결과 후보가 25종목에 못 미쳐도 그대로 진행한다.** 40종목 중 16종목 넘게 탈락한 날이
    그렇다 — 후보 수를 채우려고 방금 제외한 종목을 도로 넣지 않는다. 아래 하한선만 지킨다
  - **전일 변동폭은 거르는 데 쓰지 않는다.** 도입 검토 당시 "변동폭이 큰 종목은 위험하다"는 가설로
    하드 컷·점수식 페널티를 함께 검토했으나, 2026-08-14 후보 25종목의 실측 분포가 이를 지지하지
    않았다: 그날 LLM이 고른 3종목이 변동폭 상위 8위 안에 모두 들었고 그중 둘은 익절로 끝났다
    (한국항공우주 8.40% → +0.33%, 미래에셋증권 7.49% → +0.40%, JB금융지주 6.98% → −2.79%).
    6% 하드 컷이면 손실 1건과 함께 **이익 2건도 잘려나간다**. 변동폭이 위험 신호가 아니라 급등
    전략이 애초에 노리는 성질일 수 있어, **프롬프트에 실어 LLM이 감안하게만 하고 로그에 쌓는다** —
    표본이 하루치뿐이므로, `trades.exit_reason`과 대조할 데이터가 모인 뒤에 하드 컷 여부를 판단한다
    (2026-08-12에 익절 방식을 27건 대조로 판단한 것과 같은 순서)
  - **조회 실패는 종목을 떨어뜨리지 않는다**: 현재가 조회에 실패한 종목은 당일 필터를 **통과**시키고
    프롬프트에 "당일 지표 없음"으로 적는다. 일시적 조회 실패로 멀쩡한 종목을 잃지 않기 위함이며,
    갭 판정의 "조회 실패면 건너뛰고 진행"과 같은 원칙이다
  - **거를 후보 자체가 없으면(수집이 통째로 실패한 날) 필터는 조용히 지나간다** — 아래 하한선
    알림을 내지 않는다. 여기서 알리면 한 장애에 메일이 두 통 나가고(뒤이어 "당일 데이터 수집
    실패" 알림이 따라온다), 먼저 오는 쪽이 원인을 필터로 잘못 지목한다. 공시 배제가 같은 이유로
    두고 있는 가드와 같다
  - **필터 통과 종목이 `MIN_CANDIDATES_AFTER_FILTER`(5종목) 미만이면 필터를 통째로 걷고 전일 기준으로
    25종목을 채운 뒤 운영 알림을 보낸다.** 지수가 통째로 갭 하락한 날에 후보가 비어 추천이 스킵되는
    것을 막는다 — 2026-08-06에 필터 하나로 후보 0종목 → 매수 0건이 된 실패를 되풀이하지 않기 위한
    하한선이다. 5는 추천 종목 수 기본값(3)보다 여유를 둔 값이며 코드 상수다. **필터를 걷는 날은
    당일 하락 종목이 후보에 섞이므로**, 그날의 방어는 09:08 주문 직전 갭 판정 하나만 남는다
- **공시 수집과 악재 배제 (확정 2026-08-12)**: 프롬프트의 `뉴스/공시` 항목을 **DART 전자공시**로 채우고,
  악재 공시가 뜬 종목을 후보에서 제외한다. 키움 API는 뉴스를 주지 않는다(10절 확정 항목). 구현은
  `src/data/disclosure.py`의 `DisclosureClient`이며 종전 스텁 `NewsClient`를 대체한다.
  - **종목당 조회가 아니라 하루치 일괄 조회다**: `https://opendart.fss.or.kr/api/list.json`에
    `corp_cls=Y`(유가증권)·`bgn_de`(직전 거래일)·`end_de`(당일)·`page_count=100`을 넘겨 페이지를 돌며
    코스피 공시를 통째로 받고, 응답의 `stock_code`로 유니버스 종목에 붙인다. 응답에 종목코드가 들어
    있어 **고유번호(`corp_code`) 매핑 파일(`corpCode.xml`)이 필요 없다** — 매핑이 걸림돌이라고 보고
    2026-08-05에 미뤘으나 실제로는 없어도 됐다. 호출 수는 페이지 수만큼이라 종목 수와 무관하다
  - **기간은 직전 거래일 + 당일이다**: `rcept_dt`가 날짜 단위(시각 없음)라 "전일 15:30 이후"를 시각으로
    골라낼 수 없다. 전일 일봉 기준 수집과 기간을 맞춘다
  - **배제는 후보 25종목을 추리기 전에 적용한다** — 뒤에 적용하면 후보가 그만큼 줄지만, 앞에 적용하면
    빈자리를 다음 후보가 채운다. 조회 비용은 어느 쪽이든 같다
  - **배제 대상은 좁게 시작한다**: 유상증자결정, 전환사채·신주인수권부사채·교환사채 발행결정,
    횡령·배임, 회계처리기준 위반, 불성실공시법인 지정. `report_nm` 부분 문자열로 판정하며, 배제된
    종목은 공시명과 함께 로그에 남긴다
    - **소송등의제기는 배제하지 않는다** — 대형주에는 일상적으로 뜨고 주가 영향이 없는 건이 많아,
      멀쩡한 종목을 걸러낼 위험이 배제 이득보다 크다. 헤드라인으로만 넘겨 LLM이 감안하게 둔다
  - **배제되지 않은 공시 제목은 종목당 최신 3건까지** `DailyStockData.headlines`에 담아 프롬프트의
    `뉴스/공시:` 자리에 그대로 싣는다. LLM에는 배제 판정을 시키지 않고 정보만 준다
  - **조회 실패나 키 미설정이면 헤드라인 없이, 배제 없이 진행한다** — 공시 때문에 그날 추천 전체가
    멈추지 않게 한다. 키가 없으면 로그만 남기고, 키가 있는데 조회가 실패하면 운영 알림을 보낸다
  - **효과 기대치는 낮게 잡는다**: 대상이 코스피 대형주 98종목이라 배제 대상 공시가 뜨는 날 자체가
    드물다. 대부분의 날은 배제 0종목이고, 실질 변화는 프롬프트에 공시 제목이 실리는 쪽이다
  - `.env`의 `DART_API_KEY`에 두고, **UI "계좌 / API 설정"의 'DART Key' 칸에서 수정한다 (2026-08-25에 노출로 변경)**.
    종전에는 "SMTP 비밀번호와 같이 한 번 넣고 바꾸지 않는 값"이라 UI에 올리지 않았으나, 키가 없거나 잘못돼도
    추천이 조용히 진행되는 값이라(공시 없이 통과) 화면에서 채워졌는지 확인할 수 있어야 한다. App Secret과
    같이 가려서 표시하며, 바꾸면 다른 설정과 마찬가지로 엔진이 재시작된다.
    키는 opendart.fss.or.kr에서 무료 발급이며 하루 20,000건 한도라 이 사용량에는 여유가 있다
- **갭 허용치 설정 (UI에서 조정 가능, 확정 2026-08-07, 역할 변경 2026-08-26)**: 09:08 매수 직전
  현재가가 목표 매수가보다 얼마나 높으면 그 종목을 포기할지를 UI "리스크 관리" 그룹에서 %로
  조정한다(기본 2%). `.env`(`BUY_PRICE_TOLERANCE_PERCENT`)에 저장되며, 다른 퍼센트 설정과 같이
  `_percent` 필드와 `_ratio` 프로퍼티 쌍으로 둔다. 0으로 두면 목표가를 조금이라도 넘는 순간
  건너뛴다.
  - **이 값은 참여 판정선인 동시에 지정가 그 자체다 (2026-08-26).** 범위 지정으로 바꾸면서
    주문가가 `목표가 × (1 + 이 값)`이 됐으므로, **이 값이 곧 최악의 체결가**다. 넉넉히 잡으면
    참여 기회가 늘어나는 대신 목표가보다 그만큼 비싸게 체결될 수 있다 — 손절선보다 크게 잡으면
    진입 직후 손절 구간에서 시작한다. 상세는 아래 "주문 방식" 참고
- **갭 하락 허용치 설정 (UI에서 조정 가능, 확정 2026-08-11, 기준값 변경 2026-08-14)**: 얼마나 낮으면
  그 종목을 포기할지를 같은 "리스크 관리" 그룹에서 %로 조정한다(기본 1%).
  `.env`(`GAP_DOWN_TOLERANCE_PERCENT`)에 저장되며 위와 같은 `_percent`/`_ratio` 쌍이다.
  **0으로 두면 두 판정이 모두 꺼진다** — 갭 상승 쪽(0 = 가장 엄격)과 반대 규약인데, 임계값의 근거가
  아직 약해 언제든 꺼둘 수 있어야 하기 때문이다.
  - **이 한 설정이 두 지점에서 쓰인다 (확정 2026-08-14)**: 09:05 후보 선정에서는 **전일 종가** 대비
    당일 등락률에(위 "당일 지표 병행 수집"), 09:08 주문 직전에는 **추천 시점 가격** 대비 현재가에
    적용된다. 두 판정의 역할이 다르므로 상세는 아래 "주문 방식" 참고
- **추천·매수 시각 설정 (`.env` 전용, 확정 2026-08-04, UI 조작 제거·매수 시각 추가 2026-08-20)**:
  LLM 추천과 지정가 매수를 트리거할 시각을 `.env`의 `RECOMMEND_TIME`·`BUY_TIME`(`"HH:MM"` 문자열,
  기본 **09:05**·**09:08**)로 둔다. `Settings.recommend_time`/`buy_time`이 `datetime.time`으로 환산해
  스케줄러에 넘긴다. 값이 깨져 있으면 기본값으로 돌리고 경고를 남긴다 — `.env` 오타 한 줄로 그날
  매매 전체가 멈추지 않도록 한 선택이다.
  - **UI는 값을 보여주기만 한다 (확정 2026-08-20)**: 콤보박스를 없애고 "매매 시각" 라벨에 현재
    값(추천 → 매수, 미체결 취소·청산)과 "`.env`에서 바꾸고 다시 시작해야 반영됨"을 표시한다.
    고를 수 있게 두면 개장 전 추천이나 추천보다 앞선 매수처럼 **성립하지 않는 조합이 UI에서
    만들어진다**. 종전 콤보박스 범위(09:00~09:20 → 09:00~09:05)가 계속 좁아진 것도 같은 이유였다
  - **두 시각의 관계는 `Settings.validate()`가 막는다**: 추천은 개장(09:00) 이후여야 하고, 매수는
    추천보다 **3분 이상**(`MIN_RECOMMEND_TO_BUY_MINUTES`) 뒤여야 한다. 값이 깨진 경우와 달리 여기서는
    기본값으로 넘어가지 않고 **엔진 시작을 막는다** — 순서가 뒤집히면 추천이 없는 채로 매수가 돌아
    그날 매매가 조용히 통째로 비기 때문이다. 3분의 근거는 10절 "매수 타이밍 조정" 참고
  - **종전 범위는 08:40~08:55(기본 08:45)였다.** 장 전 추천이라 당일 지표를 볼 수 없었고, 수집이 전일
    일봉 기준이라 그 범위 안에서는 시각을 바꿔도 결과가 같았다 — 의미가 "09:00 전 여유 시간"뿐이었다
  - **새 범위의 하한 09:00은 개장 시각이다.** 그보다 앞서면 당일 지표가 오지 않아 이동 자체가 무의미해진다.
    상한 09:20은 매수 시각(추천 +5분)이 개장 30분 안에 들어오게 하는 선이다. 값이 이 범위를 벗어나면
    당일 지표 수집이 성립하지 않으므로, 콤보박스 밖의 값(`.env` 직접 편집)도 같은 폴백으로 막는다
- **주문 방식: 허용 밴드 상단 지정가 (확정 2026-08-06, 가격 지정 → 범위 지정 2026-08-26)**:
  09:08에 **지정가** 매수를 낸다. 시장가는 쓰지 않는다 — 종전의 시장가 매수는 개장 직후 고가를
  그대로 따라가 매수 직후 손절되는 일이 잦았다.
  - **주문 조건과 주문가는 하나의 밴드로 정의된다 (2026-08-26)**:

    ```
    주문 조건: 추천시점가 × (1 − 갭 하락 허용치) ≤ 현재가 ≤ 목표가 × (1 + 갭 상승 허용치)
    주문가   : 현재가 ≤ 목표가 → 목표가 (종전과 동일)
               현재가 > 목표가 → 목표가 × (1 + 갭 상승 허용치), 호가 단위 내림
    ```

    현재가가 밴드 안이면 **반드시 매수한다**. 목표 매수가는 밴드의 기준점일 뿐 주문가가 아니다.
    지정가 매수는 그 가격 **이하**의 매도호가에 체결되므로, 상단에 걸어두면 실제 체결은 현재가
    부근에서 이뤄지고 상단을 넘겨 지불하는 일은 없다. 두 허용치의 역할은 서로 다르다 — 상승
    허용치는 **참여 판정선이자 지정가**이고, 하락 허용치는 **참여 판정선일 뿐**이다
  - **왜 바꿨나 (2026-08-26)**: 종전에는 밴드 안이라고 판정해 놓고 주문은 **목표가 한 점**에
    걸었다. 현재가가 목표가보다 조금이라도 높으면 지정가가 시장가 아래에 놓여, 되밀리지 않는 한
    영원히 미체결이다 — "참여해도 좋다"는 판정과 실제로 낼 수 있는 주문이 어긋나 있었다.
    2026-08-26에 015760 한국전력이 정확히 여기 걸렸다: 목표가 33,200원, 09:05 현재가 약
    33,400원, 허용 상한 34,196원(허용치 3%)으로 **밴드 안이라 주문까지 접수됐지만** 지정가
    33,200원이 종일 체결되지 않아 10:10에 취소, 그날 투입 0원으로 끝났다. 밴드 상단
    34,150원(호가 내림)에 걸었다면 33,400원 부근에 즉시 체결됐을 상황이다
  - **밴드의 위쪽 절반에서만 올린다.** 현재가가 목표가 이하면 목표가 지정가가 이미 시장가 위에
    있어 즉시 체결되므로, 상단으로 올리면 체결에는 아무 차이가 없고 수량만 줄어든다. 그래서
    이 경우 주문가는 종전 그대로 목표가다
  - **수량 = 종목당 매수금액 ÷ 주문가.** 현재가로 산정하지 않는다 — 체결가는 주문가 이하이므로
    이 기준이면 배정액을 넘겨 쓰는 일이 없다
  - **현재가를 조회하지 못하면 종전대로 목표가 지정가로 낸다.** 밴드를 확인할 수 없는 상태에서
    상단에 거는 것은 근거 없이 비싸게 사는 것이라, 이 경우에만 가격 지정으로 되돌아간다
  - **목표 매수가의 ±5% 가드레일 기준은 당일 현재가다 (변경 2026-08-14)**: `normalize_target_price`가
    자릿수를 틀린 값만 막는 가드레일인데, 종전 기준인 전일 종가를 그대로 두면 갭이 큰 날 LLM이 **당일
    가격을 보고 낸 정상적인 목표가**가 잘려나간다. 프롬프트가 당일 현재가 기준으로 목표가를 요구하므로
    (아래 프롬프트 v9) 가드레일도 같은 기준이어야 한다. 당일 현재가를 모르면 전일 종가로 돌아간다
  - **갭 상승한 종목은 건너뛴다 (확정 2026-08-07, 지정가 겸용 2026-08-26)**: 09:08 주문 직전에
    현재가를 조회해, 목표 매수가의 **`BUY_PRICE_TOLERANCE_PERCENT`(기본 2%, UI 조정)** 상한을
    넘으면 그날 그 종목은 매수하지 않는다. 목표가는 추천 시점 가격을 근거로 잡은 값이라, 그보다
    크게 높으면 그 전제가 이미 깨진 것이다.
    - **이 상한이 곧 주문 지정가다 (2026-08-26)**. 따라서 상한을 넘겨 체결되는 일은 없지만,
      **목표가보다는 이 허용치만큼 비싸게 체결될 수 있다** — 종전의 "목표가보다 비싸게 체결되는
      일 자체가 없다"는 성질은 범위 지정으로 바뀌면서 사라졌다. 허용치를 손절선보다 크게 잡으면
      진입 직후 손절 구간에서 시작하므로, 이 값은 참여 기회와 체결가를 함께 정한다
    - 시세 조회에 실패하면 **판정을 건너뛰고 목표가 지정가로 매수한다**. 일시적 조회 실패로 당일
      진입 전체를 포기하는 쪽이 손해이고, 밴드를 모르는 채로 상단에 걸 수는 없기 때문이다
    - 건너뛴 종목은 매수 결과 메일의 '매수하지 못한 종목'에 사유(현재가·목표가·허용치)와 함께 실린다
  - **갭 하락한 종목도 건너뛴다 (확정 2026-08-11, 기준값 변경 2026-08-14)**: 09:08 현재가가
    **추천 시점(09:05) 가격**의 **`GAP_DOWN_TOLERANCE_PERCENT`(기본 1%, UI 조정, 0이면 끔)** 하한을
    밑돌면 그날 그 종목은 매수하지 않는다. 갭 상승 판정과 짝을 이루는 반대쪽 가드다
    - **기준이 목표가가 아닌 이유**: 목표 매수가 자체가 눌림을 노려 기준가보다 낮게 잡히는 값이라
      (2026-08-11 추천 3종목 모두 전일 종가 -1.1~-1.5%), 목표가를 기준으로 하한을 두면 "얼마나 낮게
      출발했는지"를 잡지 못한다. 실제로 2026-08-11 포스코퓨처엠은 목표가 163,500원 대비 시가가
      -0.12%(163,300원)에 불과해 목표가 기준 ±2% 밴드에는 걸리지 않지만, 전일 종가 165,500원
      대비로는 -1.33%다
    - **기준이 전일 종가에서 추천 시점 가격으로 바뀐 이유 (2026-08-14)**: 전일 종가 대비 판정은
      이제 **09:05 후보 선정 단계가 맡는다**(위 "당일 지표 병행 수집"). 주문 직전에 같은 판정을
      되풀이하면 4분 사이에 새로 하한을 뚫은 종목만 잡히고, 정작 **추천 이후 무너진 종목**은
      전일 종가 하한 위에 있는 한 그대로 통과한다. 두 지점이 각각 "전일 대비 약세"와 "추천 이후
      급변"을 맡도록 나눈 것이다
    - **판정 기준가는 추천 시각에 수집한 현재가를 그대로 실어 나른다** — `StockRecommendation`에
      `recommend_price`로 담으며, 종전에 전일 종가를 실어 나르던 방식을 그대로 물려받았다.
      09:08에 일봉이나 기준가를 다시 조회하지 않는다. 0은 '모름'이며 그 경우 갭 하락 판정을 건너뛴다
    - **이 판정이 없으면 매수 바구니에 갭 하락 종목만 남는다.** 지정가 매수는 원리상 가격이
      목표가까지 **내려온** 종목만 잡는다 — 예상대로 오르는 종목은 미체결로 빠지고 빠지는 종목만
      100% 체결되는 역선택이다. 실제로 2026-08-06~08-11 체결 6건이 **예외 없이** 전일 종가보다
      낮은 가격에 개장 직후 전량 체결됐다(전일 대비 -0.53%~-1.88%). 갭 상승 판정만으로는 이걸
      막지 못한다 — 갭 상승한 종목은 어차피 지정가라 체결되지 않기 때문이다
    - **범위 지정으로 바뀌면서 이 역선택 자체가 약해졌다 (2026-08-26)**: 밴드 안에서 오른
      종목도 이제 상단 지정가로 체결되므로, "빠지는 종목만 100% 체결"되는 편향이 밴드 폭만큼
      완화된다. 다만 밴드 **아래로** 무너진 종목을 거르는 역할은 그대로여서 이 판정은 유지한다 —
      역선택이 줄었을 뿐 없어진 것은 아니다
    - **임계값 1%는 잠정값이다.** 체결 표본이 6건뿐이라 최적값을 정할 근거가 없다. 과거 6건에
      -1%를 대입하면 2026-08-11 손실 1건(-24,318원)은 막지만 2026-08-10 이익 2건(+964원,
      +1,508원)도 함께 막힌다. 그래서 UI로 조정 가능하게 두고 0이면 꺼지게 했다
    - 시세 조회 실패, 또는 기준가를 모르는 종목(`recommend_price` 0 이하)은 **판정을 건너뛰고
      그대로 매수한다** — 갭 상승 판정과 같은 원칙이다
    - 기준가는 수집 단계에서 잡은 당일 현재가를 `StockRecommendation.recommend_price` →
      `BuyPlan`으로 실어 09:08까지 넘긴다. 09:08에 기준가를 다시 조회하지 않는다(API 호출을 늘리지
      않는다)
    - **전일 종가는 09:08까지 실어 나르지 않는다 (2026-08-14).** 종전에는 갭 하락 판정의
      기준값이라 `StockRecommendation.prev_close` → `BuyPlan.prev_close`로 넘겼으나, 기준이
      추천 시점 가격으로 바뀌면서 읽는 곳이 없어졌다. 목표가 가드레일의 폴백도 추천 산출
      시점에 `DailyStockData`를 직접 보고 끝나므로 넘길 이유가 없다 — 값이 죽은 채 남아
      있으면 갭 판정에 기준이 둘인 것처럼 읽힌다
  - 목표 매수가는 **호가 단위로 내림**해 보정한다 — 호가 단위에 맞지 않는 가격은 주문이 거부된다.
    내림(더 낮은 가격)으로 맞추는 것은 매수에 불리하지 않은 방향이기 때문이다
  - 목표 매수가가 **전일 종가 대비 ±5%를 벗어나면 그 경계로 자른다** — LLM이 자릿수를 틀리는 것을
    막는 가드레일이며, 정상 범위의 판단에는 개입하지 않는다
  - **후보 목록에 없는 종목은 주문 경로에 들어가기 전에 제외한다 (확정 2026-08-07)**. 프롬프트
    "절대 규칙 1"로만 막혀 있던 것을 코드로도 강제한다 — 후보 밖 종목은 전일 종가를 알 수 없어
    위 ±5% 가드레일이 통째로 꺼지고, 자릿수를 틀린 값이 그대로 주문가가 된다. 제외 후 남은
    종목이 추천 종목 수보다 적으면 모자란 몫은 현금으로 남기고, 전부 제외되면 그날 매수를
    스킵한다 (기존 "추천이 모자라면 그만큼 매수하지 않는다" 정책과 같다)
  - **목표 매도가는 받되 주문에는 쓰지 않는다 (확정 2026-08-07)**: LLM이 추천 종목마다
    `target_sell_price`("오늘 장중에 닿을 것으로 보는 매도 목표가")를 함께 내고, 그 값은 **추천 메일에
    참고로 표시하고 로그에 남기기만** 한다. 청산은 종전대로 순손익 기준 익절/손절(5.6절
    `check_portfolio_exit`)과 15:15 강제청산이 담당한다.
    - **지금 이 값을 청산에 쓰면 거의 발동하지 않는다.** 익절선 기본값 순손익 +0.5%는 가격으로 약
      +0.8%인데, LLM이 내는 매도 목표는 그보다 훨씬 위라 익절이 항상 먼저 걸린다. 즉 어느 쪽을
      우선하든 결과가 바뀌지 않으므로, 먼저 "LLM 목표 매도가가 실제 고가와 얼마나 맞았는지"를
      며칠 모아 보고 판단한다
    - **보정하지 않는다** — 매수가와 달리 호가 단위 내림도 ±5% 클램프도 걸지 않는다. 주문에 쓰이지
      않아 보정할 이유가 없고, 보정하면 LLM이 실제로 무슨 값을 냈는지 되짚을 수 없어 관찰 데이터가
      오염된다. 매수가보다 낮게 나오면 **경고 로그만** 남기고 값은 그대로 둔다
    - 값이 없으면 0으로 두고 메일에서 그 줄을 통째로 뺀다 (거래량 급증 배수·최근 가격대와 같은 규약)
    - **DB에는 남지 않는다** — 목표가 기록은 `logs/autotrade/YYYY-MM-DD.log`뿐이고 보관 기간은
      40일이다. 실제 체결가만 `data/trades.db`에 영구 저장된다. 이 값을 오래 축적하려면 별도 저장이
      필요하다 (미결정)
  - **10:10까지 미체결로 남은 매수 주문은 취소**하고 그날 그 종목은 포기한다(코드 상수). 익절 기준이
    "순수익 발생 즉시"라 회전이 빠른 전략이므로 아침에 체결되어야 의미가 있고, 늦게 체결될수록
    15:15 강제청산까지 회수할 시간이 부족하다. 부분체결분은 그대로 보유하고 잔량만 취소한다
  - 미체결 주문은 **당일 체결내역 조회 결과에서 찾는다**(`unfilled_quantity > 0`) — 엔진이
    재시작되어 인메모리 주문 목록이 사라져도 취소가 누락되지 않게 하기 위함이다.
    다만 체결내역 TR(`ka10076`)이 *한 주도 체결되지 않은 대기 주문*을 싣는지는 확인하지
    못했다(확인하려면 장중에 실제로 미체결로 걸린 주문이 있어야 한다). 싣지 않는 경우를
    대비해, **조회 결과에 주문번호 자체가 없는 접수 주문은 아직 살아 있는 것으로 보고 잔량
    전부를 취소**한다. 체결된 주문은 조회 결과에 잡히므로 이 보완 경로에 걸리지 않는다
  - **15:15 강제청산 직전에 미체결 취소를 한 번 더 돌린다 (확정 2026-08-07)**. 스케줄러는
    시작 시점에 이미 지난 트리거를 소급 실행하지 않으므로(`TimeScheduler.skip_past_due_for_today`),
    엔진을 10:10 이후에 켠 날은 그날 취소가 통째로 빠진다. 그 상태로 두면 목표가에 걸린 매수
    주문이 장중에 체결되고, 15:15 청산은 매도만 하므로 **오버나이트 포지션이 남아 당일 매도
    원칙이 깨진다**. 그래서 마감 정리는 `취소 → 청산` 순서로 고정한다(`runtime.close_out`).
    취소가 실패해도 청산은 반드시 진행한다 — 청산이 당일 매도의 마지막 방어선이다.
    10:10이 정상적으로 돌았다면 거둘 주문이 없어 무해하다
  - 10:10 취소를 수동으로 돌릴 수 있도록 **UI 즉시 실행에 ③ 버튼**을 둔다 (5.10절)
  - **09:08 매수 기록은 파일(`data/buy_records.json`)로 10:10에 넘긴다 (확정 2026-08-07)**.
    인메모리로 두면 09:08~10:10 사이 설정 저장 등으로 엔진이 재시작될 때 기록이 사라져,
    취소는 체결내역 조회로 살아나도 **매수 결과 메일만 조용히 빠진다** — 사용자는 미체결인지
    장애인지 구분할 수 없다. 기록에는 날짜를 함께 적어 다른 날 것이면 무시하고, 메일을 보낸
    뒤에는 파일을 지워 15:15 마감 정리가 같은 메일을 다시 보내지 않게 한다
- **LLM 연동 요구사항**
  - 사용할 LLM 및 API는 구현 단계에서 확정 (예: Anthropic Claude API 등)
  - LLM에 입력하는 데이터 출처와 프롬프트 템플릿을 코드/설정으로 버전 관리하여, 추천 근거를 나중에 추적할 수 있어야 함
  - LLM 응답이 형식을 벗어나거나(파싱 실패), 응답 자체가 지연되어 09:00까지 도착하지 않으면 **해당일 매수는 스킵**하고 알림 발송 (무리하게 대체 로직으로 임의 매수하지 않음)
  - **추천 결과 이메일 발송 (필수)**: 파싱된 추천 종목(최대 설정값 개수, 기본 3개)에 대해 아래 항목을 포함한 이메일을 추천 시각~09:00 사이 발송
    - 종목코드
    - 종목명
    - 목표 매수가 (호가 단위·가드레일 보정을 마친 값, 2026-08-06 추가)
    - 추천 근거 (LLM이 제시한 사유)
  - 종목별 매매 가능 여부(대형주 유니버스 소속, 거래정지 여부 등)에 대한 **사전 자동 검증은 수행하지 않음** — 매매 불가 종목이 추천된 경우 09:08 매수 시도 시 키움 API 주문 거부로 드러나며, 이 경우 주문 실패 로그/알림(5.7, 5.8절)으로 사용자가 인지
  - **추천 개수 확보 원칙 (확정 2026-08-04)**: 절대적인 확신이 없어도 상대적으로 가장 강한 종목
    순으로 설정된 개수(기본 3개)를 항상 채우도록 프롬프트로 요구한다 — 확신 있는 종목이 없다는
    이유로 그날 매수를 통째로 건너뛰는 일을 줄이기 위함. 후보 자체가 부족한 극단적 예외 상황에서만
    설정된 개수 미만이 나올 수 있으며, 그 경우의 처리 방식은 10절 확정 항목 참고.
    **2026-08-20의 "추천 유형 제한"이 이 원칙의 범위를 좁혔다** — 정원을 채우는 대상이 후보 전체가
    아니라 낙폭 되돌림 종목으로 한정된다
  - **추천 유형 제한: 낙폭 되돌림만 (확정 2026-08-20)**: LLM이 고른 종목 중 **최근 낙폭을 되돌리는
    구간(`rebound`)**에 해당하는 것만 매수한다. 프롬프트(v10)가 유형을 제한하고, 응답 스키마의
    `setup` 필드(`rebound` / `breakout` / `momentum`)를 코드가 다시 확인해 `rebound`가 아닌 추천을
    잘라낸다 (`drop_other_setups`)
    - **근거 텍스트(`reason`)로 거르지 않는다** — 자유 문장이라 같은 패턴이어도 표현이 매번 달라
      키워드 매칭은 신뢰할 수 없다. LLM이 유형을 스스로 분류하게 하고 그 값을 거른다
    - **유형 선택지를 셋으로 둔 것은 의도적이다.** `rebound` 하나만 열거하면 모든 종목에 그 값을
      붙여 내므로 필터가 무력해진다. 고르지 않을 유형을 함께 두어 분류를 정직하게 만든다
    - **전략의 성격이 바뀐다.** 종전 1순위는 "전일 급등 + 당일도 강세"의 추종이었고, 이제는
      이동평균 아래 / 최근 저가 근처까지 밀렸다가 오늘 되돌리는 종목이 1순위다. 프롬프트의 판단
      기준 순서도 그에 맞춰 최근 가격대에서의 위치를 맨 위로 올렸다
    - **조건에 맞는 종목이 없는 날은 그날 매수를 스킵한다.** 유형을 바꿔 적어 정원을 채우지 말라고
      프롬프트에 명시했고, 코드도 남은 추천이 0개면 `None`을 반환한다. 추천이 설정 개수보다 적게
      남은 날은 종전과 같이 모자란 몫을 현금으로 둔다 (종목당 투입금액은 추천 개수와 무관하게 고정)
    - **되돌리기 쉬운 범위로 한정했다** — 후보 수집(`_shortlist`의 거래량 급증 배수 상위)은 그대로다.
      수집 단계에서 낙폭 종목만 남기는 안은 파급이 커 채택하지 않았다
  - **방향성 무신호 종목 제외 (2026-08-05 도입 → 2026-08-06 폐지)**: 등락률과 시가갭이 둘 다
    0.00%인 종목을 후보에서 빼던 규칙(`DailyStockData.has_direction`)은 제거한다. 09:00 이전에는
    **모든 종목**이 이 조건에 걸려 후보가 0종목이 되고 추천 자체가 스킵되는 것이 실측으로 확인됐다
    (10절 "장 전 당일 지표 부재"). 후보 선정이 전일 지표로 바뀌면서 이 필터의 전제도 함께 사라졌다
- **매도(청산) 조건 (합산 판정으로 변경 2026-08-10)**: 판정 대상은 종목 하나가 아니라 **보유 종목 전체를
  합산한 손익**이고, 판정 기준은 가격 변동률이 아니라 매매수수료·매도세금·슬리피지를 뺀 **순손익률**이다
  (`COMMISSION_PERCENT`/`TAX_PERCENT`/`SLIPPAGE_PERCENT`, `.env`, 2026-07-31 반영).
  - **합산 순손익률 = (전체 예상 매도수령액 − 전체 매입원가) ÷ 전체 매입금액**
    - 전체 매입금액 = Σ(평단ᵢ × 수량ᵢ), 전체 매입원가 = 전체 매입금액 × (1 + 수수료)
    - 전체 예상 매도수령액 = Σ(현재가ᵢ × 수량ᵢ) × (1 − 슬리피지) × (1 − 수수료 − 세금)
    - 매입금액으로 가중한 평균이므로 **"보유 종목 손익을 전부 더해 투입 총액으로 나눈 값"** 과 같다.
      종목 개수로 나누는 단순평균이 아니다 — 그렇게 하면 100만 원짜리와 20만 원짜리가 같은 비중이 되어
      실제 계좌 손익과 어긋난다. 보유 종목이 하나뿐이면 종전의 종목별 순손익률과 같은 값이 나온다
    - **평단·수량·현재가 중 하나라도 0인 종목은 계산에서 통째로 뺀다.** 특히 현재가 0은 조회 실패나
      장 전 상태인데, 그대로 넣으면 −100%로 잡혀 합산이 즉시 손절선을 넘는다
    - **이미 매도 주문을 낸 종목도 뺀다** — 체결이 잔고에 반영되기까지 시차가 있어, 남겨두면 이미 판
      물량이 다음 틱의 합산을 계속 왜곡한다
  - **익절: 합산 순손익률이 +설정값 이상이면 보유 종목 전량 매도** (UI에서 % 조정 가능, **기본 0.5%**,
    `TAKE_PROFIT_PERCENT`). 기본값을 2%에서 0.5%로 낮춘 것은 작은 이익이라도 빠르게 확정하기 위함이다
    (확정 2026-08-06). 순손익률은 이미 왕복 비용을 뺀 값이라, 0.5%에 도달하면 그만큼이 실수령 이익이다.
    **단, 아래 "단순익절"이 기본 적용이라 평소에는 이 설정값이 쓰이지 않는다** (확정 2026-08-12)
  - **손절: 합산 순손익률이 -설정값 이하면 보유 종목 전량 매도** (UI에서 % 조정 가능, 기본 2%,
    `STOP_LOSS_PERCENT`)
  - **종목별 익절/손절은 두지 않는다 (확정 2026-08-10, 2026-08-12에 단순익절만 예외)**: 규칙을 하나로
    유지하기 위한 선택이다. 종목별 손절을 함께 두면 먼저 팔린 종목의 확정 손실이 이후 합산에서 빠져,
    어느 쪽이 먼저 걸리느냐에 따라 결과가 달라진다. **대가로 한 종목이 크게 무너져도 다른 종목이
    상쇄하면 매도가 나가지 않고 15:15 강제청산까지 간다** — 사용자가 이 위험을 알고 선택한 규칙이다.
    아래 **단순익절만 이 규칙의 예외로 종목별 판정**이며, 그 순서 의존성도 알고 택한 것이다
  - 이 조합(+0.5% / -2%)은 **이익이 작고 손실이 큰 구조**이므로 승률이 높아야 기대값이 양수가
    된다 — 사용자가 이 성질을 알고 선택한 값이다 (2026-08-06)
  - **단순익절 (확정 2026-08-12)**: 익절선을 설정값(%) 대신 **0**으로 두고, **종목마다 따로**
    판정하는 방식. 그 종목의 순손익률이 0을 **초과**하면(비용을 빼고 조금이라도 이익이면) **그
    종목만** 즉시 매도하고 나머지는 계속 보유한다. 익절(%) 입력란 오른쪽 "단순익절적용" 체크박스로
    다루며 `RiskManager.simple_take_profit_enabled` / `check_simple_take_profits`가 담당한다.
    **기본값이 적용이므로 평소 익절 동작은 `TAKE_PROFIT_PERCENT`가 아니라 이쪽이다**
    - **판정 단위가 다르다** — 손절과 퍼센트 익절은 합산(`check_portfolio_exit`)이고 단순익절만
      종목별(`check_simple_take_profits`)이다. 엔진은 **합산을 먼저** 본다: 계좌 전체가 손절선
      아래인 상황에서 이익 난 종목 하나를 파는 것보다 전량 청산이 우선이기 때문이다
    - **이익 난 종목이 먼저 빠지면 남은 보유는 손실 종목 위주가 되어 합산 손절이 더 쉽게 걸린다.**
      예: A +1% / B -3%는 합산 -1%로 아무것도 팔리지 않지만, 단순익절이 켜져 있으면 A가 먼저 나가고
      남은 B 하나가 -3%로 손절선(-2%)을 넘겨 결국 둘 다 정리된다. 2026-08-10에 종목별 청산을 없앤
      이유가 이 순서 의존성이었고, 이번에는 그 쪽을 알고 택한 것이다
    - 비교가 `>=`가 아니라 `>`인 것은 본전(0)에서 팔지 않기 위해서다 — 비용만 내고 끝나는 매매를
      만들지 않는다. '단순'은 기준선이 0이라는 뜻일 뿐, 순손익률 계산에서 수수료·세금·슬리피지를
      빼는 것은 합산 판정과 같다
    - **익절 "적용"(합산 퍼센트 익절)과 배타적이다** — 한쪽을 켜면 다른 쪽이 자동으로 꺼지고, 둘 다
      끄면 익절이 없다. 상위/하위 관계가 아니라 두 가지 방식 중 하나를 고르는 구조다. 단순익절이
      켜진 동안 **익절(%) 입력란은 비활성화**되며(값은 `.env`에 그대로 남는다), 비활성 위젯은
      스타일시트의 `:disabled` 규칙으로 회색 처리한다
    - 매수 실행 결과 메일은 익절선을 `+0.00%`가 아니라 "단순익절 (종목별 0% 초과)"로 적고, 종목별
      익절가 칸은 손익분기 가격으로 계산한다 — 합산 판정과 달리 이 가격이 그 종목의 실제 매도
      지점이다 (`BuyExecution.simple_take_profit`)
  - **선택 삭제를 접수 행까지 확대 (확정 2026-08-26)**: 매수 예정 표의 '선택 삭제'가 `매수 대기`뿐
  아니라 **`접수` 행에서도 동작**하게 하고, 그 경우 미체결 매수 주문을 그 자리에서 취소한다.
  상세는 5.10 "선택 삭제" 참고.
  - **계기 — 체크박스가 없다는 사용자 확인 요청.** 실제로는 표시 버그가 아니라, 체크박스가
    붙는 `매수 대기` 상태가 **추천 시각~매수 시각 3분(운영값 09:02→09:05)** 동안만 존재해
    그 뒤로는 표 전체에 체크박스가 하나도 없던 것이다. Qt는 `CheckStateRole`이 비어 있는
    셀에 체크 표시를 아예 그리지 않는다 — 열과 버튼이 종일 죽어 있어 고장으로 보였다
  - **표시만 살리는 안(비활성 체크박스를 그린다)은 택하지 않았다.** 열은 살아 보이지만 여전히
    아무것도 못 하므로 3분 문제가 그대로 남는다. 보유 종목 표의 '선택 매도'가 언제든 동작하는
    것과 같은 수준으로 맞추는 쪽을 골랐다
  - **접수 행은 지우지 않고 `미체결 취소`로 바꾼다.** 주문이 나갔었다는 사실은 그날 확인해야 할
    기록이고, 행을 없애면 10:10 결과 메일과 화면이 어긋난다 (5.10 "체결된 종목은 표에서 빠진다"와
    같은 원칙 — 빠지는 것은 완전 체결뿐이다)
  - **대가 — UI 버튼이 실주문 취소 경로가 된다.** 그래서 `drop_plan`을 `ORDER_ACTIONS`로 옮겨
    실전 계좌 경고를 함께 띄운다. 취소 실패 시에는 기록도 표도 건드리지 않고 운영 알림만 보낸다 —
    주문이 살아 있는데 화면만 정리된 상태가 가장 위험하다
- **매수 결과 메일 조기 발송 — 도입 후 철회 (2026-08-31 도입 → 2026-09-01 철회)**: 접수한
  매수가 전부 체결되면 10:10 `cancel_unfilled_buys`를 기다리지 않고 그 시점에 결과 메일을
  보내는 감시(`runtime.watch_buy_result`)를 넣었다가, 실매매 첫날 되돌렸다. **결과 메일은
  종전대로 10:10에 나간다.**
  - **도입 근거**: 주문 지정가를 밴드 상단으로 올린 뒤로(아래 "가격 지정에서 범위 지정으로")
    접수 직후 전량 체결이 기본이 됐는데, 결과 메일만 10:10에 묶여 이미 끝난 매수를 한 시간
    뒤에 알렸다. 판정은 매수 예정 표와 같은 잔고 대조였다
  - **철회 근거 — 2026-09-01 실매매.** 09:06:00에 "전부 체결"로 판정하고, 실제로는 살아 있던
    (161390)한국타이어 주문(0092505)을 취소했다. 10:10까지 갈 주문을 1시간 4분 일찍 잘랐다
  - **원인은 판정 근거다.** 잔고 대조는 "이 종목을 들고 있는가"에 답할 뿐 **"내 주문이
    체결됐는가"에 답하지 못한다.** 그날은 인스턴스 3개가 동시에 돌아 다른 인스턴스가 산 같은
    종목 12주가 잔고에 잡혔고, 인스턴스가 하나여도 **이월 보유 종목이 추천되면 같은 오판**이
    난다 (아래 "체결 반영 시차를 잔고로 메운다"가 근사치라고 적어 둔 바로 그 한계다)
  - **표시와 주문 취소는 요구 정확도가 다르다.** 표는 틀려도 다음 갱신에 맞춰지지만 취소된
    주문은 되돌릴 수 없다. 근사치를 주문 결정에 재사용한 것이 설계 실수다
  - **다시 하려면** 판정을 체결내역 조회(주문번호 대조)로 세워야 한다 — 잔고 대조로는 안 된다
- **매수 결과 메일 조기 발송 재도입 (2026-09-02)**: 철회한 기능을 판정 근거만 바꿔 다시
  넣었다(`DailyWorkflow.buy_orders_filled`, `runtime.watch_buy_result`). 잔고 대조 대신
  당일 체결내역 조회의 주문번호를 대조해, 접수한 주문번호가 전부 미체결 잔량 0이고 체결
  수량이 주문 수량 이상일 때만 10:10보다 앞당긴다.
  - **이월 보유 종목이나 다른 인스턴스가 산 물량은 섞이지 않는다.** 그 물량은 오늘 접수한
    주문번호와 다르므로 애초에 판정 대상(`pending`)에 들지 않는다 — 위 철회 근거에서 잔고
    대조가 속았던 바로 그 두 경우다
  - **확인되지 않으면 기다리는 쪽으로 틀린다.** 기록 파일이 없거나, 조회가 실패하거나,
    한 주문이라도 흔적이 없거나 미체결 잔량이 남아 있으면 그대로 False를 돌려주고
    10:10 `cancel_unfilled_buys`가 종전대로 마무리한다 — 실패해도 대가는 결과 메일이
    한 시간 늦어지는 것뿐이다
- **체결 반영 시차를 잔고로 메운다 (확정 2026-08-26)**: 매수 예정 표가 `접수` 행을 엔진의 잔고
  캐시와 대조해, 보유 수량이 주문 수량 이상이면 표에서 빼고 그보다 적으면 `부분체결`로 바꾼다.
  함께 '선택 삭제'의 취소 실패를 '이미 체결됨'과 '진짜 실패' 두 갈래로 나눈다. 상세는 5.10
  "체결 반영 시차를 보유 수량으로 메운다" 참고.
  - **계기 — 2026-08-26 13:40 수동 매수 실측.** 위 "가격 지정에서 범위 지정으로"를 넣은 뒤 첫
    실거래에서 (015760)한국전력 19주가 밴드 상단 35,900원에 **접수 즉시 전량 체결**됐는데,
    표는 계속 `접수`였고 같은 종목이 보유 종목 표에도 실려 두 표가 겹쳤다. 13:49에 선택 삭제를
    누르자 키움이 `취소가능수량이 없습니다`로 거절했고, 그 응답이 "주문이 살아 있습니다.
    직접 확인하세요"라는 **정반대 알림**으로 나갔다
  - **원인은 체결 반영 지점이 둘뿐이라는 것이다.** `_fill_buy_prices`는 `cancel_unfilled_buys`
    안에서만 불려 10:10과 15:15에만 돈다. 그 사이에는 `FILLED`로 바뀔 길이 없고,
    `_board_from_records`가 `FILLED`만 표에서 빼므로 행이 남는다. 수동으로 ② 매수 실행을 돌린
    날은 다음 반영이 15:15이라 시차가 한 시간 반을 넘었다
  - **범위 지정으로 바꾸면서 드러난 문제다.** 종전에는 목표가 지정가가 잘 체결되지 않아 `접수`
    행이 실제로도 미체결인 경우가 대부분이었다 — 시차가 있어도 표가 맞았다. 밴드 상단으로
    올린 뒤로는 접수 직후 체결이 기본이 되어 시차가 그대로 드러난다
  - **잔고 캐시를 쓰는 이유**: 표 갱신은 API를 호출하지 않는다는 5.10의 규약을 지키면서 2초
    타이머에 그대로 실린다. 체결내역을 새로 조회하거나(호출 증가) 스케줄을 하나 더 두는
    (수동 실행에는 안 붙는다) 안보다 싸다. 대가는 근사치라는 것이고, 기록과 결과 메일은
    종전대로 10:10·15:15의 체결내역 조회가 확정한다
- **익절·손절 폭 통합 (확정 2026-08-21)**: 익절선과 손절선의 **폭을 한 값으로 묶는다**. UI의 익절(%)·
    손절(%) 두 입력란이 "익절/손절 (%)" 한 칸으로 합쳐지고, 그 값이 익절 +N%·손절 -N%로 동시에 쓰인다.
    - **`.env` 키는 `TAKE_PROFIT_PERCENT`/`STOP_LOSS_PERCENT` 두 개 그대로다.** UI가 저장할 때 두 키에
      **같은 값**을 쓰고, 읽을 때는 `TAKE_PROFIT_PERCENT`를 기준으로 한 칸을 채운다. `Settings`도
      `RiskManager`도 종전대로 두 비율을 따로 받으며, 그 두 값이 항상 같아질 뿐이다 — 엔진과 테스트는
      바뀌지 않는다. 나중에 폭을 다시 갈라야 하면 UI만 되돌리면 된다
    - **`.env`를 손으로 다르게 적어 두면 익절 쪽 값이 UI에 뜨고, 저장하는 순간 손절 값이 그 값으로
      덮인다.** 통합 이후로는 UI를 통해 두 값이 달라질 경로가 없다. `.env.example`의 두 값도 서로 달랐던
      것(0.5 / 2)을 2로 맞췄다 — 다르면 새로 설치한 사용자가 저장을 누르는 순간 손절선이 조용히 0.5로 바뀐다
    - **켜고 끄는 것은 여전히 따로다.** 폭이 하나여도 '익절'·'손절' 체크박스는 각각 남아, 폭은 그대로 두고
      익절만 꺼서 손절만 남기는 조작이 계속 가능하다. 한 칸에 셋이 서므로 체크박스 이름은 "적용"에서
      "익절"·"손절"로 바꿨다
    - **단순익절이 켜져도 입력란을 잠그지 않는다.** 종전에는 익절선이 0이 되므로 익절(%) 입력란을
      비활성화했는데, 이제는 같은 칸이 손절선도 정하므로 잠그면 손절 폭을 못 고치게 된다
  - **익절/손절 적용 여부 (확정 2026-08-11)**: 익절·손절 각각을 그날 하루만 끌 수 있다. 통합된 "익절/손절 (%)"
    입력란 오른쪽의 "익절"·"손절" 체크박스로 다루며, 해제된 라인은 `check_portfolio_exit`이 건너뛴다
    (`RiskManager.take_profit_enabled` / `stop_loss_enabled`, 기본값은 손절만 적용 — 합산 퍼센트 익절은
    기본 해제이고 평소 익절은 아래 단순익절이 맡는다). 합산 순손익률 계산
    자체는 멈추지 않으므로 UI 표시는 그대로 유지된다
    - **`.env`에 저장하지 않고 엔진도 재시작하지 않는다.** 다른 리스크 설정은 저장 → 엔진 재시작
      경로를 타는데, 재시작 사이에는 WebSocket이 끊겨 감시가 멈춘다 — 정작 손절을 끄고 싶은 순간에
      그 공백이 생기면 안 된다. UI가 `EngineThread.set_exit_flags`로 돌고 있는 엔진에 즉시 반영하며,
      다음 시세 틱의 판정부터 바뀐 값이 쓰인다. 앱을 다시 켜면 항상 기본 상태(손절 + 합산 퍼센트 익절)로 돌아간다
    - **기본 상태로 되돌리는 것은 프로그램 재시작뿐이다 (확정 2026-08-13)**: 세 체크박스의 기본 상태
      (손절 + 합산 퍼센트 익절)는 `MainWindow` 생성 시 한 번 적용되고, 그 뒤 바꾼 상태는 프로그램을 끌 때까지
      유지된다. 시각 기반 원복(08:45 → 08:00으로 옮겼던 `EXIT_FLAG_RESET_TIME` UI 타이머)은 제거했다 —
      장중에 판단해서 바꾼 상태를 다음 날 아침 프로그램이 임의로 되돌리는 것보다, 되돌리는 시점을
      사용자가 재시작으로 직접 정하는 편이 낫다는 판단이다
      - **대가**: 프로그램을 여러 날 연속 켜 두면 어제 바꾼 상태가 오늘까지 그대로 간다. 손절을 끈 채
        방치되면 감시가 빠진 줄 모르고 매매에 들어갈 수 있으므로, 해제 상태를 알리는 UI 세 곳
        (경고 문구·보유 요약·매수 확인 팝업)이 이 위험을 떠받치는 유일한 장치가 된다
    - **15:15 강제청산은 이 플래그와 무관하게 그대로 나간다** — 당일 매도 원칙은 유지된다. 둘 다 끄면
      실시간 청산이 사라지고 15:15까지 보유하게 되므로, UI가 해제 상태를 경고 문구·보유 요약·매수 확인
      팝업 세 곳에 드러낸다
  - **매도 방식 (조사 결과 반영, 2026-07-27)**: 커뮤니티 REST API 래퍼(207개 엔드포인트 기준) 조사 결과 매수/매도/정정/취소 기본 주문만 확인되고 조건부 예약주문(스탑오더) 엔드포인트는 확인되지 않음 — **공식 문서로 최종 확인 전까지는 미지원으로 가정**하고, 시스템이 실시간 시세를 모니터링하다 위 익절/손절 조건 도달 시 시장가 매도하는 방식(5.6절 `check_portfolio_exit`)을 **1차이자 사실상 유일한 방식**으로 운영. `OrderClient.send_stop_order`는 추후 키움이 네이티브 스탑오더를 지원하는 것으로 확인될 경우를 대비한 스텁으로 유지
  - **당일 매도 원칙 (데이트레이딩)**: 장 마감 전까지 익절/손절 조건이 충족되지 않은 포지션은 **장 마감 직전 강제 매도**로 당일 내 정리 (다음날로 이월하지 않음)

**(C) 향후 전략 확장**
- 2호 이후 전략은 이번 범위에서 확정하지 않으며, 위 프레임워크 인터페이스를 그대로 따르는 새 모듈로 추가

### 5.6 리스크 관리 (필수, 시스템 레벨 강제)
- 종목당 최대 투입 금액/비중 한도 — 1호 전략은 구조적으로 예수금의 일정 비율(투입 비율 ÷ 추천 종목 수, 기본값 기준 약 1/6)씩 투입되므로, 이 값을 초과하는 주문은 리스크 레이어에서 거부 (전략 로직 버그로 잘못된 금액이 계산되는 경우를 대비한 이중 안전장치)
- 예수금 중 투입 비율(UI 설정값, 기본 50%)을 초과하는 나머지는 항상 미투입 상태로 유지(1호 전략 자체 규칙) — 사용자가 비율을 낮게 설정할수록 미투입 여유 자금이 커지며, 시스템은 이 여유 자금이 임의로 소진되지 않도록 보장
- 일일 최대 손실 한도 도달 시 자동으로 신규 매수 중단
- **보유 종목 합산** 순손익률이 익절(+설정값, 기본 0.5%)·손절(-설정값, 기본 2%)에 도달하면 **보유 종목 전량 자동 매도** (`RiskManager.check_portfolio_exit` → `TradingEngine._execute_portfolio_exit`) — 키움 스탑오더 미지원(조사 결과) 가정 하에 시스템 모니터링이 사실상 1차 방식(전략과 무관하게 항상 동작하는 안전장치). 종목별 판정은 두지 않는다 (5.5-B "매도(청산) 조건")
- 익절·손절은 각각 UI 체크박스로 해제할 수 있다 (기본값은 프로그램 시작 시에만 적용, 시각 기반 자동 원복은 2026-08-13에 제거) — 해제해도 15:15 강제청산은 그대로 나간다 (5.5-B "익절/손절 적용 여부")
- 장 마감 전 당일 매도 원칙에 따른 미청산 포지션 강제 정리
- 매도가 계속 거부되는 종목(상장폐지·거래정지 추정)은 자동으로 보유 목록·감시 대상에서 제외해 무의미한 재시도를 막되, 계좌에는 그대로 남으므로 UI "매도 불가" 표와 리포트 메일에 사유와 함께 노출한다 — 자동 청산 대상이 아니므로 사용자가 직접 확인해야 한다
- 전체 계좌 대비 최대 노출 비중 제한 — 기본 상한 100% (`RiskManager.max_total_exposure_ratio`, 기존 70%에서 완화, 확정 2026-08-03). 예수금 투입 비율을 100%까지 열어둔 UI 설정값이 이 안전장치에 걸려 매수가 거부되는 일이 없도록 상한만 완화했고, 검사 로직 자체는 유지해 전략 버그로 인한 과도한 매수는 여전히 걸러낸다
- 설정된 종목 수(기본 3종목) 동시 매수로 인한 종목 집중 리스크는 대형주 한정으로 일부 완화되지만, 종목들이 서로 다른 업종/테마인지까지는 전략이 보장하지 않으므로 필요 시 업종 중복 체크 로직 추가 검토

### 5.7 로깅 및 기록
- 모든 매수/매도 주문 요청·체결·실패 내역을 타임스탬프와 함께 저장 (파일 또는 로컬 DB)
- **이 프로그램이 내지 않은 체결도 기록한다 (확정 2026-08-11)**: 체결내역 조회 결과 중 주문번호가
  당일 기록에 없는 건은 **새 행으로 넣는다**(`TradeStore.apply_fills`). 종전에는 건너뛰어서, 키움
  앱(MTS/HTS)에서 직접 판 물량이 일일 리포트 손익에 통째로 빠졌다 — 2026-08-11 엘앤에프 9주가
  이 경로로 누락돼 리포트가 -24,448원을 찍었지만 계좌 실제 손익은 그와 달랐다. 리포트가 계좌와
  어긋나면 성과 판단의 근거 자체가 무너지므로 기록 대상에 포함한다
  - 수동 매도는 **평단을 알 수 없다**. 같은 날 같은 종목의 매수 체결가를 평단으로 대신 써서
    실현손익을 계산하고, 그마저 없으면(전일 이월분을 판 경우) 손익은 비워 두고 체결 자체만 남긴다
    — 틀린 손익을 적는 것보다 빈 값이 낫고, 수수료·세금은 어느 쪽이든 그대로 집계된다
  - 같은 날 여러 번 실행해도 결과가 같아야 한다(리포트 재발송·엔진 재시작) — 주문번호로 먼저
    찾고 없을 때만 넣으므로 두 번째 실행부터는 UPDATE로 흘러 중복 집계되지 않는다
- 일별 손익 요약 자동 생성
- 월별 누적 수익률·누적 수익금을 계산할 수 있도록 일별 손익을 월 단위로 집계·누적 저장
- 시스템 오류/예외 상황 로그 별도 기록

### 5.8 알림
- 주문 체결, 주문 실패, 연결 끊김, 손실 한도 도달 시 알림 발송
- **채널은 이메일 단독으로 확정 (2026-07-27)** — 텔레그램은 사용하지 않기로 하여 구현에서 제거했다. 운영 알림은 `AlertNotifier`가 이메일로 발송한다(`src/notification/alert.py`)
- 1호 전략의 LLM 추천 결과 알림(종목코드·종목명·추천근거)과 15:35 성과 리포트도 동일하게 이메일로 발송된다 (5.11절)

### 5.9 (Phase 2 이후) 백테스팅
- 과거 데이터로 전략을 미리 검증할 수 있는 별도 모듈
- 실거래 전략 인터페이스와 동일한 구조를 재사용하여 "백테스트 → 모의투자 → 실전" 순서로 검증 단계를 밟도록 설계

### 5.10 모니터링 UI (구현 완료 — 웹 대시보드 대신 PyQt6 데스크톱 앱으로 확정)
- 실행 로그, 보유 종목(수량·평단·현재가·평가손익), 매도 불가 종목(사유·시각)을 한 화면에서 확인한다 (`src/ui/main_window.py`)
- **즉시 실행 버튼과 스케줄 실행이 같은 큐를 탄다 (확정 2026-09-02)**: ①~⑤ 버튼(추천·매수·
  미체결 취소·전량 매도·리포트)과 08:40/추천 시각/매수 시각/10:10/15:15/15:35에 도는 자동
  단계는 같은 실행 큐(`src/core/actions.py`의 `ActionRunner`)를 거친다 — 종전에는 버튼과
  스케줄러가 서로 다른 경로로 같은 workflow 메서드를 불러, 09:07에 누른 ① 버튼의 LLM
  호출이 도는 사이 09:08 매수가 그대로 발동할 수 있었다.
  - **자동 실행이 도는 동안에는 ①~⑤ 버튼이 잠기고 상태바에 진행 중인 단계가 뜬다.**
    스케줄 실행 중인 줄 모르고 버튼을 눌러 같은 단계가 겹치는 것을 막는다.
  - **실행이 겹치면 거부하지 않고 순서대로 기다린다.** 거부하면 그날 매수·청산이
    그대로 빠질 수 있어서다 — 스케줄 작업이 조용히 빠지지 않게 하는 쪽을 택했다. 다만
    같은 액션이 이미 대기·실행 중이면 새로 접수하지 않는다(`ActionRunner`가 중복 접수를
    거른다) — 같은 메일이 두 번 나가거나 취소가 되풀이되는 것을 막는다
  - **단계별로 실행 스레드가 갈린다.** 매수·청산처럼 주문을 내는 단계는 실시간 익절/손절
    감시와 직렬화하도록 루프 스레드에서 그대로 돌고, 수집·LLM 호출·메일 발송처럼 오래
    걸리는 단계는 별도 스레드로 넘어가 그동안 WebSocket PING 응답이 막히지 않게 한다
- **보유 종목 패널 요약 줄에 목록 전체 합산을 표시한다 (2026-08-10)**: `N종목 · 평가손익 ±원 (±%) · 합산 순손익 ±% (익절 +0.5%)` 형태다. 평가손익률은 "지금 계좌가 얼마"를, 합산 순손익률은 "언제 팔리나"를 답하므로 둘 다 필요하다 — 청산 판정이 목록 전체 합산으로 바뀌면서(5.5-B) 이 줄이 곧 매도 트리거가 됐는데, 비용을 뺀 판정값과 평가손익률은 서로 어긋나 평가손익률만 보이면 익절선이 +0.5%인데 +0.8%에서 팔린 것처럼 읽힌다. 뒤쪽 괄호에는 현재 향하고 있는 쪽 라인(이익이면 익절, 손실이면 손절)만 붙여 한 줄에 담는다. 값은 UI가 다시 계산하지 않고 판정에 쓴 함수를 그대로 부른다 (`TradingEngine.portfolio_return_snapshot` → `RiskManager.portfolio_return`) — UI가 수수료율을 따로 읽어 계산하면 엔진이 실제로 판정한 값과 어긋날 수 있다
- **매수 예정 표 (확정 2026-08-24)**: 추천 시각에 나온 종목을 매수지정가·매도예상가·진행 상태와 함께 보여준다 (`DailyWorkflow.buy_plan_snapshot` → `MainWindow._refresh_buy_plans`). 로그만으로는 매수 절차가 어디까지 갔는지 알 수 없다는 것이 도입 이유다
  - 열은 선택·종목·상태·수량·매수지정가·**현재가**·매도예상가·비고이며, 상태는 `매수 대기` → `접수` → `부분체결`/`미체결 취소`, 또는 `건너뜀`/`실패`다. 상태 문구는 매수 결과 메일의 `BUY_OUTCOME_LABELS`를 그대로 쓴다 — 메일과 화면이 서로 다른 말을 쓰면 대조가 안 된다
  - **매도예상가는 익절(%) 설정을 순손익 기준으로 역산한 가격이다** (`exit_trigger_price`, 매수 결과 메일과 같은 계산식). LLM이 제시한 목표 매도가가 아니다 — 그 값은 주문에 쓰이지 않는 참고 수치라 "언제 팔리나"에 답하지 못한다. 익절이 꺼져 있으면 빈칸이고, 익절 판정은 보유 종목 합산이므로(5.5-B) 종목별로 적힌 이 가격은 근사치다
  - 표를 채우는 것은 `DailyWorkflow`가 들고 있는 **메모리 스냅샷**이지 `data/buy_records.json`이 아니다 — 그 파일은 10:10 결과 메일과 함께 지워지므로 그 뒤로는 표가 비어 버린다. 스냅샷은 추천·매수·10:10 세 지점에서 통째로 갈아끼우며(제자리 수정 금지 — 보유 종목 캐시와 같은 규약), 기록된 날짜가 오늘이 아니면 빈 목록을 돌려줘 전날 행이 남지 않는다
  - 갱신은 보유 종목 표와 **같은 2초 타이머**를 타고 캐시만 읽는다 — API를 호출하지 않는다
  - **현재가 (추가 2026-08-25)**: 엔진이 받아둔 마지막 시세를 그대로 찍는다 (`TradingEngine.last_price` → `BuyPlanView.current_price`). UI는 여전히 API를 부르지 않는다. 매수지정가보다 위면 빨강, 아래면 파랑이다 — 매수 시각에 현재가가 목표가보다 설정 폭 넘게 높은 종목은 건너뛰므로(`buy_price_tolerance_ratio`), 빨간 종목이 그 후보다
    - 추천 종목은 아직 보유가 아니라 아무도 구독하지 않는다. **추천 시각에 추천 종목을 실시간 시세에 미리 구독한다** (`DailyWorkflow._watch_plan_prices`) — 접수분 구독(`execute_buys`)은 익절/손절 감시가 목적이라 매수 시각에야 걸려서, 그 전 구간의 현재가가 비어 있었다
    - 이 구독은 `_off_loop` 스레드에서 들어오므로 `WebSocketClient.subscribe`가 루프 스레드로 넘겨 처리한다 — 그 스레드에는 실행 중인 이벤트 루프가 없어 `asyncio.create_task`가 곧바로 RuntimeError다
  - **체결 반영 시차를 보유 수량으로 메운다 (2026-08-26)**: 체결 반영(`_fill_buy_prices`)은 10:10과 15:15에만 도는데, 지정가가 허용 밴드 상단으로 올라간 뒤로는 접수 직후 체결되는 것이 보통이다. 그 사이 표가 `접수`로 얼어붙어 **같은 종목이 보유 종목 표와 겹쳐 실린다** — 2026-08-26 13:40 수동 매수에서 실측됐다(19주 전량 체결됐는데 표는 15:15까지 `접수`).
    - `buy_plan_snapshot`이 표를 그릴 때 **엔진의 잔고 캐시(`TradingEngine.position_snapshot`)와 대조한다.** 보유 수량이 주문 수량 이상이면 완전 체결로 보고 행을 빼고, 그보다 적으면 `부분체결`로 바꿔 보유 수량을 싣는다. 캐시만 읽으므로 **API 호출은 늘지 않는다** (현재가를 얹는 것과 같은 자리·같은 방식)
    - **표시만 바꾸고 기록(`data/buy_records.json`)은 건드리지 않는다.** 기록을 확정하는 것은 체결내역 조회를 보는 10:10·15:15의 일이고, 결과 메일도 그 값을 쓴다 — 잔고 대조는 표를 맞추기 위한 근사치다
    - **한계 — 전일부터 들고 있던 종목이 추천되면 접수 직후 체결로 오판한다.** 1호 전략은 당일 청산이라 실무상 드물지만, 15:15 청산에 실패해 남은 종목이 다음 날 추천되면 걸린다. 매수 직전 보유 수량을 기준선으로 남기는 방식은 데이터 모델이 늘어 택하지 않았다
    - **`부분체결` 행은 체크 열이 비어 선택 삭제 대상이 아니다.** 남은 미체결분은 10:10·15:15가 거둔다 — 여기서 취소하면 이미 체결된 몫까지 '미체결 취소'로 덮어써 결과 메일이 틀어진다
  - **체결된 종목은 표에서 빠진다 (2026-08-25)**: 이미 산 것은 '매수 예정'이 아니고, 아래 '보유 종목' 표에 현재가·손익과 함께 실린다 — 두 표에 같은 종목이 겹쳐 실리지 않는다. 빠지는 것은 **완전 체결(`FILLED`)뿐**이고, 부분체결(남은 수량이 미체결)·접수·건너뜀·실패·미체결 취소는 남는다. 못 산 이유는 그날 확인해야 할 정보다
  - **선택 삭제 (추가 2026-08-25, 접수 행까지 확대 2026-08-26)**: 표에서 종목을 골라 오늘 매수 대상에서 뺀다 (`DailyWorkflow.drop_buy_plans`, UI '선택 삭제' 버튼). 보유 종목 표의 '선택 매도'와 같은 체크 열 방식이다
    - **`매수 대기`와 `접수` 두 상태에서 지울 수 있고, 상태마다 하는 일이 다르다.**
      - `매수 대기` (매수 시각 전): 추천 목록에서 빼 그날 주문이 나가지 않게 한다. 주문은 나가지 않는다
      - `접수` (매수 시각 이후): **그 종목의 미체결 매수 주문을 그 자리에서 취소한다** — 10:10 `cancel_unfilled_buys`를 기다리지 않는다. 행은 표에서 지우지 않고 `미체결 취소`로 바꾼다. 주문이 있었다는 사실 자체가 기록이고, 지워 버리면 10:10 결과 메일과 화면이 어긋난다
      - `건너뜀`·`실패`·`미체결 취소`·`부분체결`은 취소할 살아 있는 주문이 없으므로 체크 열이 비어 있다
    - **왜 접수 행까지 열었나 (2026-08-26)**: `매수 대기`는 추천 시각과 매수 시각 사이에만 존재하는 상태라, 종전 구조에서는 이 기능을 쓸 수 있는 시간이 **3분(운영값 09:02→09:05)뿐**이었다. 그 뒤로는 표 전체에 체크박스가 하나도 없어 열과 버튼이 종일 죽어 있었다 — 기능이 있는 줄 모르거나 고장으로 보이는 상태다
    - **취소는 `data/buy_records.json`의 해당 기록을 `미체결 취소`로 바꾸고 파일을 다시 쓴다.** 그래야 10:10이 같은 주문을 다시 취소하려 들지 않고, 결과 메일에도 취소된 것으로 실린다. 취소에 실패하면 기록을 건드리지 않고 운영 알림을 보낸다 — 주문이 살아 있는데 표만 정리되는 상태를 만들지 않는다
    - **취소 실패를 두 갈래로 나눈다 (2026-08-26)**: 키움은 이미 전량 체결된 주문의 취소를 `취소가능수량이 없습니다`로 거절하는데, 이것을 실패로 뭉뚱그리면 **"주문이 살아 있습니다. 직접 확인하세요"라는 정반대 안내**가 나간다 (2026-08-26 13:49 실측). 취소가 거절됐을 때 보유 수량이 주문 수량 이상이면 **이미 체결된 것으로 보고 운영 알림을 보내지 않는다** — 위 잔고 대조와 같은 캐시를 쓴다. 그 외의 거절만 종전대로 알린다
    - 전략의 추천 목록(`LLMMomentumStrategy.drop_recommendations`)에서도 함께 빼야 매수 시각에 주문이 나가지 않는다 (`매수 대기` 경로)
    - **뺀 종목의 배정액은 남은 종목에 재분배되지 않고 현금으로 남는다.** `build_buy_plans`가 종목당 금액을 `예수금 × 투입비율 ÷ 목표종목수`로 고정하기 때문이며, 추천이 모자란 날의 처리(10절, 2026-07-27 확정)와 같은 동작이다
    - 엔진 루프 스레드에서 실행한다 (`manual_steps`의 `drop_plan`, `touches_orders=True`) — 매수 시각 작업과 같은 추천 목록·표를 건드린다. **접수 행 취소가 실제 주문 취소를 부르므로 2026-08-26에 `ORDER_ACTIONS`로 옮겼다** — 실전 계좌 경고를 함께 띄우기 위해서다. 확인 팝업은 종전에도 띄웠다 (`CONFIRM_ACTIONS`), 되돌리려면 LLM 추천을 다시 돌려야 하고 그러면 추천 종목 자체가 달라지기 때문이다
  - **엔진이 재시작되면 표가 빈다.** 추천 목록은 메모리에만 있어서(`LLMMomentumStrategy._recommendations`) 설정 저장 등으로 재시작하면 사라지고, 그러면 매수 시각이 와도 매수가 실행되지 않는다. 표가 이 상태를 드러내 주는 것이 현재 동작이며, 추천을 파일로 남겨 재시작을 견디게 하는 것은 이번 범위에 넣지 않았다 (2026-08-24)
- **매도 불가 표**는 오늘 매도하지 못한 종목만 담고, 해당 건이 없으면 숨겨진다. 상장폐지·거래정지 추정으로 보유 목록에서 제외된 종목은 자동 청산되지 않으므로(5.6절) 이 표가 유일한 확인 경로다
- **선택 매도 (확정 2026-08-11)**: 보유 종목 표 맨 앞에 '선택' 열을 두고, 체크한 종목만 시장가로 매도하는 버튼을 표 바로 아래에 둔다 (`TradingEngine.close_positions`, 액션 키 `sell_selected`)
  - **고르지 않은 종목은 그대로 보유하며 감시도 이어진다.** 익절/손절은 보유 종목 합산이므로(5.5-B) 판 종목이 빠진 나머지로 다시 판정된다 — 일부만 덜어내 합산 손익을 조정하는 용도로 쓸 수 있다
  - 전량 청산(`force_close_all_positions`)과 **주문 루프를 공유한다**(`_sell_positions`) — 매도가능수량 0·주문 거부·상장폐지 제외 처리가 두 경로에서 갈리지 않아야 한다. 실행도 ①~⑤와 같은 경로(`manual_steps` → `touches_orders=True`)를 타 실시간 익절/손절 콜백과 직렬화된다
  - **버튼은 ①~⑤ 그리드가 아니라 보유 종목 표 아래에 둔다** — 대상을 그 표에서 골라야 하므로 떨어뜨려 놓으면 무엇이 팔리는지 알기 어렵다. 체크된 종목이 없으면 비활성이고, 실행 전 확인 팝업에 대상 종목을 그대로 나열한다
  - 체크 상태는 표가 아니라 UI가 따로 갖는다(`MainWindow._checked_tickers`) — 표는 2초마다 다시 그려지므로 표에 상태를 두면 매번 날아간다. 잔고에서 사라진 종목은 선택에서도 함께 빠진다
  - 표가 `NoEditTriggers`라 셀 체크박스는 클릭으로 토글되지 않는 것이 실측으로 확인됐다. 체크 표시만 아이템으로 그리고 토글은 `cellClicked`에서 직접 처리하며, 아이템에 `ItemIsUserCheckable`을 주지 않는다 — 그 플래그가 있으면 체크 표시를 정확히 눌렀을 때 Qt 델리게이트가 한 번 더 뒤집어 클릭이 상쇄된다
- **설정 저장 시 엔진 자동 재시작 (확정 2026-08-04)**: 엔진은 시작 시점에 읽은 `Settings`를 그대로 들고 돌기 때문에 `.env` 저장만으로는 스케줄 시각·익절/손절 라인 등이 반영되지 않는다. 따라서 "설정 저장" 시 **저장값이 실제로 바뀐 경우에만** 엔진을 정지 후 자동 재시작한다(`MainWindow._needs_restart_for_changed_settings` → `_restart_engine`). 저장 완료 팝업("설정이 저장되었습니다. (재시작됨)")을 **먼저 띄우고 그 뒤에** 재시작한다 — 알림보다 정지가 앞서면 사용자가 영문을 모른 채 화면이 멈춘 것처럼 보인다. 재시작 중에는 WebSocket이 끊겨 익절/손절 감시가 잠시 멈추므로, **보유 종목이 있으면 먼저 확인을 받고** 사용자가 거부하면 저장만 하고 다음 시작 때 반영한다. 재시작 후 보유 종목은 `adopt_carried_over_positions`가 다시 감시 대상으로 편입한다

### 5.11 일일/월간/연간 성과 리포트 이메일 (발송 시점 확정 2026-07-30, 2026-08-12 개정, 연 누적 추가 2026-09-01)
- 발송 시점은 다음 둘 중 **먼저 도달하는 쪽 하나**다 (하루 최대 1회, `DailyWorkflow.send_final_report`)
  - **15:35 정시** (기본) — 정규장 마감(15:30) 후 5분을 두어 마감 동시호가 체결분까지 체결내역 조회에 잡힌 뒤 집계한다, 또는
  - **보유 종목이 그 전에 전부 매도되고 체결까지 확인되면 즉시** — 마지막 체결이 잔고에 반영될 시간(60초)을 기다린 뒤 발송하고 15:35 정기 발송은 생략한다. 상장폐지·거래정지로 보유 목록에서 제외된 종목(5.6절)은 이 판단에 넣지 않는다. 발송 뒤 재매수가 일어나면 판단이 초기화되어, 그 물량이 다시 전량 매도되면 한 번 더 발송하고 남으면 15:35가 보낸다
- **매수 쪽에도 같은 구조의 조기 발송이 있다 (확정 2026-09-02)**: 이 성과 리포트와는 별개의
  메일이지만 나란히 적어 둔다. 접수한 매수 주문이 전부 전량 체결되면 10:10을 기다리지
  않고 그 시점에 매수 결과 메일이 나간다(`runtime.watch_buy_result`). 판정은 당일
  체결내역 조회의 주문번호 대조이며, 한 건이라도 확인되지 않으면 종전대로 10:10
  `cancel_unfilled_buys`가 마무리한다. 상세는 5.5-B "매수 결과 메일 조기 발송 재도입" 참고
- **미체결 매도가 남아 있으면 청산 리포트를 보내지 않고 미룬다 (확정 2026-08-12)**: 청산은 주문 **접수** 시점에 완료로 표시되므로(`TradingEngine._mark_exited`), 체결이 늦어지면 그 매도가 `pending`으로 남아 집계에서 통째로 빠진다 — 판 종목이 "보유중"으로 실리고 손익·수수료·세금이 0으로 나간다. 따라서 청산 즉시 발송 경로는 체결내역을 먼저 동기화하고, 당일 매도 주문 중 체결도 거부도 아닌 건이 남아 있으면 **발송 표시를 세우지 않고 건너뛴다**. 그날 리포트는 15:35 정기 발송이 정산된 값으로 보낸다
- 리포트 내용
  - 당일 매수/매도 결과: 종목별 체결가·수량·당일 손익
  - 이번 달 누적 수익률(%) 및 누적 수익금(원) — 월초부터 당일까지 합산, 그 아래 올해 누적을 같은 꼴로 한 번 더
  - **이번 달 누적 순손익 꺾은선 그래프** (추가 2026-08-24) — 날짜별 누적 순손익을 PNG로 그려 HTML 본문에 인라인으로 붙인다. 점은 **매매가 있었던 날만** 찍는다 (휴장일까지 평평하게 이어 붙이면 거래일 간격이 왜곡된다). 마지막 점의 값은 위의 누적 순손익 숫자와 항상 일치한다 — 같은 수수료·세금 기준(`TradeStore.monthly_cumulative_series`)을 쓴다
    - **PNG인 이유**: Gmail이 메일 본문의 인라인 `<svg>`를 통째로 제거하고 Outlook 데스크톱도 SVG를 그리지 못한다. 그래서 matplotlib(Agg)으로 PNG를 그려 CID 인라인 첨부(`EmailNotifier.send(images=...)`)로 보낸다
    - **거래일이 하루뿐이어도 그린다 (2026-09-01)** — 점 하나를 0선 대비 위치로 찍는다. 종전에는 "꺾은선이 성립하지 않는다"며 생략했는데, 그러면 **매달 첫 거래일 리포트에는 그래프가 통째로 빠졌다**. 이번 달 매매가 한 건도 없을 때만 생략한다. 렌더링이 실패해도 리포트 발송 자체는 막지 않는다
    - 평문 파트에는 그래프가 없다 — 숫자 목록이 그대로 남는다
  - **올해 누적 (추가 2026-09-01)** — 월 블록 **바로 아래에 같은 꼴로** 붙는다: 누적 실현손익·누적 수수료·세금·누적 순손익. 기준은 월 집계와 같고 기간만 1월 1일부터다(`TradeStore.yearly_summary`) — 두 블록이 같은 규칙으로 나와야 "이번 달"과 "올해"를 나란히 읽을 수 있다
    - **현재 주문가능금액은 월 블록에만 둔다** — 기간과 무관한 값이라 두 번 적을 이유가 없다
    - **수익률의 분모는 연초 자산 추정치(= 현재 총자산 − 올해 순손익)다.** 월 블록과 같은 방식이지만 **기간이 길어 그만큼 거칠다** — 연중 입출금이 있으면 그 금액만큼 어긋난다. 정확히 하려면 연초 자산을 따로 기록해야 하고, 그 값은 아직 남기지 않는다
  - **올해 달별 누적 순손익 꺾은선 그래프 (추가 2026-09-01)** — 연 블록 아래에 두 번째 CID 인라인 첨부로 붙는다. 점 하나가 **한 달**이고 매매가 없던 달은 건너뛴다(`TradeStore.yearly_cumulative_series`). 그리는 코드는 월 그래프와 같고 x축 눈금만 날짜(`07/29`)에서 달(`07월`)로 바뀐다 — 점 타입(`DailyPoint`)도 그대로 재사용한다
  - **매도하지 못한 종목과 사유** (매도 거부, 매도가능수량 0, 보유 목록 제외 등) — 있을 때만 표시
- **정산 타이밍 이슈 (해소)**: 국내 정규장 마감 시각이 15:30이라 마감 직전 체결이 리포트에 반영되지 않을 수 있던 문제는 세 가지로 막는다 — ① 청산을 동시호가 이전(15:15)에 내 장중에 체결시키고, ② 미체결 매도가 남으면 청산 리포트를 미루고, ③ 정기 발송을 마감 5분 뒤(15:35)로 옮겼다. 강제청산 일부가 거부되어 15:35 시점에도 보유 종목이 남아 있는 날에는 그 종목이 "보유중"으로 실리는데, 이는 실제로 팔리지 않은 것이므로 정산 오류가 아니다
- 이 리포트도 5.8절과 마찬가지로 이메일로 발송

---

## 6. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 안정성 | API 연결 끊김/오류 발생 시 프로세스가 죽지 않고 재시도 또는 안전 정지 |
| 보안 | 인증정보는 코드/저장소에 노출 금지, 로컬 암호화 또는 환경변수 사용 |
| 성능 | 실시간 시세 수신부터 주문 실행까지 수 초 이내 (초단타 수준의 초저지연은 목표 아님) |
| 운영 환경 | 기본은 REST API 기반으로 OS 독립적 구성, 필요 시 개인 PC/소형 서버(NAS, 클라우드 VM 등)에서 상시 구동 |
| 유지보수성 | 전략 모듈과 핵심 엔진(주문/리스크/로깅)의 결합도를 낮게 유지 |
| 장애 대응 | 장중 예외 발생 시 "포지션을 정리하고 정지"할지 "정지만 하고 포지션 유지"할지 설정 가능해야 함 |
| 단일 인스턴스 | 한 번에 **하나만** 실행된다. **로그인 세션이 달라도 하나다** — 작업 스케줄러가 세션 0에 띄운 인스턴스도 같은 하나로 센다. 이미 떠 있으면 두 번째 실행은 안내 후 즉시 종료하고, `--auto-start`(무인 실행)는 기존 인스턴스를 대체하되 **대체에 실패하면 뜨지 않는다** (10절 "단일 인스턴스 강제", "무인 실행과 인스턴스 대체", "인스턴스가 세션을 넘어 쌓인 사고") |

---

## 7. 시스템 구성 (개략)

```
[스케줄러: 추천 시각(설정) / 매수 시각(설정) / 10:10 / 15:15 / 15:35 트리거]
        |
        v
[데이터 수집 모듈] --당일 데이터--> [LLM 추천 모듈] --추천 최대 N종목(설정값, 기본 3종목; 코드·종목명·근거)--> [이메일 발송 모듈] --> (사용자 메일함)
        |                                    |
   (시세/뉴스 등)                             v
        |                              [전략 모듈] --신호(매수+매도조건)--> [리스크 관리 레이어]
        v                                                                       |
[시세 수신 모듈] -------------------------------------------------------> (승인된 주문)
                                                                                 v
                                                                       [주문 실행 모듈] --> [키움 API]
                                                                                 |
                                                                                 v
                                                                       [로깅/알림 모듈] --15:35 집계--> [리포트 생성 모듈] --> [이메일 발송 모듈] --> (사용자 메일함)
```

- **스케줄러**: 추천 시각(`.env` 설정값, 기본 09:05) 데이터수집·LLM추천, 09:08 지정가 매수, 10:10 미체결 취소·매수 결과 발송, 15:15 마감 정리(미체결 취소 → 강제청산 순서)를 시각 기준으로 트리거한다. 성과 리포트는 15:35 정시 또는 보유 종목 전량 매도·체결 확인 완료 시(먼저 오는 쪽, 5.11절) 트리거한다. **이미 지난 트리거는 시작 시점에 소급 실행하지 않으므로**, 엔진을 늦게 켠 날에 빠지는 단계는 UI 즉시 실행으로 직접 돌린다 (1호 전략 전용, 실시간 이벤트 기반 전략도 병행 지원 가능해야 함)
- **데이터 수집 모듈**: LLM 프롬프트에 쓸 당일 데이터(등락률, 거래량, 뉴스 등)를 모음
- **LLM 추천 모듈**: 데이터를 프롬프트로 구성해 LLM 호출, 응답을 구조화된 추천 종목 리스트(코드·종목명·근거)로 변환
- **이메일 발송 모듈**: LLM 추천 결과(추천 시각~09:00)와 성과 리포트(15:35)를 자동 검증 없이 그대로 사용자에게 이메일로 전달
- **시세 수신 모듈**: WebSocket으로 실시간 시세를 받아 전략/리스크 판단에 사용
- **전략 모듈**: 표준 인터페이스를 따르는 교체 가능한 로직 (현재는 1호 전략 탑재, 매수 신호와 매도 조건을 함께 산출)
- **리스크 관리 레이어**: 전략의 신호를 그대로 실행하지 않고, 종목당 한도/손실 한도를 먼저 검사
- **주문 실행 모듈**: 실제 키움 API 호출 담당, 재시도/오류 처리 포함
- **로깅/알림 모듈**: 전 구간(LLM 추천 근거, 매수·매도 결과 포함)의 이벤트를 기록하고 필요 시 알림 발송
- **리포트 생성 모듈**: 로그에 쌓인 당일 매수·매도 결과, 월간 누적 데이터, 매도하지 못한 종목 목록을 집계해 리포트 내용을 구성 (15:35 정시 또는 전량 매도·체결 확인 완료 시, 5.11절)

---

## 8. 리스크 및 제약사항

- **규제/약관 리스크**: 증권사 Open API 이용약관상 금지 행위(비인가 재판매, 과도한 요청 등)를 반드시 준수해야 하며, 자동매매라 해도 투자 결과에 대한 책임은 본인에게 있음
- **API 변경 리스크**: 신규 REST API는 상대적으로 이력이 짧아 스펙 변경 가능성이 있으므로, 외부 API 호출부는 별도 모듈로 감싸 변경에 대응하기 쉽게 설계
- **요청 제한(Rate Limit)**: TR/REST 호출 건수 제한이 있으므로 과도한 폴링 대신 WebSocket 실시간 구독을 우선 활용
- **재무적 리스크**: 전략 오류나 시스템 버그로 인한 실계좌 손실 가능성 — 반드시 모의투자에서 충분히 검증 후 실전 전환, 실전 초기에는 소액으로 시작
- **단일 장애점**: 개인 PC/서버 1대에 의존하므로 정전, 인터넷 장애 시 대응 방안(예: 장애 시 자동 전량 정지) 필요
- **LLM 예측 불확실성**: LLM의 "급등 예상" 추천은 예측일 뿐 정확성이 보장되지 않으며, 시장 상황에 따라 손실로 이어질 수 있음 — 시스템 설계로 이를 없앨 수는 없고, 종목당 한도·손절 규칙으로 손실 폭을 제한하는 것이 현실적 목표
- **LLM 응답 지연/비용 리스크**: 추천 시각~09:00 사이(설정에 따라 5~20분) 안에 응답을 받아야 하므로 API 응답 지연 시나리오에 대한 타임아웃/스킵 처리가 필수이며, LLM API 호출 비용도 지속 발생. 추천 시각을 08:55로 당겨 잡을수록 여유가 5분까지 줄어든다
- **사전 검증 부재 리스크**: 추천 종목에 대한 자동 유효성 검증을 이메일 알림으로 대체했으므로, 거래정지·상장폐지 등 매매 불가 종목이 추천되면 09:08 매수 시도 시점에야 주문 실패로 드러남 — 사용자가 추천 결과 이메일을 실제로 확인하지 않으면 이상 상황을 사전에 인지하기 어려움
- **리포트 타이밍 리스크 (해소, 5.11절)**: 국내 정규장 마감 시각이 15:30으로 리포트 발송 시각과 겹치던 문제는 청산 시각 15:15 이동 + 미체결 매도 시 발송 보류 + 정기 발송 15:35 이동으로 해소했다. 남은 위험은 그 반대편이다 — **`data/trades.db`에 `pending`으로 남은 매도는 그 뒤로 영영 채워지지 않는다.** 체결내역 조회(`ka10076`)가 당일분만 주므로, 하루가 넘어가면 그 매도의 손익·수수료가 일간 리포트뿐 아니라 **월 누적에서도 빠진 채 고정된다** (2026-08-12 실제 발생)
- **매도 불가 종목 잔존 리스크**: 상장폐지·거래정지로 추정되는 종목은 반복 매도 시도를 막기 위해 보유 목록·감시 대상에서 자동 제외하지만, 그만큼 자동 청산도 되지 않는다 — 계좌에는 그대로 남으며 UI "매도 불가" 표와 리포트 메일로만 알리므로, 사용자가 직접 확인·처리해야 한다

---

## 9. 개발 로드맵 (제안)

| Phase | 내용 | 산출물 |
|---|---|---|
| Phase 0 | **(사용자 직접 진행)** 키움증권 계좌 개설 및 Open API 사용신청, 앱키/시크릿 발급 | 발급된 인증키 |
| Phase 1 | API 인증/연동, 시세 조회, 주문 실행 기본 기능 구현 (모의투자 계좌 대상) | 동작하는 API 래퍼 |
| Phase 2 | 전략 인터페이스 + 시간 기반 스케줄러 설계, 더미 전략(HOLD)으로 전체 파이프라인 연결 검증 | 최소 동작 시스템 |
| Phase 3 | 리스크 관리 레이어, 로깅(매수/매도 결과 포함), 알림 기능 추가 | 안전장치 포함 시스템 |
| Phase 4 | LLM 연동 모듈(데이터 수집 → 프롬프트 → 추천 파싱) 및 추천 시각 이메일 발송, 09:00 매수·매도조건 설정 로직 구현 | 1호 전략 매매 동작 버전 |
| Phase 5 | 로그 기반 장 마감 후 일일/월간 성과 리포트 이메일 기능 구현 | 리포트 이메일 동작 버전 |
| Phase 6 | 모의투자 환경에서 1호 전략 장기 안정성 검증 (최소 2~4주, LLM 추천 정확도·리포트 정확성 관찰 포함) | 검증 로그/리포트 |
| Phase 7 | 소액 실계좌로 전환 후 투입 금액 단계적 상향, 모니터링 강화 | 실전 운영 |

---

## 10. 추후 결정 필요 사항 (Open Questions)

- 실행 환경: 상시 켜두는 개인 PC vs 클라우드 VM
- **월간 누적 수익률 산정 기준일**: 매월 1일부터 누적할지, 실전 전환일부터 누적할지 (우선 매월 1일 기준으로 구현됨, 변경 가능)

**확정된 항목 (구현 완료, 2026-07-27):**
- 개발 언어/기술 스택: Python + PyQt6(UI) + requests/websockets
- 데이터 저장 방식: SQLite (`src/logger/trade_store.py`) — 개인 프로젝트 규모에 맞춰 별도 서버 없이 파일 하나로 운영
- 알림 채널: **이메일 단독** — 텔레그램은 사용하지 않기로 하여 제거했다. 운영 알림·LLM 추천·성과 리포트가 모두 이메일로 나간다 (5.8절)
- **키움 REST API 스탑오더 지원 여부 (2026-07-28 확인 완료, 미지원 확정)**: 공식 API 가이드의
  국내주식 주문 카테고리는 `kt10000`(매수)/`kt10001`(매도)/`kt10002`(정정)/`kt10003`(취소) 4개뿐이며,
  커뮤니티 래퍼(국내주식 207개 엔드포인트) 역시 주문 관련은 위 4개 + 금현물(`kt50000~3`) +
  신용(`kt10006~9`) 12개가 전부다. **조건부·스탑·예약 주문 엔드포인트는 존재하지 않는다.**
  → 모니터링 방식(`RiskManager.check_portfolio_exit`)이 API 경로에서 유일한 청산 수단으로 확정.
  → 단, **영웅문4 `[0624]` 자동감시주문은 서버 측 감시**이며("HTS, MTS의 ON/OFF 상태에 상관없이
  저장된 감시조건이 만족될 경우 자동으로 주문이 실행됩니다", 최대 90일 지속, 보유종목 자동매도 50개
  한도) 이익실현·손실제한·트레일링스탑을 지원한다. **API로는 등록할 수 없어 HTS 수동 설정만 가능**하다.
  참고로 `[0621]` 주식StopLoss는 HTS가 켜져 있어야 동작하므로 본 프로그램과 한계가 같다.
- **유니버스 기준 (2026-08-05 확대 → 2026-08-06 환원)**: 키움이 코스피200 구성종목을 직접 제공하지 않아, `ka10099`의 시가총액 규모별 분류에서 **`대형주`(약 98종목)**만 유니버스로 사용한다. 2026-08-05에 `중형주`(192종목)까지 넓혔다가 지정가 매수 도입과 함께 하루 만에 되돌렸다 — 중형주는 호가가 얇아 목표가 지정가의 체결·슬리피지 조건이 나쁘다. `소형주`(512종목)와 코스닥은 계속 제외한다. 거래정지·관리종목·투자경고 종목은 수집 단계에서 제외한다
- **장 전 당일 지표 부재 (실측 확인, 2026-08-06)**: 09:00 이전에는 `ka10001`이 등락률·시가갭을 주지 않는다. 2026-08-06 08:55(동시호가 한복판, 개장 5분 전) 수집에서 **288종목 전부**가 등락률 0.00%·시가갭 0.00%로 돌아왔고, 그 결과 `has_direction` 필터에 전량 걸려 후보 0종목 → 추천 스킵 → 09:00 매수 0건이 됐다(`logs/autotrade/2026-08-06.log`). 같은 코드가 09:02에는 288종목 중 3종목만 제외했다. `get_stock_detail`이 대비해 둔 동시호가 예상체결가(`exp_cntr_pric`) 대체 경로는 실제로 값이 오지 않아 동작하지 않았다.
  → **09:00 이전 추천은 당일 지표를 근거로 삼을 수 없다**는 것이 확정 사실이며, 이것이 전일 일봉 기준 수집(5.5-B)으로 바꾼 이유다
  → **2026-08-14에 추천 자체를 09:05로 옮기면서 이 제약의 사정권을 벗어났다** (아래 "개장 후 추천으로 이동"). 위 실측은 **여전히 유효하다** — 철회된 것이 아니라, 그 시각에 판단하지 않기로 한 것이다. 추천 시각을 09:00 이전으로 되돌리려는 시도가 있으면 이 항목이 그대로 근거가 된다
- **목표 매수가 지정가 (확정 2026-08-06, 범위 지정으로 변경 2026-08-26)**: LLM이 추천 종목과 함께 목표 매수가를 제시하고, 그 값을 기준점으로 잡은 **허용 밴드 상단**에 지정가 매수한다 (2026-08-26 이전에는 목표가 그 자체로 주문했다). 코드가 호가 단위 내림과 ±5% 클램프로 보정하며, 미체결이면 취소한다. **시각은 09:00/09:30이었고 2026-08-14에 09:10/09:40으로, 클램프 기준은 전일 종가에서 당일 현재가로 바뀌었다** (아래 '개장 후 추천으로 이동'). 상세는 5.5-B "주문 방식" 참고
- **목표 매도가는 참고용 (확정 2026-08-07)**: LLM에 목표 매도가도 함께 요구하되, 추천 메일 표시와 로그 기록에만 쓰고 청산 로직은 종전대로 둔다. 익절선(순손익 +0.5% ≈ 가격 +0.8%)이 LLM 목표가보다 훨씬 아래라 지금 구조에서는 어느 쪽을 우선해도 결과가 같기 때문이다 — 종목별 익절선으로 대체할지는 실제 고가와 대조한 데이터가 쌓인 뒤에 판단한다(미결정). 손절은 계좌 리스크 문제이므로 LLM에 맡기지 않는다. 상세는 5.5-B "주문 방식" 참고
- **토큰 무효화와 자동 재발급 (확정 2026-08-06)**: 키움은 **앱키당 접근토큰을 하나만 유지**한다. 다른 프로세스가 같은 앱키로 토큰을 발급하면 실행 중인 엔진의 토큰이 만료 전에 죽고, 이후 모든 TR이 `return_code=3`(`8005:Token이 유효하지 않습니다`)으로 거부된다.
  - 2026-08-06 09:14:14에 진단 스크립트가 토큰을 발급하면서 실제로 발생했다. 09:14:18부터 잔고·예수금 조회와 **손절 청산 주문이 전부 거부**되어, (079900)전진건설로봇이 -2.7%에서 22분간 팔리지 못한 채 방치됐다(09:36 엔진 정지까지). 재시작 후 받은 토큰의 만료 시각이 `2026-08-07 09:14:14`로 찍혀 무효화 시점이 특정됐다
  - **운영 규칙: 엔진이 떠 있는 동안에는 API를 호출하는 스크립트를 돌리지 않는다.** `scripts/check_balance.py` 등 조회 전용 스크립트도 해당된다. **UI를 두 번 띄우는 것도 같은 경로다** — 그쪽은 2026-08-13에 코드로 막았다 (아래 "단일 인스턴스 강제")
  - **코드 대응**: `AuthClient._is_token_valid`는 로컬 만료 시각만 보므로 서버가 죽인 토큰을 가려낼 수 없다. `KiwoomClient.request`가 인증 오류를 만나면 `AuthClient.refresh_token(rejected=…)`로 강제 재발급하고 **한 번만** 재시도한다. 인증 오류 판정은 `return_code=3` + 메시지 키워드(`Token`/`토큰`/`인증`)를 함께 본다 — 이 코드가 인증 전용이라고 확인되지 않아, 주문 거부 같은 업무 오류까지 재시도해 같은 주문이 두 번 나가는 것을 막기 위함이다. 토큰 발급은 `threading.Lock`으로 직렬화하고, 재발급 전에 다른 스레드가 이미 새 토큰을 받아 왔으면 건너뛴다
- **동일 알림 중복 억제 (확정 2026-08-06)**: 운영 알림(`AlertNotifier`)은 **같은 장애를 거래일당 한 번만** 메일로 보낸다. 중복 판정은 본문에서 숫자를 지운 키로 하며(가격·수량·손익만 달라 문장이 매번 바뀌기 때문), 종목명·사유·오류 메시지가 다르면 별개 알림으로 본다. 억제된 알림도 로그에는 그대로 남는다.
  - 계기: 2026-08-06 09:14~09:36 손절 청산이 토큰 오류로 계속 거부되면서 같은 알림 메일이 **30통** 나갔다
- **익절 기본값 변경 (확정 2026-08-06)**: 익절 기준을 **순손익 +2% → +0.5%**로 낮춘다. UI의 익절(%) 설정(`TAKE_PROFIT_PERCENT`)은 그대로 유지하며 기본값만 바뀐다. 손절은 설정값(기본 2%) 그대로다. 검토 과정에서 "순손익 0 초과 시 즉시 매도"(설정 폐지)안을 한 차례 구현했다가, 익절 폭을 사용자가 조절할 수 있어야 한다는 판단으로 되돌렸다 (5.5-B "매도(청산) 조건")
- **합산 손익 기준 익절/손절 (확정 2026-08-10)**: 익절/손절 판정을 **종목별에서 보유 종목 전체 합산으로** 바꾸고, 조건에 닿으면 **보유 종목을 전량 매도**한다. 설정값(`TAKE_PROFIT_PERCENT` 기본 0.5% / `STOP_LOSS_PERCENT` 기본 2%)과 `.env` 키, 순손익률 계산식(수수료·세금·슬리피지 차감)은 그대로 두고 적용 범위만 바뀐다.
  - 합산은 **매입금액 가중 평균 순손익률** = 전체 손익 ÷ 전체 매입금액이다. 종목 개수로 나누는 단순평균은 투입금액이 다른 종목을 같은 비중으로 취급해 실제 계좌 손익과 어긋나므로 쓰지 않는다. 상세 계산식과 제외 조건은 5.5-B "매도(청산) 조건" 참고
  - **종목별 익절/손절은 폐지한다.** 규칙을 둘로 두면 먼저 팔린 종목의 확정 손실이 이후 합산에서 빠져 순서에 따라 결과가 달라진다. 대가로 **한 종목이 크게 무너져도 다른 종목이 상쇄하면 15:15 강제청산까지 방치된다** — 사용자가 이 위험을 알고 선택했다
  - **알림은 종목마다 보내지 않고 한 통으로 묶는다** — 합산 손익률과 매도한 종목 요약을 담아 한 번만 보내고, 개별 주문 **거부**만 종목별로 알린다. 종목별로 보내면 보유 3종목에 메일 3통이 나간다 (같은 날 중복 억제는 종목명이 달라 걸리지 않는다)
  - 매수 실행 결과 메일의 **종목별 익절가·손절가 칸은 참고 가격으로 남긴다** — 그 가격에 닿아도 그 종목만 팔리지는 않으며, "이 종목 혼자였다면 조건에 닿는 가격"이라는 의미다
- **선택 매도 (확정 2026-08-11)**: 보유 종목 표에서 체크한 종목만 시장가로 매도한다 (`TradingEngine.close_positions`). 고르지 않은 종목은 그대로 보유하며, 익절/손절은 남은 종목의 합산 손익으로 다시 판정된다. 전량 청산과 주문 루프(`_sell_positions`)를 공유해 매도가능수량 0·주문 거부·상장폐지 제외 처리가 갈리지 않게 하고, 실행도 ①~⑤와 같은 경로를 타 실시간 감시와 직렬화된다. 상세는 5.10절 "선택 매도" 참고
  - 매도 로그·알림 문구를 사유별로 나눈다(`CLOSE_REASON_LABELS`) — 종전에는 사유 문자열을 그대로 끼워 넣어 UI ④ 전량 매도에도 "장마감 강제청산 (manual)"이 나갔고, 선택 매도가 붙으면 더 어긋난다
- **익절/손절 적용 여부 플래그 (확정 2026-08-11)**: 익절·손절을 각각 UI 체크박스("적용")로 그날 하루만 끌 수 있게 한다. 해제된 라인은 `RiskManager.check_portfolio_exit`이 건너뛰며, 합산 순손익률 계산과 UI 표시는 그대로 유지된다. 기본값은 **손절 + 합산 퍼센트 익절**이다 (2026-08-12에 손절만 → 2026-08-13에 손절+단순익절 → **2026-08-18에 손절+퍼센트 익절** — 아래 '익절 기본 해제', '단순익절 기본 적용', '퍼센트 익절 기본 적용' 참고). 단순익절이 기본 해제로 남는다.
  - **`.env`에 저장하지 않고 엔진도 재시작하지 않는다** — 다른 리스크 설정이 타는 저장→재시작 경로는 재시작 사이에 WebSocket이 끊겨 감시 공백을 만드는데, 정작 손절을 끄고 싶은 순간에 그 공백이 생기면 안 된다. `EngineThread.set_exit_flags`가 돌고 있는 엔진의 `RiskManager` 플래그를 직접 바꾼다 (bool 대입은 원자적이고 주문을 내지 않아 루프 스레드 직렬화가 필요 없다)
  - **기본값(손절 + 합산 퍼센트 익절)은 프로그램 시작 시에만 적용된다** — 도입 당시에는 매일 08:45(뒤에 08:00)에 UI 타이머가 되돌렸으나 **2026-08-13에 제거했다** (아래 '청산 조건 시각 원복 제거')
  - **15:15 강제청산은 플래그와 무관하다** — 당일 매도 원칙은 그대로 유지된다. 대신 해제 상태를 UI 세 곳(리스크 박스 경고 문구, 보유 종목 요약의 진행 표시, 매수 확인 팝업)에 드러내 "감시 중"이라고 오해하지 않게 한다
- **단순익절 (확정 2026-08-12)**: 익절선을 설정값(%) 대신 0으로 두고 **종목별로** 판정하는 방식을 UI 체크박스("단순익절적용")로 추가한다. 그 종목의 순손익률이 0을 초과하면 **그 종목만** 매도하고 나머지는 계속 보유한다. 도입 당시 기본 적용 → 2026-08-12 기본 해제 → **2026-08-13에 다시 기본 적용**으로 돌아왔다 (아래 '익절 기본 해제', '단순익절 기본 적용'). `RiskManager.simple_take_profit_enabled` / `check_simple_take_profits`가 담당하고, 저장·반영·원복 방식은 위 익절/손절 플래그와 같다 (5.5-B "단순익절")
  - **2026-08-10의 "종목별 익절/손절은 두지 않는다"에 대한 유일한 예외다.** 손절과 퍼센트 익절은 그대로 합산 판정이고, 엔진은 합산을 먼저 본다 — 계좌 전체가 손절선 아래면 이익 난 종목 하나를 파는 것보다 전량 청산이 우선이다
  - **이익 난 종목이 먼저 빠지면 합산 손절이 더 쉽게 걸린다** — A +1% / B -3%는 합산 -1%로 안 팔리지만, 단순익절이 켜져 있으면 A가 먼저 나가고 남은 B가 -3%로 손절선을 넘겨 둘 다 정리된다. 종목별 청산을 없앴던 이유(순서 의존성)를 이번에는 알고 택했다
  - **본전(0)에서는 팔지 않는다** (`>=`가 아니라 `>`) — 비용만 내고 끝나는 매매를 만들지 않는다. '단순'은 기준선이 0이라는 뜻이고, 순손익률에서 수수료·세금·슬리피지를 빼는 것은 그대로다
  - **익절 "적용"과 배타적이다** — 한쪽을 켜면 다른 쪽이 자동으로 꺼지고, 둘 다 끄면 익절이 없다. 상위/하위가 아니라 두 방식 중 하나를 고르는 구조다. 단순익절이 켜진 동안 익절(%) 입력란은 비활성화되며, 비활성 위젯은 스타일시트 `:disabled` 규칙으로 회색 처리한다 (스타일시트를 씌우면 Qt 기본 disabled 표현이 먹지 않아 활성 상태와 구분되지 않았다)
  - **원복 시각을 08:45 → 08:00으로 옮긴다** — 추천 시각이 설정값(08:40~08:55)이라 08:45 원복이 그날의 추천·매수 판단보다 뒤로 밀릴 수 있었고, 08:20에 끈 라인이 25분 만에 되켜지는 문제도 있었다. 08:00에 기본 상태로 되돌린다 (**2026-08-13에 시각 원복 자체를 제거해 이 항목은 무효**)
- **익절 기본 해제 (확정 2026-08-12, 단순익절 부분은 2026-08-13에 철회 — 아래 '단순익절 기본 적용')**: 익절 두 방식(합산 퍼센트 익절 / 단순익절)을 **모두 기본 해제**로 바꾼다. 기본 상태는 **손절만**이며, 익절은 켜는 쪽이 그날 하루짜리 판단이 된다. `RiskManager`의 `take_profit_enabled`/`simple_take_profit_enabled` 기본값과 UI 체크박스 초기 상태, 08:00 원복 기본값이 함께 바뀐다. 손절은 계좌를 지키는 장치라 그대로 기본 적용이다.
  - **근거 — 실매매 27건(2026-07-29~08-12)을 당일 일봉 고가와 대조한 결과**: 실제 순손익 −209,464원(−1.22%)인데, 같은 매수 건을 **익절 없이 손절만 두고 15:15에 청산했다면 +59,320원(+0.35%)**이었다. 익절선을 어디에 두든(0%·0.5%·1%·2%·2.3%·3%·4%) "익절 없음"보다 못했다
  - **원인은 상방 절단이다.** 매수일 당일 고가가 매수가 대비 평균 **+4.6%**이고 27건 중 16건이 +2.5% 이상을 찍었는데, 단순익절의 실제 발동가는 +0.21%(순손익 0 + 비용)에 불과했다. 08-10 (066970)엘앤에프는 고가 +16.38%·종가 +14.12%인 날 +0.34%에, (003670)포스코퓨처엠은 고가 +8.50%인 날 +0.39%에 팔렸다
  - **손절은 없애지 않는다.** 같은 대조에서 손절까지 빼면 +184,538원(+1.08%)으로 더 좋았지만, 이 표본에 재난일이 없었을 뿐이다(저가 −12.88%·−10.15%·−9.50%를 손절이 −2.70%에서 잘랐다). 27건으로 안전장치를 떼지 않는다
  - **표본이 작다는 것을 전제로 한 결정이다** — 27건 / 12거래일이고, 그사이 익절 규칙이 2% → 0.5% → 합산 → 단순익절로 여러 번 바뀐 데다 수동 매도(UI ④)가 섞여 있다. 되돌리기 쉬운 쪽(체크박스 기본값)만 바꾸고, 지정가 익절 같은 구조 변경은 데이터가 더 쌓인 뒤로 미룬다
  - **지정가 익절은 채택하지 않았다 (재검토 예정)**: 매수 체결 직후 목표가에 지정가 매도를 걸어두면 거래소에 남아 앱이 꺼져도 유효하고 시장가 슬리피지(0.1%p)도 사라진다. 다만 (a) 선을 얼마로 둘지가 위 대조에서 "익절 없음"보다 나은 값이 없었고, (b) 손절이 걸리면 **미체결 익절 주문을 먼저 취소하고 취소를 확인한 뒤** 시장가를 내야 해(수량이 잠겨 "매도가능수량 부족"으로 거부된다) 급락장에서 손절이 늦어진다. 키움 REST에는 OCO가 없어 이 순서를 직접 관리해야 한다. LLM 목표 매도가(`target_sell_price`)와 실제 고가를 더 대조한 뒤 다시 판단한다
- **단순익절 기본 적용 (확정 2026-08-13)**: 바로 위 '익절 기본 해제' 중 **단순익절 부분만 철회**한다. 기본 상태를 **손절 + 단순익절**로 되돌려, 평소 익절 동작은 다시 종목별 단순익절이 맡는다. 합산 퍼센트 익절('적용')은 그대로 기본 해제이고, 둘의 배타 관계는 바뀌지 않는다. `RiskManager.simple_take_profit_enabled` 기본값과 UI 체크박스 초기 상태가 함께 바뀐다.
  - **사용자의 재판단이다.** 08-12 대조 결과(27건 / 12거래일, 익절이 상방을 잘라 −209,464원 → 익절 없이 손절만이면 +59,320원)는 그대로 유효하며 철회되지 않았다. 다만 그 표본이 작고 그사이 익절 규칙이 여러 번 바뀌었다는 전제도 함께 기록돼 있었고(위 '표본이 작다는 것을 전제로 한 결정'), 바뀌는 것이 체크박스 기본값 하나뿐이라 되돌리기 쉬운 쪽이다
  - **끄는 쪽이 그때그때의 판단이 된다** — 08-12와 방향이 반대다. 단순익절을 빼고 싶으면 체크를 해제하고, 프로그램을 다시 시작하면 켜진 상태로 돌아온다
- **퍼센트 익절 기본 적용 (확정 2026-08-18)**: 기본 상태를 **손절 + 합산 퍼센트 익절**로 바꾼다. 익절 '적용'(합산 퍼센트) 체크박스가 시작 시 켜져 있고, 단순익절은 기본 해제가 된다. 두 방식의 배타 관계와 `.env` 미저장·시작 시에만 적용이라는 규칙은 그대로다. `RiskManager.take_profit_enabled`/`simple_take_profit_enabled` 기본값과 UI 체크박스 초기 상태가 함께 바뀐다.
  - **사용자 지시다.** 익절 기본값은 이것으로 네 번째다: 단순익절(도입) → 둘 다 해제(2026-08-12) → 단순익절(2026-08-13) → 퍼센트 익절(2026-08-18)
  - **08-12 대조 결과와 방향이 어긋난다는 점을 기록해 둔다.** 그 분석(27건 / 12거래일)은 익절선을 **0%·0.5%·1%·2%·2.3%·3%·4% 어디에 두든 "익절 없음"보다 못했다**고 결론냈고, 그 결론은 철회되지 않았다. 지금 기본값은 그중 하나를 켜 두는 선택이므로, 표본이 쌓이면 `trades.exit_reason`으로 다시 대조해 볼 값이다
  - **단순익절과 무엇이 달라지는가**: 판정 단위가 종목별에서 **보유 목록 합산**으로, 익절선이 0(비용 제외 후 이익이면 즉시)에서 **`TAKE_PROFIT_PERCENT` 입력값**으로 바뀐다. 익절이 걸리면 그 종목만이 아니라 **보유 종목 전량**이 나간다. 익절(%) 입력란도 다시 활성화된다
- **단일 인스턴스 강제 (확정 2026-08-13)**: AutoTrade는 한 번에 하나만 실행된다. 이미 떠 있으면 두 번째 실행은 `scripts/run_ui.py`가 **창을 만들기 전에** 안내 대화상자를 띄우고 그대로 종료한다(`sys.exit(0)`).
  - **근거는 토큰이다.** 두 번째 인스턴스가 뜨면서 같은 앱키로 토큰을 발급하면 먼저 돌던 엔진의 토큰이 즉시 죽고 손절 청산까지 거부된다 — 위 "토큰 무효화와 자동 재발급"의 2026-08-06 사고와 같은 경로이며, 그때는 진단 스크립트였지만 UI를 두 번 띄워도 똑같이 벌어진다. 실시간 감시가 사실상 유일한 청산 수단이라 이 사고의 대가가 크다
  - **판정은 Windows 명명된 뮤텍스**다(`CreateMutexW(r"Global\AutoTrade-SingleInstance")` → `GetLastError()==ERROR_ALREADY_EXISTS`). **락 파일을 쓰지 않는 이유**는 비정상 종료 시 파일이 남아 다음 실행을 영영 막기 때문이다 — 뮤텍스는 프로세스가 끝나면 OS가 놓으므로 정리 코드도 stale lock 처리도 없다
  - **`Global` 접두사는 필수다 (정정 2026-09-01)**. 접두사가 없으면 뮤텍스가 **로그인 세션 단위**로 만들어진다. 도입 당시에는 "`.env`가 사용자별로 다르므로 그 범위가 맞다"고 봤으나, **문제는 사용자가 아니라 세션이었다** — 같은 사용자의 작업 스케줄러 태스크가 세션 0에서 돌아 세션 1의 실행과 서로를 보지 못했다 (아래 "인스턴스가 세션을 넘어 쌓인 사고")
  - **뮤텍스 검사는 조건과 무관하게 항상 부른다** — 그 호출이 곧 뮤텍스를 잡는 일이라, 단락 평가로 건너뛰면 이 인스턴스가 소유자가 되지 못해 다음 실행이 아무것도 보지 못한다
  - **검사가 실패하면 막지 않고 진행한다** — 두 번 뜨는 것보다 아예 못 뜨는 쪽이 더 나쁘다
  - **안내를 띄우는 이유**: `AutoTrade.bat`이 `pythonw.exe`로 띄워 콘솔이 없다. 조용히 종료하면 "눌렀는데 아무 일도 안 일어남"이 된다. Qt가 올라오기 전이라 `QMessageBox`가 아니라 Win32 `MessageBoxW`를 쓴다(기존 `_show_fatal`과 같은 방식)
  - **기존 창을 앞으로 끌어올리지는 않는다** — `QLocalServer`로 프로세스 간 통신을 붙여야 해서, "실행 안 함"이라는 요구에 비해 값이 크다
  - **`--auto-start`로 뜬 경우는 예외다 (2026-08-27)** — 아래 "무인 실행과 인스턴스 대체" 참고
- **인스턴스가 세션을 넘어 쌓인 사고 (2026-09-01)**: 인스턴스 **3개**가 동시에 돌아
  추천 메일이 3통 나가고, 세 엔진이 같은 예수금을 각자 전액으로 보고 매수해 **의도의 1.5배**가
  접수됐다(2종목 173만 예정 → 4건 259만, 같은 종목 중복 포함). WebSocket은 5초마다 서로를
  밀어내 익절/손절 감시가 사실상 멈췄고, 현재가 조회는 429(rate limit)로 실패했으며,
  `data/buy_records.json`을 셋이 공유해 서로의 기록을 지웠다.
  - **원인 ① 뮤텍스가 세션 단위였다.** 08:30 실행은 `_already_running()`이 False를 받아
    **중복 검사도 대체도 아예 타지 않았다** — 로그에 "이미 실행 중"도 "대체"도 없이 곧바로
    `AutoTrade UI 시작`이 찍혔다. 세션 0의 인스턴스가 만든 `Local` 뮤텍스가 세션 1에서
    보이지 않았기 때문이다 (위 "단일 인스턴스 강제" 정정 항목)
  - **원인 ② 그렇게 뜬 인스턴스가 PID 파일을 덮어썼다.** 앞서 돌던 세션 0 인스턴스의 PID가
    지워져, 다음 날 08:00 무인 실행의 대체가 그 인스턴스를 찾지 못했다. 그래서 전날 것이
    하루를 넘겨 살아남았다
  - **대응 (2026-09-01)**: ①뮤텍스에 `Global` 접두사를 붙여 세션 너머로 본다 ②PID 대조에서
    **열지 못한 프로세스는 살아 있는 것으로 본다** — 세션 0 인스턴스는 일반 권한에서 열리지
    않는데 그것을 '죽었다'로 처리하면 중복을 허용한다 ③대체에 실패하면 **새 인스턴스를 띄우지
    않는다** ④다른 세션이라 `WM_CLOSE`를 받을 창을 찾지 못하면 15초를 기다리지 않고 바로 강제
    종료한다
  - **남는 한계 — 세션 0 인스턴스는 일반 권한으로 종료할 수 없다.** 화면(세션 1)에서 수동
    실행하면 "이미 실행 중"에 막히고, 그 인스턴스를 끄려면 관리자 권한 명령 프롬프트가
    필요하다(`taskkill /PID <pid> /F`). 세션 0에서 GUI 앱을 돌리는 한 없앨 수 없다 —
    작업 스케줄러를 `LogonType = Interactive`로 바꾸면 사라지지만, 그러면 로그온하지 않은
    날의 실행이 통째로 빠진다. **무인 실행을 지키는 쪽을 택했다 (2026-09-01)**
- **무인 실행과 인스턴스 대체 (확정 2026-08-27, 대상 판정 보강 2026-09-01)**: `scripts/run_ui.py --auto-start`는 창의 "▶ 시작"을 누르지 않아도 엔진을 자동으로 띄운다(`MainWindow(auto_start=True)`). `AutoTrade_AutoStart.bat`이 이 인자를 넘기며, 윈도우 시작프로그램·작업 스케줄러 등록용이다. **이 경로로 실행됐을 때만 기존 인스턴스를 종료하고 새로 시작한다.**
  - **수동 실행(`AutoTrade.bat`)은 종전대로 막는다.** 뮤텍스가 실제로 걸리는 경우는 대부분 "이미 켜져 있는데 아이콘을 한 번 더 눌렀다"이고, 그때 기존 것을 죽이면 **살아 있는 익절/손절 감시를 끊는 것**이 된다. 무인 실행에서만 여는 이유는 그쪽은 사람이 화면을 보고 있지 않아 "이미 실행 중" 대화상자가 뜨면 아무도 닫지 않고, 그 상태로 그날 매매가 통째로 빠지기 때문이다
  - **종료는 정상 종료 먼저, 안 되면 강제 종료다.** 대상 프로세스의 최상위 창에 `WM_CLOSE`를 보내 `MainWindow.closeEvent`를 태운다 — 그래야 엔진이 정상 정지한다. 다만 **보유 종목이 있으면 그 경로가 확인 팝업에서 멈춘다**(`_confirm_close_with_positions`). 무인 실행에는 누를 사람이 없으므로 `INSTANCE_REPLACE_TIMEOUT_SECONDS` 안에 끝나지 않으면 `TerminateProcess`로 강제 종료하고, **강제 종료했다는 사실을 경고 로그로 남긴다**
  - **대상 프로세스는 PID 파일(`data/autotrade.pid`)로 찾는다.** 뮤텍스는 "누군가 떠 있다"만 알려줄 뿐 상대 PID를 주지 않는다. PID 재사용으로 엉뚱한 프로세스를 죽이지 않도록 `QueryFullProcessImageNameW`로 대상 실행 파일이 `python.exe`/`pythonw.exe`인지 확인하고, 어긋나면 **죽이지 않는다**
  - **살아 있는지 판정할 때 "열지 못함"은 "살아 있음"이다 (2026-09-01)**. `OpenProcess`가 `ERROR_ACCESS_DENIED`를 돌려주면 프로세스는 있는데 권한이 모자란 것이다 — 세션 0의 무인 실행 인스턴스가 정확히 이 경우다. 그 외의 실패(대개 없는 PID)만 죽은 것으로 본다
  - **대체하지 못하면 새 인스턴스를 띄우지 않는다 (2026-09-01)**. 종전에는 대체 실패가 곧 "안내 후 종료"였지만 무인 실행에서는 그 안내를 닫을 사람이 없어 `sys.exit(1)`로 끝낸다. 대체에 실패했는데 그대로 뜨면 그것이 곧 중복 실행이다
  - **PID 파일은 지우지 않는다.** 비정상 종료로 남더라도 위 두 조건이 걸러내므로 stale 처리가 필요 없다 — 뮤텍스를 락 파일 대신 쓴 것과 같은 이유다
  - **대가 — 강제 종료는 주문 도중에 끊길 수 있다.** 주문은 나갔는데 `data/buy_records.json`에 기록이 없는 상태가 이론상 가능하다. 그래도 무인 실행에서 그날 매매가 통째로 빠지는 것보다 낫다는 판단이며, 확률을 낮추려 정상 종료를 먼저 시도한다
  - **시작프로그램은 실행 시각을 보장하지 않는다.** `shell:startup`·`HKCU\...\Run`은 부팅이 아니라 **로그온** 시점에 돌고 Windows 8 이후 기본 약 10초가 지연된다. PC를 켜 둔 채 날이 바뀌면 다시 실행되지 않고, 로그인이 09:02(추천 시각) 뒤로 밀리면 그날을 놓친다. **시각을 보장하려면 작업 스케줄러의 시각 트리거를 쓴다** — 이 문서는 등록 방식을 강제하지 않는다
- **청산 조건 시각 원복 제거 (확정 2026-08-13)**: 익절/손절/단순익절 체크박스를 매일 정해진 시각(08:45 → 08:00)에 기본 상태로 되돌리던 UI 타이머를 **제거한다**. 기본 상태는 `MainWindow` 생성 시 한 번만 적용되고, 되돌리는 방법은 **프로그램 재시작뿐**이다. `EXIT_FLAG_RESET_TIME` / `EXIT_FLAG_RESET_CHECK_MS` 상수, `_exit_flag_timer`, `_reset_exit_flags_daily`, `_exit_flags_reset_on`이 모두 사라진다.
  - **되돌리는 시점을 사용자가 정한다.** 장중에 근거를 보고 바꾼 상태를 다음 날 아침 프로그램이 임의로 원복하는 것보다, 껐다 켜는 행위로 사용자가 직접 초기화하는 편이 낫다는 판단이다
  - **대가 — 프로그램을 여러 날 연속 켜 두면 어제 상태가 오늘까지 간다.** 손절을 끈 채 방치되면 감시가 빠진 줄 모르고 매매에 들어갈 수 있고, 원복 도입 당시의 근거가 정확히 이것이었다. 이제 그 위험은 해제 상태를 알리는 UI 세 곳(리스크 박스 경고 문구, 보유 종목 요약, 매수 확인 팝업)만으로 떠받친다
  - **엔진의 08:40 `reset_for_new_day`는 그대로다** — 일일 손실 한도·매매 중지 해제·매도 불가 목록 초기화는 계속 매일 돈다. 이번에 없어진 것은 UI 체크박스 원복 하나뿐이다
- **개장 후 추천으로 이동 (확정 2026-08-14)**: LLM 추천을 **08:45 → 09:05**로, 매수를 **09:00 → 09:10**으로, 미체결 취소를 **09:30 → 09:40**으로 옮긴다. 08:40 일일 초기화, 15:15 강제청산, 15:35 리포트는 그대로다. 이로써 후보 선정이 **전일 데이터만 보던 구조에서 당일 지표를 함께 보는 구조로** 바뀐다 (5.5-B "당일 지표 병행 수집").
  - **계기 — 2026-08-14 (175330)JB금융지주**: 전일 거래량 급증 배수 2.78배로 후보 25종목 중 1위였고, 전일 +5.01%·고가 근처 마감·이동평균 위·최근 고가 근접으로 프롬프트의 판단 기준 네 항목에 모두 부합해 추천됐다. 09:00 지정가 29,000원 주문이 **28,650원에 체결**됐는데 이는 전일 종가 29,350원 대비 **-2.39%**로, `GAP_DOWN_TOLERANCE_PERCENT` 1% 하한(29,056원)을 한참 밑도는 값이다. 그런데도 "건너뜀 0종목"으로 통과했다 — 09:00:00 판정 시점의 현재가가 아직 동시호가라 실제 시가를 반영하지 못한 것으로 보인다(그 시점 조회값은 로그에 남지 않아 정황 추정이다). 09:14:57에 -2.79%로 손절됐다
  - **근본 원인은 "09:00 이전에 판단하고 09:00에 주문한다"는 시각 구조다.** 전일 데이터를 쓰는 것 자체가 아니라, 갭 가드조차 실제 시가를 보지 못하는 시점에 돌았다는 것이 문제였다. 개장 후로 옮기면 후보 선정·목표가 산정·갭 판정이 모두 실제 당일 가격 위에서 돈다
  - **되살릴 코드는 없었다.** 2026-08-06에 `get_stock_detail`/`StockDetail`을 지웠지만 `MarketDataClient.get_current_price`(`ka10001`)가 그대로 남아 현재가·거래량을 준다. 당일 등락률은 이 현재가와 전일 종가로 직접 계산하므로, 그때 값이 오지 않았던 `flu_rt`·`exp_cntr_pric` 필드에 다시 의존하지 않는다
  - **대가**: 개장~09:10의 상승분을 놓치고, 보유 시간이 6시간 15분 → 6시간 5분으로 줄어든다. 또 09:05에 `get_current_price`가 정상 값을 준다는 것은 2026-08-06 로그의 09:02 관측(288종목 중 3종목만 제외)에 기댄 **추정이지 직접 실측이 아니다** — 구현 첫 단계로 장중 UI ① 즉시 실행으로 확인하고, 값이 오지 않으면 시간표를 뒤로 미룬다
  - **검토했으나 채택하지 않은 것 — 전일 변동폭 하드 컷**: 5.5-B "당일 지표 병행 수집"의 변동폭 항목 참고. 2026-08-14 실측 분포가 "변동폭 = 위험"이라는 전제를 지지하지 않아, 거르지 않고 프롬프트에 싣기만 한다

- **매수 타이밍 조정 (확정 2026-08-20)**: 매수를 **09:10 → 09:08**로, 미체결 취소를 **09:40 → 10:10**으로
  옮긴다. 위 "개장 후 추천으로 이동"과 5.5-B "주문 방식"에 적힌 시각을 이 항목이 대체한다.
  - **근거 — 09:10 매수 체제 4거래일(08-17~08-20)의 실제 매수는 1종목(08-18, 218,000원)뿐이다.**
    나머지 3거래일은 투입 0원이었고, 걸린 지점이 셋으로 갈린다: ①갭 상승 초과(08-19 3종목,
    08-13 3종목) ②갭 하락 미만(08-14 3종목, 08-18·08-20 각 1종목) ③09:40 미체결 취소(08-13·08-20
    각 1종목). ①②는 **09:05 추천 → 09:10 주문의 5분** 사이에 가격이 허용 창을 벗어난 것이다 —
    08-19 DB손해보험은 그 5분에 183,700 → 191,000원(+4.0%)으로 움직였다. 갭 판정의 허용 창은
    `[추천시점가 ×0.99, 목표가 ×1.02]`로 2% 남짓이라, 개장 직후 5분은 이 창을 번번이 벗어난다
  - **09:07이 아니라 09:08인 이유**: 추천 작업은 수집 약 40초 + LLM 호출(타임아웃 상한 120초)이라
    최악의 경우 09:07:35에 끝난다. 그보다 앞당기면 **추천이 아직 없는 채로 매수가 돌아** 그날이
    통째로 빈다. 실측 소요는 57~66초(08-17~08-20)로, 09:08은 여기에 약 2배의 여유를 둔 값이다
  - **미체결 취소를 10:10으로 미룬 이유**: 목표 매수가는 프롬프트가 눌림을 기다리라고 시켜 현재가보다
    낮게 나오는 경우가 많은데(08-20 삼성증권 87,300 → 목표 86,500), 30분 안에 닿지 않으면 그대로
    죽는다. 매수 +62분으로 늘려 되돌림이 실제로 오는지를 본다. 대가는 체결이 늦어질수록 15:15
    강제청산까지의 보유 시간이 짧아진다는 것이다
  - **추천 시각 선택지를 09:00~09:05로 좁혔다가, 같은 날 콤보박스 자체를 없앴다** — 종전 상한이
    09:20이라 추천이 매수보다 뒤로 갈 수 있었다. 좁히고 나니 고를 값이 09:00·09:05 둘뿐인데 09:00은
    동시호가 위험이 있어 사실상 하나였다. 그래서 **추천·매수 시각을 모두 `.env`로 옮기고 UI에서는
    라벨로 보여주기만 한다** (5.5-B "추천·매수 시각 설정"). 매수 시각을 설정값으로 연 것은 이 항목의
    09:08이 실측 4거래일에 근거한 잠정값이라, 앞으로 조정할 값이 바로 여기이기 때문이다.
    두 시각의 순서·간격은 `Settings.validate()`가 엔진 시작 단계에서 막는다
  - **갭 하락 허용치(`GAP_DOWN_TOLERANCE_PERCENT`)는 이번에 건드리지 않는다.** 낙폭 되돌림 종목은
    09:08에 다시 눌릴 확률이 높아 이 필터와 부딪히지만(08-20 삼성에피스홀딩스는 목표가 378,000원
    아래인 375,000원까지 왔는데도 하한 378,180원에 걸려 스킵됐다), 이 필터의 도입 근거가 08-14
    JB금융지주 손절 사고라 손절 방어를 함께 약화시키지 않는다. 위 두 시각을 며칠 돌려 보고 그때도
    되돌림 종목이 계속 걸리면 그때 재검토한다
- **추천·매수 시각 앞당김 (확정 2026-08-21)**: 운영값을 **추천 09:05 → 09:02**, **매수 09:08 → 09:05**로
  옮긴다. `.env`의 `RECOMMEND_TIME`/`BUY_TIME`만 바꾼 것이고, `config/settings.py`의 기본값
  (09:05/09:08)과 `.env.example`은 그대로 둔다 — 위 "매수 타이밍 조정"이 이 두 값을 `.env` 전용
  설정으로 연 것이 정확히 이런 조정을 위해서였다. **5절·5.5-B에 09:05/09:08로 적힌 시각은
  기본값이며, 실제 운영 시각은 이 항목이 대체한다.**
  - **추천을 09:01이 아니라 09:02로 둔 이유**: `ka10001`이 정상 값을 준다고 확인된 가장 이른 시각이
    09:02다(2026-08-06 로그의 288종목 중 3종목만 제외 관측). 09:01은 실측이 없다. 위 "장 전 당일 지표
    부재"가 무너뜨린 것은 09:00 **이전**이므로 개장 후 2분이면 전제는 성립한다
  - **매수를 09:05(추천 +3분)로 둔 이유**: 추천 작업은 수집 약 40초 + LLM 타임아웃 상한 120초라
    최악 09:04:40에 끝난다. `MIN_RECOMMEND_TO_BUY_MINUTES`가 허용하는 하한(09:05)이 그 최악값을
    간신히 덮는 값이다. 더 앞당기면 추천이 없는 채로 `execute_buys`가 돌아 그날이 통째로 빈다.
    실측 소요 57~66초 기준으로는 3배 이상의 여유다
  - **노림수는 갭 허용 창이다.** 추천→매수 간격이 5분에서 3분으로 줄어, 그 사이 가격이
    `[추천시점가 ×0.99, 목표가 ×1.02]`를 벗어나 스킵되는 경우(08-19 DB손해보험 5분에 +4.0%)가
    줄어들 것으로 본다. 개장 직후의 상승분을 3분 더 잡는 효과도 함께 노린다
  - **대가**: `today_volume`이 개장 2분치 누적이라 장초 거래량의 표본이 얇아지고, `today_change_rate`도
    첫 2분 변동이라 흔들림이 커 "당일 하락 출발" 필터(5.5-B '당일 지표 병행 수집')가 엉뚱하게 걸릴
    여지가 있다. 후보가 하한 미만으로 줄면 필터를 통째로 걷는 기존 안전장치가 그대로 받는다
  - **미체결 취소 10:10은 그대로다** — 매수가 3분 앞당겨진 만큼 되돌림을 기다리는 시간이 그만큼 늘어난다
- **가격 지정에서 범위 지정으로 (확정 2026-08-26)**: 매수 지정가를 **목표 매수가**에서 **허용 밴드
  상단(`목표가 × (1 + 갭 상승 허용치)`, 호가 단위 내림)**으로 옮긴다. 두 갭 판정은 그대로 두고
  주문가만 바뀐다. 상세는 5.5-B "주문 방식" 참고.
  - **사용자 지시다.** 요구는 한 문장 — "갭 상승·하락 허용치 내에 있으면 매수가 되어야 한다."
    갭 판정이 "이 종목은 참여해도 좋다"고 통과시켰으면 실제로 사져야 하는데, 종전 구조는 통과시킨
    종목의 상당수를 살 수 없었다
  - **계기 — 2026-08-26 (015760)한국전력**: 목표가 33,200원, 09:05 현재가 약 33,400원(전일 종가
    32,700원 대비 +2.14%), 허용 상한 34,196원(허용치 3%). **밴드 안이라 갭 판정을 통과해 주문까지
    접수됐지만**, 지정가가 33,200원이라 시장가 아래에 놓여 종일 체결되지 않았고 10:10에 취소돼
    그날 투입 0원으로 끝났다. 같은 날 나머지 1종목(000150 두산)은 1주 가격이 배정액을 넘어
    스킵됐으므로, 이 한 건이 그날의 전부였다
  - **구조적 결함이었지 그날의 불운이 아니다.** `목표가 < 현재가 ≤ 상한` 구간은 갭 판정이
    통과시키면서 지정가로는 절대 체결되지 않는 **죽은 구간**이었다. 목표 매수가는 프롬프트가
    눌림을 기다리라고 시켜 현재가보다 낮게 나오는 경우가 많으므로(2026-08-20 "매수 타이밍 조정"
    참고), 이 구간에 걸리는 것이 예외가 아니라 흔한 경우다
  - **왜 `max(목표가, 현재가)`가 아니라 상단인가**: 현재가로 내는 것도 결국 **한 점**이라, 조회와
    주문 사이에 호가가 한 틱만 올라가면 다시 미체결이다. 지정가 매수는 그 가격 **이하**의
    매도호가에 체결되므로, 상단에 걸어야 밴드 안 어떤 가격이든 받아낼 수 있다. 그러고도 상단을
    넘겨 지불하는 일은 없다
  - **대가 — 갭 상승 허용치가 최악의 체결가가 된다.** 종전에는 이 값이 참여 여부만 정해서 넉넉히
    잡아도 손해가 없었지만(넘겨도 어차피 미체결), 이제는 목표가보다 그만큼 비싸게 살 수 있다는
    뜻이다. **운영값 3%는 손절선(3%)과 같아 진입 직후 손절 구간에서 시작할 수 있다** — 값 조정은
    이 항목과 분리해 실매매를 며칠 보고 판단한다. `.env`·기본값·`.env.example`은 건드리지 않았다
  - **갭 하락 허용치는 역할이 그대로다** — 밴드 하단은 참여 판정선일 뿐 주문가가 아니다. 현재가가
    목표가보다 낮은 구간에서는 종전에도 지정가가 시장가 위에 있어 즉시 체결됐으므로, 이번 변경이
    건드리는 것은 밴드의 **위쪽 절반뿐**이다
- **익절·손절 폭 통합 (확정 2026-08-21)**: UI의 익절(%)·손절(%) 두 입력란을 **"익절/손절 (%)" 한 칸**으로
  합친다. 한 값이 익절 +N%·손절 -N%를 함께 정한다. 상세 규칙은 5.5-B "익절·손절 폭 통합" 참고.
  - **사용자 지시다.** 계기는 실제 `.env`가 이미 `TAKE_PROFIT_PERCENT=3`·`STOP_LOSS_PERCENT=3`으로
    같은 값이었다는 것 — 매번 같은 숫자를 두 칸에 넣고 있었으므로 칸을 나눠 둘 이유가 사라졌다
  - **데이터 모델은 건드리지 않았다.** `.env` 키 두 개, `Settings`의 두 필드, `RiskManager`의 두 인자가
    모두 그대로다. 통합은 UI 표시상의 요구이므로 UI에서 끝낸다 — 키를 `EXIT_PERCENT` 하나로 합치는 안은
    `Settings`·`RiskManager`·테스트·`.env.example`이 전부 따라 바뀌고 기존 `.env` 호환도 깨져서 택하지 않았다
  - **폭과 스위치를 구분한 것이 이 설계의 요점이다.** 합친 것은 폭(숫자)뿐이고, 켜고 끄는 체크박스는
    익절·손절 각각 남는다. 스위치까지 합치면 "익절만 끄고 손절만 남긴다"가 불가능해지는데, 그것이
    익절/손절 적용 여부(2026-08-11)의 도입 목적이었다
  - **대가**: 익절선과 손절선을 다른 폭으로 두는 조작이 UI에서 사라진다. 08-12 대조(27건)가 익절선
    후보로 0%~4%를 훑었던 것처럼 두 값을 따로 튜닝하고 싶어지면, `.env`를 손으로 고쳐도 저장 한 번에
    덮이므로 UI를 되돌려야 한다
- **뉴스/공시 소스: DART 전자공시 (확정 2026-08-12)**: 미결로 남아 있던 "당일 데이터"의 뉴스/공시 항목을 **DART OpenAPI 공시검색(`list.json`)**으로 확정한다. 키움 API가 뉴스를 제공하지 않는 것은 2026-08-05에 `ka10001` 응답 필드 전수 확인으로 결론났고, 그때 걸림돌로 본 고유번호 매핑은 응답의 `stock_code`로 대체돼 필요 없어졌다.
  - **네이버 뉴스 검색 API는 쓰지 않는다** — 검색어가 종목명이라 동명 노이즈와 중복 기사가 심하고 종목당 호출이 필요하다. 재료의 구체성 면에서는 공시보다 낫지만, 공시를 먼저 붙여 보고 부족하면 그때 다시 검토한다. **구글 뉴스 RSS는 비공식 경로**라 형식 변경 위험이 있어 실계좌 판단 근거로 쓰지 않는다
  - 용도는 두 가지다: **①프롬프트의 재료 정보 보강**(공시 제목을 그대로 실어 LLM이 참고), **②악재 공시 종목의 후보 배제**(코드가 키워드로 판정). 상세는 5.5-B "공시 수집과 악재 배제" 참고
- 이메일 발송 방식: SMTP (UI에서 서버/계정/앱비밀번호 설정)
- **LLM 제공처/모델**: Anthropic Claude API (`ANTHROPIC_API_KEY`, 기본 모델 `claude-sonnet-5`)
- **추천이 설정된 종목 수 미만일 때의 자금 배분**: 종목당 투입금액을 (투입 비율 ÷ 추천 종목 수, 기본값 기준 예수금의 약 1/6)로 고정하고, 모자란 몫은 매수하지 않고 현금으로 유지 (`LLMMomentumStrategy.build_buy_plans`). 프롬프트가 상대 비교로 항상 설정된 개수를 채우도록 요구하므로(확정 2026-08-04) 이 분기는 전 종목 데이터가 무의미한 극단적 예외 상황 대비 안전장치로만 남는다
- **15:30 리포트의 정산 타이밍 (확정, 2026-07-30 → 2026-08-12 개정)**: 보유 종목이 전부 매도되면 체결 반영 후(60초) 즉시 리포트를 보내고 정기 발송은 생략한다. 그 외에는 정기 시각에 발송한다 (`DailyWorkflow.send_final_report`, `runtime.watch_closeout_report`, 5.11절). **정기 시각과 청산 시각은 아래 항목에서 15:35 / 15:15로 옮겼다**
- **청산 시각과 리포트 정산 (확정, 2026-08-12)**: 강제청산을 **15:20 → 15:15**로, 정기 리포트를 **15:30 → 15:35**로 옮기고, 청산 즉시 리포트는 **미체결 매도가 남아 있으면 보류**한다.
  - **원인**: 15:20은 장마감 동시호가(15:20~15:30) 시작 시각이라, 그때 낸 시장가 청산 주문은 15:30 종가에야 체결된다. 그런데 엔진은 주문 **접수**를 청산 완료로 보고(`_mark_exited` → `closeout_report_due`) 60초 뒤 최종 리포트를 보냈고, 그 시점 체결내역에 없는 매도는 `pending`으로 남아 집계에서 빠졌다. 게다가 발송 표시가 서서 정기 리포트까지 생략돼 **정산본이 영영 나가지 않았다** (2026-08-12 실제 발생: 032640 60주 매도가 리포트에 "보유중"으로 실림)
  - **세 가지를 함께 고친다**. 15:15 이동만으로는 체결이 늦어지는 다른 상황(호가 공백·부분체결)이 남고, 발송 보류만으로는 매번 리포트가 정기 시각까지 밀리며, 15:35 이동만으로는 청산 즉시 발송 경로가 그대로 미정산으로 나간다
  - **`pending`은 그대로 두고 지우지 않는다** — 접수됐지만 체결이 확인되지 않은 주문이라는 사실 자체가 기록이다. 대신 그 상태에서는 "정산 완료"로 취급하지 않는다
  - **지난 날짜의 `pending`을 나중에 채우는 보정은 넣지 않는다** — 체결내역 조회(`ka10076`)가 당일분만 주어 별도 TR이 필요하고, 위 세 가지로 애초에 `pending`이 남지 않게 하는 편이 싸다. 이미 남은 건은 수동 보정한다 (8절 "리포트 타이밍 리스크")
- **LLM 추천 시각 설정 가변화 (확정, 2026-08-04)**: 고정값 08:45를 UI 콤보박스로 08:40~08:55(5분 단위, 기본 08:45) 조정 가능하게 변경한다. `.env`(`RECOMMEND_TIME`)에 `"HH:MM"` 문자열로 저장하고 `Settings.recommend_time`이 `datetime.time`으로 환산해 `build_runtime`의 스케줄 등록에 넘긴다 (5.5-B "추천 시각 설정" 참고)
- **예수금 투입 비율·추천 종목 수 설정 가변화 (확정, 2026-08-03)**: 기존 고정값(투입 비율 50%, 추천 종목 수 3개)을 UI 콤보박스로 조정 가능하게 변경한다 — 투입 비율 10~100%(10% 단위), 추천 종목 수 1~10개(1개 단위). `.env`(`INVESTABLE_RATIO_PERCENT`, `TARGET_STOCK_COUNT`)로 저장되며 LLM 프롬프트의 추천 개수 지시문도 함께 가변화된다. 설계 문서: `docs/superpowers/specs/2026-08-03-fund-allocation-settings-design.md`

---

## 11. 구현 상태 (2026-07-30 코드 재스캔)

프레임워크(스케줄러·리스크·로깅·알림)와 1호 전략 로직(자금배분·LLM 파싱·이메일 본문), 그리고 아래 키움 API 연동부까지 전부 구현되어 단위테스트(403개, 2026-08-12 기준)를 통과한 상태다. 2026-07-27 시점에는 인증 단계에서 막혀 있었지만(옛 버전의 11절), 그 뒤로 전부 채워졌다 — 실행을 막는 것은 이제 코드가 아니라 사용자가 준비해야 할 자격증명뿐이다 (11.3).

### 11.1 남은 미구현 항목 (실제 `NotImplementedError`, 총 2건)

| # | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | 분봉 조회 | `src/api/market_data.py:141` (`get_ohlcv`, `period != "D"`) | 일봉(`period="D"`)만 지원. 현재 전략·수집 로직 어디서도 분봉을 요구하지 않아 실사용 영향 없음 |
| 2 | 스탑오더 등록 | `src/api/order.py:184` (`send_stop_order`) | 의도적 스텁 — 키움 REST가 조건부/스탑 주문을 제공하지 않는다고 확인됨(5.5-B, 10절 확정 항목). 향후 키움이 지원을 추가할 경우를 위한 자리로만 남긴다 |

종전 1번이던 **뉴스/공시 헤드라인 수집은 2026-08-12에 구현됐다** (11.2의 21번) — 키움이 뉴스를 주지 않아 미뤄뒀던 항목을 DART 전자공시로 채웠다.

### 11.2 구현 완료 (2026-07-27 문서 대비 갱신)

| # | 항목 | 위치 | 비고 |
|---|---|---|---|
| 1 | API base URL | `config/settings.py:16-25` | 키움 도메인(`api.kiwoom.com` / `mockapi.kiwoom.com`, 모드별 분기)으로 구현됨 |
| 2 | 접근토큰 발급/갱신 | `src/api/auth.py` | 만료 5분 전 자동 재발급(`_is_token_valid`)에 더해, 서버가 토큰을 거부하면 `refresh_token`으로 강제 재발급하고 `KiwoomClient`가 1회 재시도한다 (10절 "토큰 무효화와 자동 재발급", 2026-08-06 추가) |
| 3 | 잔고/예수금 조회 | `src/api/account.py` | `get_balance_snapshot` / `get_positions` / `get_cash` |
| 4 | 현재가 조회 | `src/api/market_data.py:59` | `get_current_price` |
| 5 | 주문 전송 | `src/api/order.py:189` | `_call_api` — 3회 재시도, 실패 시 REJECTED 반환 |
| 6 | 대형주 유니버스 조회 | `src/data/collector.py` | `ka10099` 기반 `LargeCapUniverse` — 거래정지·관리종목·투자경고 종목 제외 포함. 2026-08-06에 중형주를 다시 뺐다 (10절 "유니버스 기준" 확정 항목 참고) |
| 7 | 실시간 시세 수신 | `src/api/websocket_client.py` | `subscribe`/`connect`/`disconnect`, 끊김 시 자동 재접속 |
| 8 | 일봉 조회 · 전일 지표 | `src/api/market_data.py` | `get_ohlcv`(`period="D"`), `get_previous_day_metrics`(전일 종가·고가·저가·등락률·거래량 + 급증 배수). 응답 필드는 실제 호출로 확인했다(`date`/`close_pric`/`flu_rt`/`trde_qty`, 한 호출에 20거래일 + 당일 봉). **2026-08-05 이전 `get_ohlcv`는 필수값 `qry_dt`를 빈 문자열로 보내 호출 시 항상 실패했다**(`return_code=2`) |
| 9 | 등락률·시가갭 계산 | — | **2026-08-06 제거.** `get_stock_detail`/`StockDetail`은 장 전 동시호가 예상체결가로 당일 지표를 만들려 했으나 그 값이 실제로 오지 않아 무용지물이었다 (10절 "장 전 당일 지표 부재") |
| 10 | 주문 정정/취소 | `src/api/order.py:134,152` | `cancel_order`는 10:10 미체결 매수 취소에서 사용한다(2026-08-06 도입, 2026-08-14에 09:30 → 10:10). `modify_order`는 아직 호출하는 곳이 없다 |
| 11 | 호가 조회 | `src/api/market_data.py:153` | `get_orderbook` |
| 12 | UI 엔진 실행/정지 및 즉시 실행 | `src/ui/main_window.py`, `src/ui/engine_thread.py` | 시작/정지, ①~⑤ 즉시 실행 버튼(① 추천 / ② 매수 / ③ 미체결 취소 / ④ 청산 / ⑤ 리포트), 보유 종목 표의 선택 매도, 매도 불가 표까지 구현 완료 (5.10·5.11절) |
| 13 | LLM 추천 프롬프트 v10 (2026-08-20) | `src/llm/recommender.py` `build_system_prompt` | **v10은 추천 대상을 낙폭을 되돌리는 구간(`rebound`)의 종목으로 제한한다** — 응답에 `setup` 필드(`rebound`/`breakout`/`momentum`)를 요구하고, 판단 기준의 1순위를 '최근 가격대에서의 위치'로 올렸으며, 정원을 채우려 다른 유형을 섞지 말라고 명시한다. 코드의 `drop_other_setups`가 같은 제한을 한 번 더 강제한다 (5.5-B '추천 유형 제한'). 이하는 v9까지의 이력이다. **v9는 "장 시작 전이라 당일 지표가 없다"는 전제를 걷어내고 당일 등락률·현재가·장초 거래량·전일 변동폭을 후보 데이터에 싣는다**. 목표 매수가 지침의 기준도 전일 종가 → **당일 현재가**로 바뀌며, `normalize_target_price`의 ±5% 가드레일 기준도 함께 옮긴다 (10절 "개장 후 추천으로 이동"). 이하는 v8까지의 이력이다. v2는 확신 없으면 3개 미만도 허용했으나 0개 추천이 잦아 v3에서 항상 채우도록 변경했고, v4는 규모별 배분과 거래량 급증 배수를 1순위 기준으로 추가했다. v5는 판단 근거를 전부 전일 데이터로 바꾸고(v4의 "당일 거래량 2배 이상"은 09:00 시점에 성립할 수 없었다 — 실측 최댓값 0.24배) 종목별 목표 매수가를 함께 요구했다. v6은 최근 고가·저가·이동평균을 판단 기준과 목표가 산정 지침에 추가했다 — 전일 하루만으로는 그 가격이 최근 범위에서 비싼 쪽인지 알 수 없었다. **v7은 종목별 목표 매도가(`target_sell_price`)를 함께 요구**한다 — 주문에는 쓰지 않고 추천 메일·로그에만 남겨, 종목별 익절선으로 쓸 만한 값인지 관찰한다. **v8은 판단 기준의 '뉴스/공시' 항목을 DART 공시 제목으로 명시**하고, 명백한 악재 공시 종목은 이미 후보에서 빠졌음을 알린다 — 종전에는 채워질 일이 없는 근거가 기준으로만 떠 있었다. `temperature`는 최신 Claude 모델이 비기본값을 거부(400)해 넣지 않음 — 필요하면 `output_config.effort`로 별도 검토 |
| 33 | 리포트에 연 누적 블록·그래프 (2026-09-01) | `src/logger/trade_store.py` `yearly_summary`/`yearly_cumulative_series`/`_range_summary`, `src/notification/chart.py` `render_yearly_cumulative`, `src/notification/templates.py` `_cumulative_items`/`_chart_img`, `src/core/daily_workflow.py` `YEARLY_CHART_CID` | 일일 리포트 메일의 월 블록 아래에 '올해 누적'을 같은 꼴로 붙이고, 달별 누적 꺾은선을 두 번째 CID 첨부로 싣는다. 집계·렌더·HTML 조각을 월 쪽과 공유해 두 블록의 규칙이 갈리지 않게 했다 (5.11) |
| 32 | 단일 인스턴스 판정을 세션 너머로 (2026-09-01) | `scripts/run_ui.py` `SINGLE_INSTANCE_MUTEX`/`_mutex_says_running`/`_running_instance_pid`/`_replace_running_instance`/`_close_windows_of` | 뮤텍스에 `Global` 접두사를 붙이고, PID 대조에서 '열지 못한 프로세스'를 살아 있는 것으로 본다. 대체에 실패하면 새 인스턴스를 띄우지 않고 `sys.exit(1)`로 끝내며, 다른 세션이라 창을 찾지 못하면 15초를 기다리지 않고 바로 강제 종료한다 (10절 "인스턴스가 세션을 넘어 쌓인 사고") |
| 30 | 무인 실행과 인스턴스 대체 (2026-08-27) | `scripts/run_ui.py` `_replace_running_instance`/`_write_pid`/`PID_PATH`/`INSTANCE_REPLACE_TIMEOUT_SECONDS` | `--auto-start`로 뜬 경우에만 기존 인스턴스를 종료하고 새로 시작한다 — 무인 실행에서는 '이미 실행 중' 대화상자를 닫을 사람이 없어 그날 매매가 통째로 빠진다. WM_CLOSE로 정상 종료를 먼저 시도하고, 보유 종목 확인 팝업에 걸려 시간 안에 안 끝나면 강제 종료한다. 대상은 PID 파일로 찾되 뮤텍스 존재와 실행 파일명(python/pythonw)으로 이중 확인해 PID 재사용 오살을 막는다. 수동 실행은 종전대로 막는다 (10절 "무인 실행과 인스턴스 대체") |
| 29 | 체결 반영 시차를 잔고로 메움 (2026-08-26) | `src/core/daily_workflow.py` `buy_plan_snapshot`/`_settled_row`/`_cancel_ordered_plans`, `BUY_PARTIAL_STATUS` | 매수 예정 표가 `접수` 행을 엔진 잔고 캐시와 대조해 완전 체결이면 빼고 부분체결이면 상태를 바꾼다 — 체결 반영이 10:10·15:15에만 돌아 그 사이 같은 종목이 보유 종목 표와 겹쳐 실렸다. 선택 삭제의 취소 실패도 '이미 체결됨'과 '진짜 실패'로 나눠, 체결된 주문에 "주문이 살아 있습니다" 알림이 나가지 않게 한다 (10절 "체결 반영 시차를 잔고로 메운다", 5.10) |
| 28 | 선택 삭제를 접수 행까지 확대 (2026-08-26) | `src/core/daily_workflow.py` `drop_buy_plans`/`_cancel_ordered_plans`/`BUY_ORDERED_STATUS`, `src/core/runtime.py` `ORDER_ACTIONS`, `src/ui/main_window.py` `_can_drop`/확인 팝업 문구 | 매수 예정 표의 '선택 삭제'를 `접수` 행에서도 쓸 수 있게 하고, 그 경우 미체결 매수 주문을 그 자리에서 취소한 뒤 행을 `미체결 취소`로 바꾼다. 종전에는 체크박스가 붙는 `매수 대기` 상태가 추천~매수 3분 동안만 존재해 열과 버튼이 종일 죽어 있었다. 실주문 취소를 부르므로 `drop_plan`이 `ORDER_ACTIONS`로 옮겨간다 (10절 "선택 삭제를 접수 행까지 확대", 5.10 "선택 삭제") |
| 27 | 가격 지정 → 범위 지정 매수 (2026-08-26) | `src/core/daily_workflow.py` `_gap_note`/`execute_buys` | 매수 지정가를 목표 매수가에서 **허용 밴드 상단**(`목표가 × (1 + 갭 상승 허용치)`, 호가 단위 내림)으로 옮긴다. 두 갭 판정과 밴드 자체는 그대로이고, 밴드 안이면 반드시 체결되도록 주문가만 바뀐다 — 종전에는 `목표가 < 현재가 ≤ 상한` 구간이 판정은 통과하면서 지정가로는 체결되지 않는 죽은 구간이었다(2026-08-26 015760 한국전력, 투입 0원). 수량 산정 기준도 목표가 → 주문가로 바뀌고, 현재가 조회 실패 시에만 종전 목표가 지정가로 되돌아간다. 대가로 갭 상승 허용치가 최악의 체결가가 된다 (10절 "가격 지정에서 범위 지정으로", 5.5-B "주문 방식") |
| 16 | 목표가 지정가 매수 · 09:40 미체결 취소 (2026-08-06) | `src/core/daily_workflow.py` `execute_buys` / `cancel_unfilled_buys` | LLM이 낸 목표가를 호가 단위·±5% 가드레일로 보정(`recommender.normalize_target_price`)해 09:10에 지정가 주문하고, 09:40에 미체결분을 취소한 뒤 매수 결과 메일을 보낸다. 취소 대상은 당일 체결내역 조회에서 찾고 매수 기록은 `data/buy_records.json`에 남겨, 엔진이 재시작돼도 취소와 결과 메일이 모두 누락되지 않는다. 09:40을 놓친 날은 15:15 마감 정리(`runtime.close_out`)가 청산 전에 한 번 더 거둔다 (2026-08-07). 시각은 2026-08-14에 09:00/09:30 → 09:10/09:40으로 옮겼다 |
| 17 | 익절 기본값 0.5% (2026-08-06) | `config/settings.py` `take_profit_percent`, `src/risk/manager.py` `check_portfolio_exit` | 순손익 기준 익절선 기본값을 2% → 0.5%로 낮췄다. UI 익절(%) 입력란과 `TAKE_PROFIT_PERCENT`는 그대로 유지한다 |
| 18 | 합산 손익 기준 익절/손절 (2026-08-10) | `src/risk/manager.py` `portfolio_net_return`/`check_portfolio_exit`, `src/core/engine.py` `_execute_portfolio_exit` | 종목별 판정(`check_exit`)을 없애고 보유 종목 전체 합산 순손익률로 판정한 뒤 전량 매도한다. 매도 루프는 종목별 청산(`_execute_exit`)을 그대로 재사용해 매도가능수량·주문 거부·상장폐지 제외 처리를 공유하고, 알림만 한 통으로 묶는다 (10절 "합산 손익 기준 익절/손절") |
| 24 | 단일 인스턴스 강제 (2026-08-13) | `scripts/run_ui.py` `_already_running`/`SINGLE_INSTANCE_MUTEX` | Windows 명명된 뮤텍스로 중복 실행을 막는다 — 이미 떠 있으면 창을 만들기 전에 `MessageBoxW`로 알리고 종료한다. 두 번째 인스턴스가 같은 앱키로 토큰을 발급하면 먼저 돌던 엔진의 감시가 멈추기 때문이다. 뮤텍스는 프로세스가 죽으면 OS가 놓아 락 파일과 달리 stale lock이 없고, 검사가 실패하면 막지 않고 진행한다 (10절 "단일 인스턴스 강제") |
| 23 | 단순익절 기본 적용 · 시각 원복 제거 (2026-08-13) | `src/risk/manager.py` `simple_take_profit_enabled` 기본값, `src/ui/main_window.py` 체크박스 초기 상태 | 기본 상태를 **손절 + 단순익절**로 되돌려 평소 익절을 다시 단순익절이 맡게 한다(합산 퍼센트 익절은 계속 기본 해제). 함께 매일 08:00에 체크박스를 되돌리던 UI 타이머(`EXIT_FLAG_RESET_TIME`, `_reset_exit_flags_daily`)를 **제거해**, 기본 상태는 프로그램 시작 시에만 적용된다 — 대가로 여러 날 연속 켜 두면 어제 바꾼 상태가 오늘까지 간다 (10절 "단순익절 기본 적용", "청산 조건 시각 원복 제거") |
| 26 | 퍼센트 익절 기본 적용 (2026-08-18) | `src/risk/manager.py` `take_profit_enabled`/`simple_take_profit_enabled` 기본값, `src/ui/main_window.py` 체크박스 초기 상태 | 기본 상태를 **손절 + 합산 퍼센트 익절**로 바꾼다 — 익절 '적용'이 시작 시 켜지고 단순익절은 기본 해제가 된다. 판정 단위가 종목별에서 보유 합산으로, 익절선이 0에서 `TAKE_PROFIT_PERCENT` 입력값으로 바뀌며, 걸리면 전량 매도한다. 08-12의 27건 대조 결론(익절선을 어디에 두든 '익절 없음'보다 못했다)과는 방향이 어긋나므로 표본이 쌓이면 재대조 대상이다 (10절 "퍼센트 익절 기본 적용") |
| 25 | 개장 후 추천으로 이동 (2026-08-14) | `src/core/runtime.py` `BUY_TIME`/`CANCEL_UNFILLED_TIME`, `src/data/collector.py` `PRESCREEN_SIZE`/당일 지표 수집, `src/llm/recommender.py` `normalize_target_price`/`StockRecommendation.recommend_price`, `src/core/daily_workflow.py` `_gap_note`, `src/ui/main_window.py` 추천 시각 콤보박스 | 추천 09:05 / 매수 09:10 / 미체결 취소 09:40으로 옮기고, 전일 급증 배수 상위 40종목에 `ka10001`을 태워 당일 등락률을 직접 계산해 하락 출발 종목을 후보에서 뺀다. 목표가 가드레일과 주문 직전 갭 하락 판정의 기준값이 각각 당일 현재가·추천 시점 가격으로 바뀐다. 전일 변동폭은 프롬프트에만 싣고 거르지 않는다 (10절 "개장 후 추천으로 이동", 5.5-B "당일 지표 병행 수집") |
| 19 | 단순익절 (종목별, 2026-08-12) | `src/risk/manager.py` `check_simple_take_profits`, `src/core/engine.py` `_execute_simple_take_profit`, `src/ui/main_window.py` "단순익절적용" 체크박스 | 익절선을 `TAKE_PROFIT_PERCENT` 대신 0으로 두고 **종목별로** 판정해, 순손익이 0을 넘은 종목만 매도한다(나머지는 계속 보유). **기본 적용**이라 평소 익절은 이 방식이며, 켜져 있는 동안 익절(%) 입력란은 비활성화된다. 익절 "적용"(합산 퍼센트 익절)과 배타적이다. 손절·퍼센트 익절의 합산 판정을 먼저 보고, 이익 종목이 먼저 빠지면 합산 손절이 더 쉽게 걸린다 (5.5-B "단순익절") |
| 20 | 청산 시각·리포트 정산 (2026-08-12) | `src/core/runtime.py` `FORCE_CLOSE_TIME`/`REPORT_TIME`, `src/core/daily_workflow.py` `send_final_report`, `src/logger/trade_store.py` `has_unsettled_sells` | 강제청산 15:15, 정기 리포트 15:35로 이동하고, 청산 즉시 리포트는 당일 매도 주문에 미체결 건이 남아 있으면 발송 표시를 세우지 않고 건너뛴다 (10절 "청산 시각과 리포트 정산") |
| 22 | 익절 기본 해제 · 청산 사유 기록 (2026-08-12) | `src/risk/manager.py` 플래그 기본값, `src/ui/main_window.py` 체크박스 초기 상태, `src/logger/trade_store.py` `record_fill`, `src/core/engine.py` `_record_trade` | 익절 두 방식을 기본 해제로 바꿨다(기본 상태 = 손절만) — 근거는 10절 "익절 기본 해제"의 27건 대조다. **단순익절 부분은 2026-08-13에 철회됐다**(아래 23번). 함께 `trades.exit_reason` 컬럼을 추가해 매도가 어느 경로로 나갔는지(`stop_loss`/`take_profit`/`simple_take_profit`/`day_end`/`manual`/`manual_selected`) 남긴다. 종전에는 규칙이 판 것과 사람이 판 것을 가르려면 로그를 파싱해야 했고, 실제로 이번 분석에서 걸림돌이었다 |
| 21 | DART 공시 수집·악재 배제 (2026-08-12) | `src/data/disclosure.py` `DisclosureClient`/`blocking_disclosure`, `src/data/collector.py` `_apply_disclosures` | 직전 거래일~당일 코스피 공시를 날짜 범위로 일괄 조회(`corp_cls=Y`, 페이지네이션)해 응답의 `stock_code`로 유니버스에 붙인다 — 고유번호 매핑 파일이 필요 없어 종전에 미뤘던 걸림돌이 사라졌다. 배제 키워드(유상증자·사채 발행·횡령·배임·회계처리기준 위반·불성실공시법인)에 걸린 종목은 **쇼트리스트 이전에** 빼고, 나머지 공시 제목은 종목당 3건까지 프롬프트에 싣는다. `DART_API_KEY`가 없거나 조회가 실패하면 공시 없이 진행한다(실패 시 운영 알림). 종전 스텁 `NewsClient`를 대체 (5.5-B "공시 수집과 악재 배제") |
| 15 | 추천 입력 로깅 (2026-08-05) | `src/data/collector.py` `_log_candidates`, `src/llm/recommender.py` | LLM에 넘긴 후보 목록과 프롬프트 원문을 로그에 남긴다 — 추천이 타당했는지 사후 검증할 유일한 근거 |
| 14 | 추천·매수 시각 설정 (2026-08-04, `.env` 전용 전환 2026-08-20) | `config/settings.py` `recommend_time`/`buy_time`/`validate`, `src/ui/main_window.py` | `.env`(`RECOMMEND_TIME`·`BUY_TIME`) → `build_runtime` 스케줄 등록. UI는 "매매 시각" 라벨로 값만 표시한다(콤보박스 제거). 값이 깨지면 기본값 **09:05**·**09:08**로 돌리고, 개장 전 추천이나 추천 +3분 미만 매수는 `validate()`가 엔진 시작을 막는다로 폴백하며 경고 로그를 남긴다. 도입 당시 범위는 08:40~08:55(기본 08:45)였고 2026-08-14에 옮겼다 |

### 11.3 모의투자 지원 현황 (2026-07-27 "미지원" 판단 철회, 그 뒤 복원됨)

2026-07-27 당시 한 차례 "모의투자 서버 사용 불가"로 판단해 UI를 실전 고정으로 바꿨으나, 이후 모의투자 서버(`mockapi.kiwoom.com`) 연동이 복원되어 현재는 아래와 같다.

- **매매 모드**: UI 라디오 버튼(모의투자/실전)으로 전환하며, 기본값은 모의투자
- **실전 확인**: 실전을 선택하고 설정을 저장하면 `LIVE_TRADE_CONFIRMED=YES_I_UNDERSTAND`가 자동으로 `.env`에 반영된다(`MainWindow._save_settings`). 이 값이 없으면 `Settings.validate()`가 실전 시작을 거부하고, 매수·매도가 나가는 즉시 실행(②·③·④·선택 매도·전체)은 실전 모드일 때 확인 대화상자에 경고 문구가 추가로 붙는다
- `TRADE_MODE` 기본값은 `paper` (5.1절과 일치 — 옛 버전 문서의 `live` 강제는 더 이상 적용되지 않는다)

### 11.4 사용자 준비 사항 (코드 외)

- 키움증권 계좌 개설 및 Open API 사용신청 → 앱키/시크릿 발급 (Phase 0)
- Anthropic API 키 발급 → `.env`의 `ANTHROPIC_API_KEY` (UI "계좌 / API 설정"의 'Anthropic Key' 칸에서도 수정 가능, 2026-08-25). 모델명(`LLM_MODEL`)은 `.env` 전용이다
- DART 전자공시 OpenAPI 키 발급(opendart.fss.or.kr, 무료) → `.env`의 `DART_API_KEY`. 없어도 실행은 되지만 공시 수집·악재 배제가 동작하지 않는다
- SMTP 발송 계정 준비 (Gmail 앱 비밀번호 등) → UI에서 입력

---

*본 문서는 초안이며, 키움증권 공식 API 문서 확인 및 개발 진행 과정에서 세부 요구사항이 조정될 수 있습니다.*
